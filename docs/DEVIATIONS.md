# Every deviation from the brief, with its reason

Brief §12 requires the fidelity report to state **every deviation with its reason**. This file is that
list. It is included verbatim as §9 of `docs/FIDELITY_REPORT.md`, and it is authored rather than
generated, because deciding what counts as a deviation is a judgement and not a query.

Each entry names the subsystem, what the brief asked for, what was built instead, why, and what would
close it. Every entry is drawn from the stage report cited beside it, where it is stated at length.
Nothing here is softened: where a number is embarrassing it is given as measured.

The four **structural** deviations come first, because they constrain everything else. The rest are
grouped by subsystem.

---

## A. Structural — these four constrain the whole build

| # | Deviation | Reason | What would close it |
|---|---|---|---|
| A1 | **The Unreal project is authored but never compiled, cooked or run.** 112 C++ files, 27,909 lines, verified only by static analysis. | No Unreal editor, no GPU and no Epic download in this environment (ADR-001). | One workstation pass following `unreal/README.md`. Nothing is known to be missing; nothing is proven to build. |
| A2 | **96.69 % of facades are inferred** — material, window pattern, trim and features come from a rule table keyed on PLUTO class, year built, LPC district and party walls, not from observation. Only 3.31 % carry a material from a real source, and only 0.20 % from OSM tags. | No lawful, feasible source of per-building street-level imagery for 1.08 M buildings here (ADR-004). | Licensed street-level imagery plus a vision model classifying material, window pattern and storefront per facade. The rule table is shaped so such a source replaces the `material_source == 3` rows without a contract change. |
| A3 | **52.43 % of roofs are inferred** from building class and footprint rather than measured, and gable-versus-hip is undetermined — every inferred pitched roof ships as a gable, of which about 36 % are really hips. Measured precision against the OSM sample is 0.805 and recall 0.456: roughly 1 in 5 inferred pitched roofs is wrong, and more than half of the genuinely pitched roofs are missed and stay flat. | The 2014 CityGML LOD2 model covers 95.42 % of footprints; the rest, and all roof *pitch*, is not published. ADR-013 records that the city model carries roof massing, not roof pitch. | A roof-plane classifier on the raw LiDAR point cloud, or an aerial photogrammetric mesh. |
| A4 | **Terrain is 0.384 m RMS vertical accuracy, not the 0.15 m the plan assumed**, and is built from the 3DEP 1 m product rather than the city's 1 ft DEM. | The measured spread is dominated by the time difference between sources (2013–14 LiDAR against 2022 planimetrics) and by real change, not noise — median offset −0.037 m. The 1 ft DEM is a 26.6 GB download against a ~30 GB disk allowance (ADR-005, ADR-017). | Re-run the same terrain stage against `NYC_DEM_1ft_Float`. No code change. |

---

## B. Buildings and facades — `docs/verification/{buildings,citygml,facade,buildings_mesh}/REPORT.md`

| # | Deviation | Reason | What would close it |
|---|---|---|---|
| B1 | **26.60 % of floor counts are derived from height**, not published. | PLUTO does not carry a floor count for those lots. The derivation divides a measured height by the class floor height. | Nothing available; PLUTO is the source of record. |
| B2 | **Facade heading is derived for 100 % of buildings.** No source publishes which side of a building is the front; 88.4 % use the longest non-party-wall edge tie-broken by lot position, and 212 buildings are fully enclosed by neighbours so their heading is arbitrary. | No published attribute exists. | Street-level imagery, as A2. |
| B3 | ~~Stepped massing was written and never applied~~ — **closed and verified from the shipped manifests.** **184,373 buildings** now carry real stepped massing across **872 tiles**, out of 307,735 candidates with more than one CityGML roof level — 60 % of the candidates and 17 % of the city. The setback towers of Midtown and Lower Manhattan are the visible case, and the before/after pair in `docs/verification/buildings_mesh/` shows a flat-topped slab becoming a five-level setback profile. | The code existed and had been applied to 8 tiles of 920. The level outlines were recovered from the CityGML `RoofSurface` triangles already on disk — 306,547 of 307,735 recovered, verified independently in that pass: the triangle blobs are in NYC_TM and reproduce each row's own bounding box to 0.0 m, and 99.1 % of recovered level heights land within 0.5 m of the published `roof_level_z`. | **What was deliberately *not* stepped is the interesting half.** 92,448 candidates keep their pitched roof instead, because `_build_stepped` caps every level flat and the lane found it was about to replace a gable with flat levels for the sake of a porch-sized step; a pitched building now keeps its roof unless the lowered part is at least a quarter of its plan. A further 40,401 traded pitch for step, 12,097 were rejected on step height and 2,009 on plan, and **8,792 were lost because the stepped shell would not close** — a real failure count, reported rather than absorbed; those losses concentrate in exactly the buildings the feature is for (median 8 plan regions against 2 for the ones that close). **Two limits survive the rebuild.** LOD2 and the merged L2/L3 skyline cells carry **no** steps at all — those tiers are convex-hull massing by ARCHITECTURE §4.3 — so a setback tower collapses back to a single block as soon as it is far enough away to drop a tier, which is the distance a skyline is normally looked at from. And 9,431 of the shipped carry measured step *depths* on an altitude that still holds a height-source offset; the manifest counts them apart as `applied_offset_z` rather than letting them pass as fully measured. |
| B4 | **49,880 storefronts carry generic wording** ("DELI GROCERY", "NAILS & SPA") rather than a real business name; 30,381 of 80,261 have a real name. | DCWP/DOHMH/OSM cover only part of the ground-floor stock. The `awning_real` column marks which is which. | A fuller business licence join. |
| B5 | **Wall billboards are deliberately not placed**, and 53 of 138 kit pieces are never placed. | There is no real source of NYC billboard locations here, and inventing advertising copy would be exactly the fabrication the brief forbids. The other 52 are detail pieces awaiting placement policy, not missing geometry. | A billboard location source; placement policy for the rest. |
| B6 | **Eaves are flush** — pitched roofs stop at the footprint ring with no overhang, fascia or soffit, which is visible on houses at street level. **8 domes are faceted pyramids and 28 complex roofs are flat-plus-parapet.** And the part this entry used to understate: `tiles/{tile}/roofs.glb` is contracted in DATA_CONTRACTS §4 and §5, **is not written for any of the 1,399 tiles that hold buildings, and 1,033,416 buildings — 95.42 % of the city — carry a `roof_mesh_ref` pointing into it** (`t_-10_-10/roofs.glb#bin_5042097` and a million more). Every one of those references is dangling. | Disk budget, and the dome/complex cases need `roofs.glb`. The dangling column survived because nothing resolves it: the reference is written by the CityGML stage, carried into every tile table by `blender/buildings/tiledata.py`, and read by no consumer anywhere — `pipeline/tests/test_citygml.py` asserts its *format* (non-empty exactly when `citygml_match`) and never that the target exists. | The roof *shape* is not missing from the world — it is baked into the shells, and the stepped massing was recovered from the same CityGML `RoofSurface` triangles — so this is a broken contract rather than absent geometry. Either write the stage, or drop the column and amend §5. What must not stay is a million rows pointing at a file that was never created. |
| B7 | **LOD ratios miss the brief's targets** — LOD1 42.6 % against ~35 %, LOD2 31.1 % against ~8 %. | Arithmetic, not laziness: a closed box costs 12 triangles and the mean LOD0 shell is 47.3, so the LOD2 floor on the small-house stock (70 % of the city) is about 25 %. Reaching 8 % needs open shells, which the brief forbids. | Nothing, without contradicting the no-open-shells requirement. |
| B8 | **213,451 accessory garages (19.7 % of footprints) share a class with multi-storey parking decks.** | `facade_classes.json` has one garage typology. Their material and roof are overridden from real lot evidence so the shells read correctly. | A class 57 `accessory_garage_1fl`, proposed to the kit lane. |
| B9 | **Shell storage is 5.70 GB, not ADR-003's ~1.3 GB estimate; placements are 1.68 GB, not "< 1 GB".** | The estimate did not budget for the LOD chain (+80 %) or per-building vertex attributes (62 % of the LOD0 payload). Draco compresses 4.7× but needs 16+ hours for the city here and makes the geometry unreadable to `pygltflib`. | Amend ADR-003 to ≈3 GB LOD0 / ≈5.7 GB with the chain, and treat Draco as a workstation packaging step. |
| B10 | **Multi-part complexes are one row per BIN, and interiors do not exist.** Every building is a shell. | The base table is keyed by BIN; the brief asks for a drivable exterior city. | Out of scope. |
| B11 | **New Jersey is built, but to a different standard than New York.** 231,382 shells across 486 tiles now stand on the New Jersey shoreline, from FEMA/ORNL USA Structures. Of them: footprints 100 % real, heights real for **73.7 %** and inferred from the ten nearest neighbours for the rest, ground sampled from this project's own terrain surface, and **nothing else measured at all** — no roof shape, no building class, no year built, no floor count, no material. `ROOF_REAL`, `FLOORS_REAL`, `YEAR_REAL`, `MATERIAL_REAL`, `SIGNAGE_REAL`, `SCAFFOLD_REAL`, `LANDMARK_MODEL` and `GROUND_REAL` are zero on every New Jersey row; the whole table holds just two distinct fidelity values. | There is no PLUTO analogue for New Jersey and no LOD2 city model. Floors come from height through an occupancy-class storey table and material from an occupancy × county × height rule; both carry `FLOORS_INFERRED` and `FACADE_INFERRED`. | New Jersey parcel data, if it can be licensed. |
| B11a | **The New Jersey tower heights are better and still wrong.** On the 25 Jersey City reference towers that predate the source imagery — the paired, like-for-like test — the median error went **−66.41 m (−54.4 %) → −53.35 m (−46.1 %)**, the count short by more than 20 m **20 → 16**, and the count within 10 % of published **0 → 6**. The tallest New Jersey building went 107.48 → **167.0 m** against a real tallest of 271 m. 10 towers improved, 28 were unchanged and **1 was made worse** (Southampton Apartments, where the OSM tag says 137.0 m against 105.5 m published). | The OSM extract's tags were joined onto the USA Structures footprints at IoU ≥ 0.5 — **105,644 matched pairs, a 45.66 % match rate**. The threshold is argued rather than tuned: if two outlines each reach IoU 0.5 against one polygon they must overlap each other, so contested matches collapse toward that bound, and the measurement follows it (1,546 contested at 0.40, 8 at 0.49, 0 at 0.50). Only **41 buildings took a real `height` tag** and **252 a storey derivation**, because the honest denominator is small: of 417 NJ height tags only 65 overlap this table at all. Containment matching was refused on purpose — Two Gateway Center's tower sits inside a superblock polygon at IoU 0.079, and accepting it would raise a whole city block to 83 m. | **`HEIGHT_REAL` went *down*, 170,547 → 170,391.** 158 rows lost it where a storey derivation replaced a real source height, against 41 gained from tags; the trade is visible in the bitfield rather than hidden. **99 Hudson Street, the real tallest at 271 m, is still absent** — its OSM outline carries the podium's `height=27`. 24 of the 47 OSM height tags over 40 m cannot be joined at all, each listed by name with its overlap; closing those needs footprint reconciliation, not a better height rule. |
| B11b | **Jersey City still cannot be seen from the Brooklyn Heights Promenade**, the frame that prompted this work. With the shells as built, 13 of the frame's 1,208 columns show a New Jersey roof clearing the New York skyline, by at most 5.6 px. | Geometry, not a missing shell: from that camera the Jersey City waterfront lies *behind* Lower Manhattan, which is half the distance and therefore angularly taller. At the towers' published heights it would be 17 columns and up to 27.8 px — a sliver either way. | Correct heights would make it a visible sliver, not a cluster. The earlier assessment's claim that the photograph shows the *Newport* towers there is wrong: Newport is at bearing 316.9°, 4.68 km out and 1.72° tall against an 8.09° Manhattan skyline. What is visible at the far left is the southern Jersey City / Paulus Hook cluster. |
| B11c | **17 structures on Ellis Island and Liberty Island were dropped** from the New Jersey table, and one LOD0 shell in tile `t_-12_20` is open. | Both islands are covered by USA Structures *and* by the NYC footprint table, several as hand-modelled landmarks; shipping both would have put two shells on one hospital. Dropped by centroid-inside or ≥1 m² overlap with the city boundary, following ADR-019's precedent. The open shell is the same defect class the NYC run reports. | — |

### The photorealism gap, stated as its own item

The comparison assessments are the most direct evidence in the project of the distance between what is built
and a photograph, and three findings recur in almost every frame. They are listed here rather than left in
the individual assessments, because together they are the honest answer to "is it photorealistic".

| # | Deviation | Reason | What would close it |
|---|---|---|---|
| B12 | **Building shells now carry real reflectance; the vertical banding does not.** Measured on the shipped glTF materials in a controlled A/B: on the Brooklyn Heights camera the tonal range (p01–p99) went **0.676 → 0.757**, standard deviation **+15 %**, and pixels below 0.10 luminance **0.20 % → 1.40 %**, with 35.9 % of pixels changed. On the Midtown aerial it is stronger — range **0.511 → 0.617**, s.d. **+44 %**, near-black pixels **0.01 % → 1.31 %**. That is the difference between a massing study and a surface that has depth. | The gap this entry originally described was overstated and has been corrected once already: the shells always carried per-class roughness and metallic, and what they lacked was transmission, IOR and specular. Those are now present. **The boundary, stated plainly: this is analytic shading, not measurement.** No reflectance was measured for any New York building. Every constant is authored per facade class and hangs off ADR-004's inferred class assignment, so it inherits that inference's own `MATERIAL_REAL` bit and earns no new claim of realness. | **Per-building variation is invisible to a plain glTF consumer.** It is a shader expression over the exported `_LIT_SEED_HI/LO`, so a viewer that renders the material exactly as the file authors it sees every building of a class at the class average; only an engine that evaluates the expression sees the spread the renders above measure. **The spandrel banding is not delivered and is the remaining half.** The Brooklyn Heights assessment asks for "strong vertical banding"; that needs a dark band at each floor line, which is neither geometry nor a glTF material property. `shellmat.floor_band_uv` records the expression an engine should evaluate, keyed on the metre UV against the exported `_FLOOR_HEIGHT`. Written up as a gap rather than closed, because a glTF viewer cannot express it. **The first of those two is no longer true of this project's own evidence.** The comparison renderer *was* the consumer that showed every building of a class at the class average, and since J63 it resolves the shell material through `blender/buildings/render_verify.py`, which evaluates the `_LIT_SEED` expression and adds the photographic surface the catalogue holds. The sheets from the eleventh render pass onward show the spread; the exported glTF still does not carry it, and an engine still has to evaluate it. The spandrel band remains undelivered. |
| B13 | ~~**Non-building structures do not exist, and this entry understated it by staying at ground level.**~~ **Closed for what was asked; the remainder is J25 and J31.** `data/processed/transit/rail_structures.parquet` held 8,341 structures -- 986.23 km of elevated, embankment, open cut and viaduct -- extracted, tiled, and consumed by exactly one file in the repository (`blender/landmarks/c_high_line.py`). Nothing in `unreal/` read them, so the J over Broadway in Bushwick and the 7 over Roosevelt Avenue were open sky, and two of the reference subjects are those exact streets. | The data was gathered and then had no consumer, which is the same shape as the `roofs.glb` nobody wrote and the three runtime files nothing read. | Done: the structures stage ships. **845 tiles, 197 MB, 3,568,448 triangles**, carrying **490.6 km of rail on 75,511 columns** from 4,574 rows, plus **1,330 stations on 11,229 canopy columns**, piers on their pile grids and the promenade decks. Soffit clearance is measured rather than assumed, over 53,778 samples, and each tile's manifest records its own median. 39 spans are ceded to the three bridge landmarks that already model them (Hell Gate 9, Manhattan 14, Williamsburg 16) and 154 rows are dropped as already being a building. The manifest names every published dimension it used. Unreal reads them: `tile_structures.glb` imports as `SM_Structures` under the `structure_nanite` rule and `build_levels.place_structures` puts it in the tile level with the `NYCTerrain` collision profile. What is still not built is the 314.7 km of embankment and 166.5 km of open cut (J25), and the rail decks of the Manhattan, Williamsburg and Hell Gate bridges (J31). |
| B14 | **Water is a mirror.** Roughness 0.06 with no wave normal map makes the East River a perfect reflector of sky and towers; the real surface at that distance is dark, broken and largely non-reflective. The mirrored towers below the waterline are the most conspicuously unreal thing in the frame. | The water normal map is not in the repository (see H3). It is bound by material parameter, so no graph editing is needed. | One texture. |

---

## C. Roads, signals and signs — `docs/verification/roads/REPORT.md`

| # | Deviation | Reason | What would close it |
|---|---|---|---|
| C1 | **Signals are a union of sources, not a published dataset.** 14,466 clusters against DOT's published ~13,700; 1,666 (8.4 %) are inferred and flagged. **Phasing is the DOT standard, not per-controller timing plans.** | DOT does not publish per-controller timing (ADR-007). | A DOT timing-plan release. |
| C2 | **123,891 regulatory signs (19.6 % of the layer) have derived positions.** ONE WAY, DO NOT ENTER, STOP, YIELD and most SPEED LIMIT signs. Their existence and control are real; their exact pole positions are not. | DOT publishes parking regulation signs only. | A sign inventory. |
| C3 | **148,674 crosswalk polygons are derived**, and driveway pavement is not produced at all. | No published crosswalk geometry was available. | A planimetric crosswalk layer. |
| C8 | **6.29 % of the city's drivable lanes are not mutually reachable, and the test cited as proof of the brief's first condition does not measure that.** `test_road_network_is_connected_enough_to_drive_across_the_city` is an *undirected* union-find asserting the largest component holds >90 % of nodes. It would pass on a city where every street is one-way into a cul-de-sac. Measured properly — strongly connected components over the 221,015 travel, bus and turn lanes — the largest component holds **207,118 lanes (93.71 %)** and **13,897 (6.29 %) sit outside it**, in 7,888 smaller components. A car on those cannot reach the rest of the city, or cannot be reached from it. | Not missing roads: only **50 lanes (0.02 %)** are fully isolated with neither successor nor predecessor, 1,834 (0.83 %) have no successor and 741 (0.34 %) no predecessor. The rest are ordinary streets — Beach 94th Street, Foch Boulevard, Rockaway Beach Boulevard, a BQE entrance ramp — sitting in one-way pockets that can be entered and not left. It is turn-restriction and one-way modelling at junctions, not absent geometry. | Now measured every run by `test_the_drivable_lane_graph_is_strongly_connected_not_merely_connected`, with the threshold at 93 % so a regression fails and an improvement does not. Closing the gap means auditing junction successor generation where a one-way pair meets a restricted turn; the 5,698 single-lane components are the place to start. |
| C4 | **7.4 % of widths, 7.1 % of lane counts and 17.4 % of posted speeds are inferred**, all flagged. 3,234 lane stacks are wider than CSCL's own `streetwidth` because its lane counts do not fit its width field. | The source is internally inconsistent for those rows. | — |
| C5 | **8 % of the drivable network is outside the largest strongly connected component** — gated, private and park roads CSCL records that do not join the public network. 0.83 % of travel lanes end without a successor. | Real property of the source. | — |
| C8 | **Central Park's car-free loop drives are modelled as ordinary streets, so the simulation drives traffic through the park.** 49 segments and **7.29 km** of `WEST DR` (22), `EAST DR` (11), `CENTER DR` (8) and `TERRACE DR` (8) carry travel lanes and posted speeds of 20 and 25 mph. The park drives have been closed to private vehicles since **27 June 2018**. The four transverse roads are a different case and are correctly open — 44 segments, and they must stay drivable. | CSCL is a centreline inventory and still carries the drives as roadway; nothing in the row says a vehicle may not use it, and no stage asked. Found from the other end: the agent placement lane refused all 52 vehicles the simulation put within 320 m of the Bethesda Terrace camera, by drive name inside the park boundary, so the *renders* are right and the *road graph* is not. | A closed-to-traffic attribute on the segment, so the restriction lives in the world rather than in one renderer's name list — the current fix is in `blender/verify/agents.py` alone and the Unreal runtime does not have it. Prospect Park was checked for the same fault and no drivable segment was found inside it; the other park drives closed since 2018 have not been audited. |
| C6 | **Bike lanes on one-way streets are always placed on the right of travel**; NYC also uses left-side protected lanes. Affects the side, never the presence, of 14,188 bike lanes. | `bike_trafdir` does not say which side. | A side attribute. |
| C7 | **The pavement layer is from an earlier run of the same code.** Its four planimetric sources were deleted mid-build to free disk, so the final rebuild ran `--no-pavement` and the 972 tile files are unchanged. | Disk exhaustion. The §8 numbers were recomputed by reading those files back, not copied from a log. | Re-download the four sources and re-run `nycsim_pipeline.roads.pavement`. |

---

## D. Street furniture, transit and traffic data — `docs/verification/{furniture,props,traffic_density}/REPORT.md`

| # | Deviation | Reason | What would close it |
|---|---|---|---|
| D1 | **31.6 % of props are rule placements** — 256,843 street lamps, 287,551 manholes, 1,402 steam vents, all flagged `source = 1`. Positions are plausible, not real. | The City publishes no citywide location dataset for any of the three. The lamp *count* is close to the published fleet size; the manhole and steam-vent counts are stated modelling choices. | A utility asset inventory. |
| D2 | **Tree height is estimated for all 650,516 trees.** | The census records species and trunk diameter but no height. The curve is a saturating fit to published mature heights; no per-tree truth exists to validate against. | — |
| D3 | **Prop z comes from survey points, not the terrain raster** the engine will render. | `tiles/{tile}/terrain.png` did not exist during that run. Flagged `z_source` 3/4; 0.58 % extrapolated from over 80 m away. | A resample pass against the finished terrain. |
| D4 | **GTFS is one representative weekday** (2026-09-09). Weekend, night and seasonal services are absent entirely. | The feed snapshot. | A multi-day GTFS ingest. |
| D5 | **Deck heights are 60.7 % measured**; 2,625 above-grade structures use their class median. Open-cut depth is not produced at all. | No *Bridge Elevation* point within 25 m. Measuring open cuts from surrounding bridge points would be wrong. | Denser elevation points. |
| D6 | **Manufacturer geometry is not reproduced** for the Better Bin, LinkNYC kiosk, Cemusa shelter and newsstand, Muni-Meter, Citi Bike bicycle and food carts — published dimensions and a recognisable silhouette, not part-for-part models. **Trees are procedural, not scanned. Sign legends are MUTCD defaults, not per-instance text.** | No licensed CAD (same statement as ADR-009 for vehicles). Real per-instance sign text arrives at runtime through the `SIGN_FACE` slot. | Licensed CAD; the `roads/signs.parquet` wiring. |
| D7 | **Citywide vehicle-kilometres land 15–30 % below the published state figure** (91.2 M against 105–130 M). **Weekend modal shares reuse weekday shares. Bicycle shares are not from the classification counts.** | DOT's classification counts are weekday-only and carry no bicycle class; the bicycle fit was scaled by 0.655 to reproduce the published citywide figure. Densities are per lane so the spatial pattern is unaffected, but the table must not be used as a citywide VMT estimate. | Weekend classification counts. |
| D8 | **Emergency-vehicle activity is the least grounded part of the fleet mix**, and **school buses have no vehicle class** — the bus share resolves entirely to transit buses. | Fleet sizes are published; daily distance and duty cycles are assumptions, each carrying a `basis` string. | Fleet telematics. |

---

## E. Vehicles and character — `docs/verification/{vehicles,character}/REPORT.md`

| # | Deviation | Reason | What would close it |
|---|---|---|---|
| E1 | **Vehicles are dimensionally exact but not photoreal.** Surfaces are lofted from station tables, so panel creases, shut lines, grille meshes and badge relief are approximate; `fusion_exterior.png` reads as *a* mid-size sedan of the right size, not as a recognisable 2019 Fusion. **No PBR texture maps at all** — materials are analytic, with no albedo, normal, ORM, dirt or wear layers. | No licensed CAD or scan data (ADR-009). Worst dimensional deviation is 1.85 %, most are 0.00 %. | Reference-photo modelling or scan data; an authored texture set. |
| E2 | **The player car's greenhouse is packaged too far forward.** The windscreen header sits 0.35 m ahead of the driver's eye where a real Fusion has ≈0.75 m; the interior render needed an 80° lens to get the A-pillars in shot. | A blueprint error in the fore-and-aft split between bonnet, screen and roof. Overall height, length and H-point are right. | Re-cut the `X_ROOF_F`/`X_COWL` stations, which moves every panel on the flagship model. |
| E3 | **Hair is a solid shell, not cards and not strands.** At portrait range it reads as a helmet: no flyaways, no strand shading, no anisotropic highlight, and no card silhouette either. Measured open-edge ratios across all ten MakeHuman CC0 hair assets: `short02` and `short03` are 0.05, which is a closed cap; `braid01` is 0.46 and still renders as a smooth helmet with no visible braid; only `afro01` (0.51) genuinely clumps. Alpha is wired on all ten — the meshes simply are not card-built. The lane calls this its single biggest fidelity gap and it is right. | Those ten assets are the entire MakeHuman CC0 hair library, so there is nothing better to switch to. No CC0 groom asset was reachable and an authored groom is a multi-day job. The rig and scalp are ready for one. | A groom asset, or authored hair cards. |
| E4 | **MakeHuman's CC0 packs contain exactly one casual jacket and no boots.** `jacket_field`, `jacket_denim`, `jacket_leather`, three puffers and two coats are all `male_casualsuit05` in eight fabrics, so a "long" coat is hip-length and a puffer has no quilting; a city that wears work boots is shod in trainers and loafers. | The asset library. Both alternatives were built and rejected on the render: cut from the tee it comes out a beige t-shirt, cut from the skin a painted-on bodysuit. | Licensed or authored garments. |
| E5 | **Carried items are bevelled boxes** — a backpack, tote, shoulder bag, courier's box and briefcase are each one box with no straps, handles or soft shape. **Garments have no zips, buttons, plackets or cloth simulation.** | Authoring cost. They read at street distance and are wrong close up. | Authored props and a cloth setup. |
| E6 | **No facial rig beyond blendshapes, no LODs, no cloth or hair physics**, and subsurface skin is authored for Cycles but invisible in glTF, which has no SSS. | Runtime-side work. Jaw motion is morphs only; the eyes are separate objects the runtime rotates. | An engine pass. |
| E7 | **`run` is a time-compressed 3.6 m/s run, not a 5 m/s sprint, and the idle's motion is authored** — only its posture is mocap. | No idle capture exists in the fourteen downloaded CMU files. | More mocap. |
| E8 | **Millimetre slivers of the base top still show through the knit at the shoulder and chest**, and the orchestrator's review of 2026-09-06 additionally found the trouser hem passing through the sneaker heel, fingertips buried in the trousers in the rest pose, and carried props floating unattached to the hand. | Linear blend skinning moves two fitted meshes slightly differently; the clearance that would close the first starts to flatten the knit. The other three were open at the time of writing and are being worked. | Garment-versus-garment resolution extended to the bottoms/feet pair and to the rest pose. |

---

## F. Traffic, pedestrians and performance — `docs/verification/{traffic,performance}/REPORT.md`

| # | Deviation | Reason | What would close it |
|---|---|---|---|
| F1 | **The 8 ms step budget is still not met, but the city-scale step is no longer the problem it was: 665.3 ms → 38.3 ms.** Synthetic is 15.2 ms. | ADR-021 is implemented. Origins and destinations are drawn from the streamed region, removing **627 of the 665 ms** measured on this machine before it — path finding was 96 % of the step and is now 9.6 %, and a vehicle route query fell from 49.8 ms to 0.46 ms. The lane corrected the published baseline downward in the process: the 777 ms in the earlier report came from a different machine, and it reported the same-machine 665.3 ms rather than claim the larger ratio. What is left is per-agent decision work — 19.1 ms traffic and 15.5 ms pedestrians at 4,549 vehicles and 21,239 pedestrians, plus 3.7 ms routing. The synthetic step *rose* from 13.9 to 15.2 ms, and that is a behaviour change rather than overhead: pedestrians now reach their goals instead of wandering, and with pedestrians disabled the vehicle side is unchanged and its trajectory hash bit-identical. | **The signal window is taken, and it was worth less than this entry claimed: 1.73 ms, not 2.7.** `TrafficSim::step()` and `PedSim::step()` now set `SignalTable::setActiveWindow` to their own despawn disc before refreshing the cache, so no host has to remember to — measured on the real table at **433 of 19,814 plans a step**, combined step **37.32 → 35.59 ms** (traffic 22.03 → 20.99, pedestrians 15.29 → 14.60), and the isolated refresh at 42.3× (0.815 → 0.019 ms). Transparency is not argued but measured twice: 0 of 39,628 plan states differ between the two modes, and **both trajectory hashes are bit-identical** across the A/B (`dedc1863de1e83bd` / `e6d5dd6607e663d6`). What is left: multithread across tiles, then structure-of-arrays on the pedestrian side, then a cheaper wall test. Genuine cross-city trips additionally need a router hierarchy, which ADR-021 excludes by name. |
| F6 | **This entry was wrong about where the 262-neighbourhood calibration is applied, and the correction is that the shipped runtime was never the problem — the benchmark was.** It said `DensityTable::assignLaneNtas()` is "called in exactly one place in the whole repository: `core/bench/bench_world.cpp`". It is not: the Unreal adapter calls it at `unreal/NYCSim/Source/NYCSimRuntime/Private/CoreAdapter/GameplayRoadNetwork.cpp:231`, immediately after `density.nycb` loads, and has since commit `d63f02e` — before the ADR-021 work that produced this entry. The grep behind the claim did not reach `unreal/`. **What is true and stands:** no lane in `roads/lanes.parquet` carries an NTA column, `runtime/roadgraph.nycb` ships with none of its 220,329 travel and bus lanes carrying one, so the assignment must happen at load time, and **nothing fails if a host forgets** — the city silently becomes one density cell. | Found while implementing ADR-021, and it is the source of that ADR's absurd 304,878-vehicle target for a 900 m ring: with every lane in one cell the density table cannot distribute anything. The benchmark genuinely was missing the call and now makes it before measuring. | Carry the NTA on the lane in `lanes.parquet` so the runtime cannot start without it. The same silent-if-forgotten shape is why the signal window (F1) was put inside `step()` rather than left as a call a host has to remember. |
| F7 | ~~The traffic invariants are asserted on a synthetic grid and only *printed* on the real city~~ — **closed, and the invariant holds.** Condition 4 claims traffic behaves as specified "across the whole city"; the compliance invariant — a law-abiding driver never enters on red — was asserted only on the synthetic Midtown grid, through a monitor that exists in the test file. On the city graph the benchmark printed `red_light_entries`, which counts the **deliberately modelled runners** (2 % of sedans, 10 % of taxis) as well, so it is legitimately non-zero and **cannot fail**. A city-scale claim was resting on a number with no failing case. | `TrafficSim::Stats` now carries `red_light_entries_law_abiding` beside the raw total, the city benchmark prints it as an invariant, and the synthetic suite asserts the shipped counter agrees with its own monitor — so the number a host can check is the number the tests check. | **Measured on the real graph: 0 law-abiding red-light entries out of 8 total, over 5,000 vehicles and 60 steps.** The invariant holds where it had never been looked at. What is still true is that this is a *benchmark* run rather than a test: the core suite does not load the 5 GB runtime directory, so nothing fails automatically if the city graph regresses. |
| F2 | **The jaywalking share (0.30) and box-blocking probability (0.02–0.35) are modelling targets, not measurements.** | No New York field count of signal non-compliance exists here. 0.30 sits inside the 20–50 % range in the literature and is a single exposed constant. | A pedestrian-count stage. |
| F3 | **Jaywalking is modelled as crossing against the signal at a crosswalk**, not as mid-block crossing with its own geometry. | Modelling choice. | Mid-block crossing geometry. |
| F4 | **22 of 3,000 sampled frames catch one vehicle pair mid-separation** at a junction entry, each clearing within two or three frames. | The impenetrability projection runs at the end of each step. The two tests that cannot assert an exact zero carry the measured value and the reason in the assertion. | A continuous-collision formulation. |
| F5 | **A published traffic figure of 35.57 ms was not reproducible** and is corrected in place to 14.6 ms. | Contention with five other agents on four cores. Every behaviour counter was identical; only the machine differed. | — (recorded so the wrong figure is not quoted again). |

---

## G. Time, weather and audio — `docs/verification/{live,unreal_gameplay}/REPORT.md`

| # | Deviation | Reason | What would close it |
|---|---|---|---|
| G1 | **Wetness, puddles, umbrella probability, snow thresholds and road-clearing rates are modelled, not measured**, and one city-wide clearing rate is applied to every street class. | NYC publishes no per-street wetness or umbrella telemetry. Each is a documented closed-form rule with a named, sourced constant. | Telemetry that does not exist. |
| G2 | **The snow-depth model has never been exercised against a real snow event** (it is September), and the forecast blend is unvalidated beyond a single adverse sample. | Season. Its terms are unit-tested individually and its constants sourced. | A winter run. |
| G3 | **Manhattanhenge disagrees with the published dates** by 1 day in May and 3 in July. | The published dates are internally inconsistent; the azimuth criterion is met exactly and the sensitivity table ships. | — |
| G4 | **No New York field recording is used for ambience.** The one Commons candidate for "city traffic ambience" turned out to be a railway-station tunnel recorded in Tampere; it is licensed and kept with its record but no zone plays it. The traffic bed is synthesised from the vehicles actually around the listener. | No lawful NYC field recording was reachable. | A licensed NYC ambience set. |
| G5 | **MetaSound graphs are not authored**; the five documented sources are C++ DSP with an identical parameter contract. | The MetaSound builder API is experimental in 5.4 and cannot be exercised here. `UNYCVehicleAudioComponent` prefers a MetaSound asset the moment one exists. | A workstation pass; no engine work needed to adopt them. |
| G6 | **Pedestrian↔vehicle coupling lags one step** — a driver reacts to a pedestrian 50 ms late. | The obstacle set handed to the traffic simulation is the previous step's. Core's `traffic/Interop.h` is designed to remove exactly this. | Adopt `Interop.h` with the traffic-simulation merge. |

---

## H. Unreal world assembly — `docs/verification/unreal_world/REPORT.md`

| # | Deviation | Reason | What would close it |
|---|---|---|---|
| H1 | **Landscape sample spacing is 1.98413 m, not 2.00 m.** | UE requires `SubsectionSizeQuads` in {7,15,31,63,127,255} and 500 quads is divisible by none of them. The importer resamples bilinearly to 504 quads and keeps the tile exactly 1 km wide; measured cost 0.002 m, and exactly zero at tile borders. The vertical data is untouched. | Export 505-sample heightmaps directly and set `component_size_quads: 63`. |
| H2 | **The four Niagara weather systems cannot be authored here**, so rain, snow, spray and fog particles are absent until a workstation pass. | UE 5.4 exposes no Python API for Niagara graphs. The subsystem logs one warning per missing system and every other weather effect stays live. | `unreal/README.md` §6 — the single manual step, with the exact user-parameter contract. |
| H3 | **The star texture and the water normal map are not in the repository**, so the star sphere is skipped and water uses a flat normal with flow-driven roughness. | No suitable licensed texture was fetched. Both are bound by material *parameter*, so dropping a texture in needs no graph editing. | Two textures. |
| H4 | **Sidewalk geometry is derived**, offsetting each centreline by `width/2 + 1.9 m`, not planimetric. | The planimetric sidewalk polygons were among the four sources deleted for disk (C7). | `GameplayPedSim::buildSidewalks()` is the one function to change. |
| H6 | ~~Three of the seven runtime files DATA_CONTRACTS §15 specifies are not produced~~ — **closed, and verified from the reader rather than the writer.** All seven now ship: `pois.nycb` **1,082,160 addresses / 27.8 MB**, `tiles.nycb` **2,916 tiles / 81,712 B**, `landmarks.nycb` **65 named points / 2,504 B**. Record sizes match `sizeof(Poi)` = 12 and `sizeof(TileRecord)` = 28 in `NycbRecords.h`, and every tile field offset matches the `NYCSIM_NYCB_FIELD` assertions. Proved through `RoadNetwork::load()` itself — the GPS consumer this entry was written about — against the real directory: **1,082,160 addresses, 13,364 bus stops, 65 landmarks, 1,106,225 index entries, no absence notes**, and `search("338 5 AVENUE")` and `search("Empire State Building")` return the same point. | The 866 buildings dropped are exactly those whose `address` is empty; **no duplicate (address, position) pair exists**, and the 223,882 address strings that appear at more than one position — 1,876 Breezy Point bungalows share `217-02 BREEZY POINT BLVD` — are kept, because collapsing them would delete real buildings. New Jersey is in `tiles.nycb` on the reader's terms: `FNYCTileRecord` reserves `borough_mask` bit 6 for it and the streaming subsystem treats absence from this table as never-stream, so excluding it would make shipped geometry unloadable. | **Four things in the original entry were wrong and are corrected here.** (1) I wrote 1,406 building tiles; it is **1,399** — 920 with `buildings.parquet` and 486 with `buildings_nj.parquet`, 7 tiles holding both, and the 920 sum to the whole 1,083,026-row base table. (2) I named `tiles/index.parquet` as the source for the tile counts; **its four count columns are zero on all 2,916 rows** (D12). (3) The worked example was wrong: there is no `350 FIFTH AVENUE` row — the Empire State Building's PLUTO address is `338 5 AVENUE`, the low number of the 338–350 lot — and the column mixes both spellings, 2,299 rows as `5 AVENUE` against 6 as `FIFTH AVENUE`. The capability the entry described was real; that string was not. (4) Borough code **7** exists on 362 tiles ("New York State outside NYC") and DATA_CONTRACTS §2 stops at 6. |
| H5 | ~~`landmarks.nycb` has no layout in DATA_CONTRACTS §15~~ — **closed, and the layout was not invented: it was read off the consumer.** `RoadNetwork::loadLandmarks()` already fixes every degree of freedom — section `points` with a `landmarks` fallback, `element_size >= 12`, x/y/name_str at offsets 0/4/8, `strtab` required (stricter than `loadPois`), empty labels skipped — and quotes the layout in the note it logs on failure. §15 now states it, and the file ships with 65 named points from `buildings/landmark_footprints.parquet`. | The contract was unspecified because nobody had looked at what the reader demanded; it turned out to demand all of it. | — |

---

## I. Landmarks and verification — `docs/verification/{landmarks,reference,comparison}/REPORT*.md`

| # | Deviation | Reason | What would close it |
|---|---|---|---|
| I1 | **Sculpture is modelled as blocked-out mass, never as figures** — the Bronx County Courthouse's sculpture groups, the Brooklyn Museum's 30 allegorical figures, the AMNH equestrian statue and the Brooklyn Public Library's fifteen bronzes are plinths and blocks. | Deliberate: inventing figure geometry would be worse than the honest gap. | The props/character competence, not the landmarks lane. |
| I2 | **Detail screens are built at reduced density.** The New York Times Building's ceramic rods are one in five (0.60 m centres against the real 0.127 m); the Javits Center's space frame follows the 90 ft structural grid, not the 5 ft module. | 186,000 rods at ~12 triangles each is ~2.2 M triangles for one facade, five times the whole group's budget. Both are stated in the catalog `dimensions`. | A triangle budget that does not exist. |
| I3 | **Two landmark heights are inferred** — Arthur Ashe Stadium (46.0 m, scaled from the published section, ±2 m, because the LiDAR predates the 2016 roof) and Two Times Square (the LiDAR roof including its sign tower). Both flagged in the catalog. | No architectural height is published for either. | A published figure. |
| I4 | **Three footprint polygons had to be divided** because one BIN covers two buildings (Shops at Hudson Yards / 30 Hudson Yards; 15 Hudson Yards / The Shed; 111 West 57th / Steinway Hall). Each cut uses a vertex already in the polygon and each is named as an inference. | The source keys by BIN. | — |
| I5 | **No interiors anywhere**, except volumes visible from the street through glass. | Scope. | — |
| I6 | **One reference subject has a single photograph** (Staten Island ranch houses) and seven have 2–3 instead of 3–4. | Commons has essentially no freely licensed street photography of ordinary Staten Island tract housing; the candidate pool was exhausted after licence, date and content filtering, and each `meta.json` records `"exhausted": true` with its rejection histogram. Padding the subject with photographs of something else was rejected. | A different photo corpus. |
| I7 | **Street-axis subjects carry the subject's azimuth, not the photograph's.** | Nothing in the file metadata gives a camera heading; every such explanation says exactly that. | EXIF that does not exist. |
| I8 | ~~Six comparison scenes render near-black and one not at all~~ — **closed. All 57 scenes now have a usable render, a sheet and a written assessment.** The last of them, the 9/11 Memorial Pools, was black for two independent reasons, and both were camera-placement faults rather than anything wrong with the world. | The first: `ground_z(mode="street")` took a low percentile of the terrain in a radius and found the bottom of a memorial pool — a 9 m void a few metres away — putting the eye 1.4 m under the paving. The second: the WTC site model's own plaza slab (I11) covered the camera. Four landmark-stage renders were red for the same class of reason and are also fixed: a camera 7.9 m underground, one inside a terrace prism, one under a boardwalk aimed 19° off, and two wall views too small to read. | — |
| I9 | **The "is this eye point indoors" test was an upward ray cast**, which cannot tell a ceiling from foliage, an awning, a scaffold shed or a bridge deck — all of which exist in this world and all of which a person stands under. | An upward hit was taken as proof of being inside a shell. | Fixed: the ray now steps past trees and reports the first *built* thing overhead, identifying foliage by material after checking those materials appear on tree geometry and nothing else across 122 props, 127 landmarks, 138 kit pieces and the tile shells. |
| I10 | ~~Two comparison sheets are stale~~ — **closed.** Both were re-rendered twice, once against the corrected WTC model and once after the Oculus axis fix, and both assessments rewritten. The entry's own description had gone stale before it was closed: the Oculus pitch difference had already been fixed by an earlier pass, and what was still wrong was the *building*, which is I11c. | — | **One WTC ships a frame its own camera search rates worse**, and that is a decision rather than an oversight: the search still ends in "open air only", the frame closes off 96 m ahead and a lamp stands 7.7 m from the lens — but the failing search centres the tower at 183 m while the passing one showed a neighbouring tower's base. Recorded in `docs/verification/comparison/REPORT.md` §2.10 and in the sheet. |
| I11 | ~~The WTC site model stands about 2.6 m too high and its plaza has no pool openings~~ — **closed and verified.** `origin_tm` z is now 4.40 m and the plaza measures 4.10–4.40 m NAVD88 (it was exactly 7.000 m); the plaza is 116 triangles with the two 61 m pool openings cut and not one triangle inside either, against the 2-triangle unbroken quad it was; the impostor card is gone from the oak mesh; the model's maximum z is 330.60 m, which is 3 WTC's published 329.2 m plus its 1.4 m mast exactly. | `GRND` served as both the local datum and the frame origin's NAVD88 z, so plaza level was counted twice. It is now two constants. The 4.40 m was chosen on evidence, not assumed: the published terrain reads 4.405 m at the frame origin, the median over the plaza deck is 4.341 m, and the choice puts this entry 0.005 m from the convention the other 92 catalogue entries follow. The plaza is also clipped to the real OSM outline (33,039 m² against a published 8 acres = 32,375 m²) instead of a 520 × 520 m quad. | — |
| I11a | **The two pool centres were derived and wrong, and are now measured.** They were **31.8 m** and **30.1 m** out of position with the squares **41.4° mis-rotated**. | The script recorded that no polygon existed for the individual pools. OpenStreetMap carries both, as ways 697722178 and 697722181, in the extract already in this repository. The corrected 29.25° edge heading agrees with the real 3 WTC (26.5°) and 4 WTC (29.4°) footprints. | — (closed; recorded because a derived value was presented as the best available when a real one was on disk). |
| I11b | ~~Terrain is drawn straight through the memorial pools~~ — **closed, and it changed nothing visible.** The scene builder no longer draws terrain or pavement inside the outer plan outline of a landmark's own ground surface, openings included, on the principle that an opening in a modelled ground plane is a modelled hole. 40 of the 93 catalogue entries supply ground; the 53 that do not are unchanged, which is right. A modelled surface more than 1 m from the heightmap beneath it is a *deck on* the ground rather than the ground and does not cut — Hudson Yards is +5.34 m and the Lincoln Tunnel portals −9.68 m, both refused by the same test in opposite directions, and every refusal is recorded per landmark in `render.json`. | The Vessel forced that second half: it "hovers on a disc above the plaza with nothing under it", so cutting under it would have left a hole rather than fixed one. | **Stated because it is the honest result:** in the memorial-pools frame the fix cuts 8,113 terrain quads and 414 pavement triangles and changes **0 %** of the pool opening's pixels, because from a 0.33 m eye above the coping the sight line never reached the depth the terrain was drawn at. The defect was real and general; from that one camera it was invisible. |
| I11c | ~~The Oculus is 32° off its own footprint~~ — **closed, and my diagnosis of it was wrong.** The Oculus now sits on BIN 1089309's own long axis, re-derived from the polygon at build time: **94.2 % of the body over its own footprint** (IoU 0.90) against 58.0 % (IoU 0.41), and **0 m²** overlapping 3 WTC where there was 231 m². | I recorded `PLAZA_AXIS_DEG = 160.6` as "the memorial plaza's own axis, which is a different thing from the Oculus's". It is not: the plaza polygon has no usable axis at all (its minimum rotated rectangle runs 15.5° and its principal axis 172.0°, 23° apart), and 160.6° was the heading of the line between the two *derived* pool centres that I11a had already replaced with measured ones. After that correction the constant measured nothing whatever. It is now three separately measured constants. | — (the fidelity statement now records the 6 % of the body's plan still outside the footprint). |
| I11d | **`b_wtc_site` had no verification renders on disk at all.** | `b_common.prop_template` kept a template cache across `new_scene()`, and touching `ob.name` on a removed StructRNA raises, so the render pass crashed before writing anything. Fixed; it has six renders now. | — |
| B16 | ~~Inland water renders dry~~ — **closed, per-polygon, and it shows almost nothing.** The scene builder now masks water from the hydrography polygons using each body's own `water_z_m` instead of a per-tile scalar, so the 121 tiles that could produce no water surface at all can produce one. | The per-tile `water_level_m` was a hard-coded 0.0 and the mask a height threshold, which on an inland tile selects nothing; I measured that patching the scalar would flood up to 76 m of real relief on 91 of those 121 tiles and rejected it. | **The honest caveat:** across all 57 rendered scenes exactly **one** camera faces a real inland body — Bethesda Terrace, THE LAKE at 317 m and 29° off axis — and from there the terrace balustrade hides all but **455 pixels** of it. The fault was general and the comparison set contains almost no camera pointed at one, so this is a fix whose evidence is a measurement rather than a picture. |
| D12 | **Eleven shipped columns are declared and never filled, and one of them is a join that was replaced by a line of zeros.** Found by sweeping all 44 processed tables for a column that is zero, empty or false on every row. The list, with what each was for: `buildings_base.osm_id` (**1,083,026 rows** — `buildings/build.py:108` writes `np.zeros(n)` into it outright, so the OpenStreetMap join the column names has never once happened for a New York building, while the New Jersey stage does the same join and reaches 45.66 %); `roof_attrs.roof_ridge_dz_m` (1,083,016 rows — the ridge rise of a pitched roof, which is B6's subject); `facade_attrs.landmark_model` and `facade_attrs.n_placements` (1,083,026 rows each — the second is the kit-piece count per building, zero for every building against 32.8 M placements); ~~`tiles/index.parquet`'s `n_buildings`, `n_road_segments`, `n_props` and `n_trees`~~ — **these four are now filled**: 1,314,408 buildings over 1,399 tiles, 138,412 segment-tile incidences over 995, 1,773,764 props over 1,648 and 699,691 trees over 1,140, backfilled by `terrain.index --counts` from the tile artefacts themselves and checked against them by `test_the_tile_index_content_counts_are_filled_and_agree_with_the_artefacts`; `bus_stops.parent_station`; `rail_stops.has_shelter` with `shelter_dist_m`; and **fidelity bit 7 `LANDMARK_MODEL`, which is zero on every row of all three building tables** — the 121 hand-built landmark models are real and the fidelity *report* counts them correctly, but it gets that from the landmark catalogue, so a consumer reading the bitfield the contract points it at sees none. | Each was declared for a stage that would fill it and no stage did. `index.parquet`'s own `nycsim.terrain_columns` metadata lists the eleven columns the terrain stage owns and the four counts are not among them, so nothing was even nominally responsible. **The New Jersey table's empty columns are deliberately excluded from this list** — `roof_type`, `year_built`, `bldg_class`, `land_use`, `has_storefront`, `has_scaffold`, `landmark_id` and `name` are empty there because B11 says they are unmeasured, which is a stated gap rather than a silent one. | **The `osm_id` one is smaller than it looks and I measured it rather than assuming.** The extract holds 1,247,724 building rows, but `material` is on 1,829 of them (0.15 %), `colour` 2,381, `roof_shape` 4,364 and `roof_material` 2,150 — so joining it would **not** meaningfully move A2's 96.69 % inferred facades or A3's 52.43 % inferred roofs. What it would add is about 12,931 building names and 4,364 roof shapes; its 1,076,377 heights are worse than the LiDAR already in place. Fill the columns or drop them; a column that names a join it does not perform is the worst of the three options. |
| D11 | **The subway exists as 2,120 street entrances, a ventilation-grate sound and nothing else.** `transit/rail_routes.parquet` (47 routes) and `rail_stops.parquet` (1,166 stops) are extracted and read by nothing but their own producer; `runtime/transit.nycb` carries `bus_routes`, `bus_stops`, `route_stops`, `vertices` and `strtab` — **no rail section at all** — so no line, no station and no train reaches the runtime. What a player meets is a stair entrance that leads nowhere, and a grate whose hum swells on the real headway of a line that is not simulated. | Defensible as scope — the brief asks for a drivable city — but it was never *stated* as scope, and the data was gathered as though it were in it. The audio subsystem takes the headway from `rail_routes` and is the only consumer of that fact anywhere. | ~~Either a rail section in `transit.nycb` with the running stock, or an explicit line in the architecture saying the subway is surface features only. The second is honest and costs nothing~~ **— and while that choice was pending, the architecture was saying the opposite.** `docs/ARCHITECTURE.md` claimed *"trains run on GTFS schedules"*, which is not a gap in a document but a false statement in it. Measured directly rather than taken from this entry: `transit.nycb` holds exactly five sections, `bus_routes` (345), `bus_stops` (13,364), `route_stops` (21,963), `vertices` (137,060) and `strtab`, and the only `train` anywhere in the engine source is `ANYCAmbienceEmitter::ScheduleNextTrain`, a sound. **The second option is now taken and written down:** rail is surface features only — 986 km of elevated structure, 2,120 entrances, platforms, and the pass-by sound on the real headway — and no rolling stock moves anywhere. What is not honest is 2,120 doorways to a system that does not exist. **Two smaller orphans found in the same sweep and recorded here rather than given rows of their own:** `osm/water_nj.parquet` (New Jersey water areas — the water stage never reads them) and `osm/signals_stops.parquet` (a second extraction of `highway=traffic_signals`, duplicating `roads/cache/osm_nodes.parquet`, which is what the signal stage actually uses). ~~Both are wasted extraction rather than a fidelity gap~~ **— and that sentence was asserted, never measured. Measured now, it is false for the first of them.** `water_nj.parquet` holds **847 polygons, 130.21 km²**. The water model covers **121.70 km², 93.46 %** of it — every large body, checked one by one: Upper New York Bay, Newark Bay, the Kill Van Kull. The remaining **8.51 km² in 654 polygons is not in the model at all**, and **7.812 km² of it, in 607 polygons, falls inside tiles this build has generated** — so it is dry land a player can drive over. They are named, real, inland New Jersey water: Cedar Grove Reservoir (0.374 km²), Packanack Lake (0.334), Pio Costa Lake (0.323), Robinson's Branch Reservoir (0.279), Orange Reservoir (0.253), Great Notch Reservoir (0.127), Lincoln Park Lake (0.115), and one unnamed 1.607 km² body in `t_-21_23`. **Why they are missing is not that the file is unread.** The water stage reads the same PBF directly, but its OSM contribution is `osm.sea.difference(nyc_land_polygon())` — the polygon derived from the *coastline*. An inland lake 25 km west of Manhattan is not part of the sea, so it is dropped, and the parquet that does hold it is never opened. The fix is small — also emit OSM water areas outside the NYC land polygon that are not part of the sea — and **the rebuild is not**: `finalize_levels` needs the 2 m DEM mosaic to give each body a water level, that intermediate was removed (`terrain/src2m_removed.json`, 1.76 GB), and `data/raw/usgs` is gone too, so regenerating it is a **4.67 GB re-download across 34 USGS products** before the water stage can run at all. Recorded at its measured size rather than closed, and the measurement is held by a test so the number cannot drift unnoticed. `signals_stops.parquet` remains what this entry said: duplicated extraction, no gap. |
| D10 | **The parks had almost no trees; they now have the trees a mapper happened to walk past, which is not the same as a canopy.** The only source was the 2015 Street Tree Census — a *street* inventory — so 1,756 of 1,916 green polygons ≥2 ha held not one tree, Central Park had **70 over 341.6 ha** against a published ~18,000, and Pelham Bay Park had **zero**. Found by opening the Bethesda Terrace render and measuring: no tree within 300 m of that camera, in Central Park. **That frame is also the one the fix does not reach, and the sheet says so**: 197 OSM trees now stand within 400 m of it where there were none, **zero** fall inside its own 66° frame at any radius, and the re-render differs from the pre-tree frame by **0.003 % of pixels**. OSM's Central Park coverage is dense where a mapper walked and this sight line crosses none of it. **49,175 OpenStreetMap trees are now placed** (650,516 → 699,691): Central Park 70 → **1,566**, Flushing Meadows 134 → **3,367**, Pelham Bay 0 → **560**, Prospect Park 0 → **507**, Bronx Park 83 → 452. **A claim I made here was wrong and is corrected: I wrote that the 9/11 memorial's oak grove "appears for the first time". It does not — `blender/landmarks/b_wtc_site.py` has placed 220 swamp white oaks on that plaza since the landmarks stage, and its own docstring says so. What the OSM ingest did there was duplicate them.** 449 OSM trees now stand within 130 m of the WTC site origin and **not one census tree** is among them, so the 5.0 m cross-source dedupe — which compares OSM against the *census* — had nothing to compare against and let all 449 through onto a plaza that already carried 220. The memorial grove is roughly 670 trees where the real one is about 400. Landmark models that plant their own vegetation were never part of the dedupe's design, by either the lane that built it or by me reviewing it. A keyword sweep of the 93 catalogue entries suggests other models may be affected and is too loose to name them — `b_wtc_site` is the one confirmed from the model's own record. **The fix and its sequencing, stated so it is not lost:** the dedupe needs a third cross-source rule, OSM against the landmark models that plant their own vegetation, keyed on a per-model flag read from the build scripts rather than on a keyword guess. That is a furniture rebuild (78 s), and it changes the props tiles, so it must wait until the 57-scene comparison pass now running has finished — changing the world mid-pass would leave the earlier and later sheets describing different cities. Afterwards only the scenes with an OSM tree inside a planted landmark need re-rendering, which is a handful rather than the set. | The extract was on disk and no stage had read it: 71,447 `natural=tree` nodes, 69,338 inside the scope box. The dedupe radius against the census is **5.0 m, measured rather than picked** — for every OSM node, the distance to its nearest census tree against a control of the same points displaced 20 m; the excess over chance peaks at exactly 5.0 m and the 5.0–5.5 m shell is the first to capture *fewer* pairs than chance. It is a cross-source rule between one named pair of datasets, never a group radius: a blanket 5 m on the tree group would have deleted ~51,000 real census trees. | **Three things this does not fix, all measured.** (1) **1,632 parks covering 181.3 km² still hold zero** — down from 1,756 and 219.3 km², so 124 parks moved and the rest did not. Van Cortlandt gains 5 trees for 460 ha of forest; Freshkills, the Palisades, Floyd Bennett Field and Saw Mill Creek gain nothing. Central Park at 1,566 is **8.7 %** of its real canopy. (2) **Their heights are inferred twice over and are not measurements of these trees.** 98.4 % carry no height tag and none carries a girth. The first rule put them at the census's unknown-DBH fallback, **3.88 m — below the 10th percentile of the 650,307 census trees that do have one** — which made them **100 % "small"** against the census's 23.4 %, a categorical inversion produced entirely by the fallback and by nothing observed about those trees. That was wrong rather than merely coarse: a missing DBH in a *census* usually means a newly planted tree, and a missing `height` in OpenStreetMap means only that no mapper typed one. Each unknown height is now a **deterministic draw from the census population**, seeded from the tree's own NYC_TM position through SplitMix64 so the value follows the tree and not its row — verified reproducible from scratch, `sha256 = 5df79f612578fc44`, and unchanged by row order or by how many rows the dedupe removed first. The draw is conditioned on the taxon where OSM names one (species pool → genus pool → whole population, pools under 30 trees falling through), which reaches 915 rows and stops an ornamental cherry being handed a 30 m oak's height. Result: **46.9 % large / 28.9 % medium / 24.1 % small against the census's 48.5 / 28.1 / 23.4**, and p10 5.02 / median 11.30 / p90 22.39 against 5.02 / 11.43 / 22.39. Central Park's 1,566 trees are now 755 large, 431 medium, 380 small where all 1,566 were small. **What that is and is not:** the census records no height either — every value in it is allometry over a measured trunk diameter (D2) — so the pool is one inference deep and a draw from it is two, and `height_source = 4` is the only thing standing between the honest reading and a false one. What makes it worth drawing from is that its input is 651,951 diameters measured in the field, so its *shape* is the shape of the New York tree population. A single median was rejected: it is wrong in shape as well as per tree, and repeats one error 48,000 times instead of spreading it. (3) **~4,784 genuinely distinct trees are lost as collateral** to the dedupe — that is what the control says would fall within 5 m by chance — which is the stated price of the rule that a double tree is worse than a missing one. `natural=tree_row` (436 lines) is extracted and deliberately **not** placed: a row has no published count or spacing, so spacing trunks along it would invent positions. Parks still have no ground surface class either — lawn, forest floor, beach and marsh are all one terrain colour. The woodland *coverage* the extract holds — the polygons under which 94.5 % of that missing canopy stands — is now filled by a declared rule, `rule:woodland_canopy`, whose every stem is inferred and says so: J86. |
| D9 | **The source footprint tables' `shape_area` column is not in square metres** — it reads **1.744×** the polygon's own area in NYC_TM for every row sampled (median 1.7445 over 300 rows). Anything in the build quoting it as m² is wrong by that factor. | It is a Web-Mercator pseudo-unit, and the factor is exactly the projection's area distortion at this latitude: sec²(40.7°) = 1.7398, and 1.744 implies 40.78°, which is Manhattan. `pipeline/nycsim_pipeline/buildings/citygml_validate.py` already documented this; `blender/landmarks/b_wtc_site.py` had quoted three footprint areas from the column as though they were metric. | Fixed at the one site that used it: all five areas in that docstring now quote the measured polygon area. The column is still loaded elsewhere (`blender/landmarks/c_common.py`) and any future use must divide by sec²(latitude) or, better, measure the polygon. |
| I13 | **Every comparison sheet shows an empty city.** 51 of the 57 written assessments record that there are no people in the frame and 37 that there are no vehicles. The verification scene places neither: `blender/verify/scene.py` loads terrain, buildings, shells, kit, props and landmarks, and no agent of any kind. | This is a limitation of the *verification* path, not of the world: the traffic and pedestrian simulations exist, are tested, and hold their invariants (zero red-light entries, zero wall crossings, saturation flow 1,708.6 veh/h/lane) — they simply do not feed the still renderer. It matters because it is systematic: a reader going through 57 sheets would reasonably conclude the city is unpopulated, and every sheet in this report understates the world in the same direction. | Place a simulation frame's agents into the verification scene. The snapshot format already exists for the Unreal runtime. |
| B15 | ~~No commercial signage geometry exists anywhere~~ — **closed, and calibrated against the photograph rather than by eye.** 584,706 lit shopfront fascia bands, 56,214 blade signs, 7,430 blank bulletins on 34,102 billboard-class buildings, 1,133 LED modules, and 9,048,881 of 30,435,823 windows switched to a lit twin at **zero extra records**, because the `FLAG_LIT` bit already existed and only the kit id changed. A controlled A/B on the Times Square night frame — same shells, same camera, same 32 samples, only the placement stream differing: area above 0.90 luma **0.34 % → 7.02 %** against the photograph's **6.17 %**; and in daylight, area above 0.70 luma 10.69 % → 16.06 % against 15.73 %, mean luminance 0.406 → 0.434 against 0.404. 11,337 m² of emissive sign face inside the camera's kit radius, from zero. | **A statement in the kit report was false and is corrected: there *are* real sources of NYC signage location in this repository.** The OSM extract carries 292 `advertising=billboard` nodes, and MapPLUTO's `zonedist` carries **`C6-7T`** — the Special Midtown District sub-zone where illuminated signage is *mandatory* — on a few dozen lots, every one of them inside a box a few hundred metres across on Broadway and Seventh Avenue between 42nd and 50th. Verified independently: that district's centroid sits **42 m from Duffy Square**. The zoning category that requires the signs *is* Times Square, and no boundary was drawn by hand. | — |
| B15a | **The 30,381 real business names still do not appear on any sign.** They get a lit *blank* fascia panel. | An instanced kit piece carries one texture, so a single fascia mesh cannot render 30,381 different names; baking them would mean 30,381 unique pieces. The name stays in `awning_text` and DATA_CONTRACTS §6.1.1 now records where a runtime binds it to the `SIGN_FACE` slot. | A runtime text binding, which is engine-side work. Stated as a gap rather than solved, and the honesty line held: bulletin faces are blank vinyl and LED faces carry the pixel matrix of the display hardware and no image, so **no advertising copy was invented anywhere**. |
| I13a | **The crowd cannot reach a plaza that is not beside a street.** The walkable network is derived from the road segments, so Bethesda Terrace gets **10 pedestrians against roughly eighty in its reference photograph** — and the terrace was not filled to hide it. Two smaller gaps in the same population: **cyclists, e-bike riders, moped riders and pedicab drivers are not drawn at all** (33 dropped in the Fifth Avenue frame alone), because the fleet table's 1.80 m height for `cyclist` is rider-plus-bicycle and the exported body is a 1.10 m bicycle — the rider is not exported, and a riderless bicycle rolling down Fifth Avenue would be worse than none; and **7 of the 18 vehicle classes are exported at a size the fleet table does not publish**, each recorded in `VEHICLE_ASSET_DEVIATIONS` with its measured deviation, with a test that fails both on a new deviation past 5 % and on a recorded row changing quietly. | `GameplayPedSim::buildSidewalks()` offsets road centrelines (H4); a park terrace, a pedestrian plaza and a boardwalk are none. | The planimetric sidewalk and plaza polygons, which are the same fix H4 asks for. The riders need a character body attached to the bicycle asset. |
| I15 | ~~The agent clearance around the camera is set for "not inside the lens", not for "not filling the frame"~~ — **closed, after a frame turned up that was nothing but an NPC.** `blender/verify/agents.py` uses `CAMERA_CLEAR_PED_M = 1.5` and `CAMERA_CLEAR_VEHICLE_M = 6.0`, and they do exactly what they say — no agent stands inside those radii. On `drive_bronx_grand_concourse` the result is that the right third of a portrait frame is the backs of four people: measured against that camera (42.2° horizontal, azimuth 25.0°), one pedestrian sits at **1.50 m and 27.9° off axis — one centimetre above the cut-off** — and the nearest one actually in frame is at **2.57 m, 21.0° off axis**, with more at 3.39 m and 4.41 m. At a 1.6 m eye and a 42° lens a body at 2.57 m fills about a third of the frame height. | The rule was written to stop an agent being rendered inside the camera, which is a rendering question, and it answers that correctly. A comparison sheet needs a different question answered — whether a person dominates the frame — and nothing asks it. It was invisible until this pass because an empty scene cannot have anyone standing in it. | **`drive_midtown_sixth_ave_45th` settled it: the nearest pedestrian stood at 1 m and the frame was one NPC's torso — no street, no buildings, no Sixth Avenue.** A person at 1 m subtends **261 %** of frame height at this set's 1.6 m eye and 38° vertical field. `CAMERA_CLEAR_PED_M` is now **3.5 m**, derived from the frame rather than picked: the frame is `0.69 × d` metres tall, a 1.8 m body fills all of it at 2.6 m, and 3.5 m puts it at 74 % — close, which a street photograph often is, without being the whole picture. `CAMERA_CLEAR_PED_BASIS` records the body height, field of view and frame share it came from, so a different lens re-derives it instead of re-guessing, and a test asserts the constant still matches its own basis and can only ever tighten. It stays a radius rather than a forward wedge because the placement runs before the final view azimuth is known. **And the constant was only half of it.** The re-render reported *no* pedestrian dropped over the observer on `drive_bronx_grand_concourse` while four still stood between 1.44 m and 2.57 m of the lens — because `add_agents` measures from the point the scene was built around, and the camera had moved 5.39 m since. Measured across the set, **26 of the 57 scenes have the camera away from the scene centre**: 5.4 m here, 11 m on Arthur Avenue, **166 m on One World Trade Center**, where `probe_origin` switched from the photograph's GPS to the nominal viewpoint. The clearance was guarding a point that in half the set is not where the camera ends up. `agents.cull_near_camera` now runs after the camera is final, removes what the placement could not know about, and folds the count into the same `dropped` keys so a reader sees one number per cause; a test asserts it uses the same radii as the placement and runs *after* `clear_of_geometry`. The cost is a fourth re-run of the pass. |
| I18 | **12 of the 20 landmark sheets that can be matched to their own catalogue entry cannot contain their subject: the building rises out of the top of its own comparison frame.** Computed from each camera's real field of view and the model's own bounds: the Chrysler Building at 72 m needs **77.1°** of elevation and the frame reaches **36.9°**; 40 Wall Street at 200 m needs 55.0° and reaches 25.3°; the Empire State Building at 317 m needs 54.5° and reaches 36.9°. Also short: the Municipal Building (68.7 vs 36.9), One Vanderbilt (74.3 vs 45.0), the Equitable Building, the NYSE, One Wall Street, the Woolworth Building, Rockefeller Center, MoMA and MetLife. **Every one of them is already at the 18 mm lens floor.** | Three individually sensible rules compose into this. The optical axis is kept level on purpose, so the render stays comparable with the photograph on proportion rather than introducing converging verticals the photograph may not have. The lens widens for a tall close subject but stops at 18 mm to avoid distortion. And the frame's aspect ratio is taken from the reference photograph, so a letterbox reference — 40 Wall Street's is 1280×606 — cuts the vertical field to 50.7° even at a 90° horizontal. None of the three is wrong; together they make a tall subject at close range impossible to frame. | What a photographer does is tilt up and accept the convergence, or step back. Stepping back is often not available in Manhattan, so the rule to change is the level axis: allow tilt when the subject would otherwise leave the frame, record the tilt in `render.json`, and say on the sheet that the verticals converge because the subject could not be contained otherwise. That is honest and it puts the landmark in its own picture. Not done in this pass, and the reason it can wait is worth stating: **none of the twelve is one of the seven viewpoints the brief names.** The mandated set is Bethesda Terrace, Washington Street in DUMBO, Fifth Avenue at 42nd (north and south), the Brooklyn Heights Promenade, the Staten Island Ferry deck, Times Square from Duffy Square (day and night) and Top of the Rock; every one of those either has no tall named subject or looks at it from far enough away to contain it. Condition 3 is unaffected. These twelve are supplementary landmark sheets, and a fifth re-run of the whole pass to re-aim them is not worth what it costs against a defect that is measured, recorded and confined to sheets the brief does not require. |
| I17 | **The camera clearance test measures a ray, and a frame is not a ray.** `camera.view_distance` casts one line along the view azimuth at eye height and reports how far it runs before a building shell stops it; `min_view_m` accepts the viewpoint on that number. `drive_queens_bayside` passes it completely — *"the viewpoint is in open air on the ground and the camera was not moved; the nearest solid thing in the view cone is `t_15_7_vinyl_siding` 23.6 m away, and the view azimuth is clear for 150 m"* — and the frame is the blank side wall of a house filling the upper third over an untextured ground plane, with **none of the 73 vehicles, 263 pedestrians or 182 trees in the scene visible**. | The ray is looking down a gap between two houses. It is a correct measurement of a line and it is silent about the rest of the image, which is the part a reader sees. The same shape as every other fault found today: a real number that cannot fail in the way the thing it stands for can. | A coverage measure rather than a distance: sample the frame — a coarse grid of rays through the actual view frustum — and require some share of it to reach beyond a minimum distance, so a camera facing a wall through a slot is rejected however clear the slot is. Cheap, since the scene is already built when the check runs. **Scale, measured rather than guessed, and it is not 12 broken sheets:** across the 55 scenes with a clearance record, **12 have the ray clear for the full 150 m probe while something solid stands within 30 m of the lens**, and only 2 within 10 m. That signature is *not* by itself a fault — on `landmark_nypl` the thing 18.9 m away is the library, on `times_square_duffy_south_day` at 23.6 m it is the TKTS booth, and on `dumbo_washington_st_manhattan_bridge` the 2.6 m object is `prop_lamp_cobra_davit_1`, a lamp standard, which `view_distance` steps past by design because it counts only building shells and landmark models. It becomes a fault when the near object is not the subject, which the clearance record has no way to say. Bayside is that case; deciding which of the other 11 are needs the frames, not the record. **And the rule's other assumption stopped holding today.** `view_distance` counts only building shells and landmark models on purpose — its docstring reasons that "a street tree or a lamp standard on the axis is not a closed view" and that a photographer looks past a tree. That is right for *a* tree. `landmark_city_hall` now stands behind **208 OpenStreetMap trees within 150 m**, placed by today's park ingest where the census had 15, and the frame is a thicket with City Hall's colonnade glimpsed through it at 126.6 m. The density is not wrong — 30 trees a hectare is if anything conservative for City Hall Park, and no landmark model plants there, so nothing is duplicated. What changed is that a rule written when parks were empty now lets a camera pass its clearance check with a grove between it and its subject. The frame-sampling fix above covers this too, provided foliage counts. **Scale, and the first way I measured it was wrong.** Counting every tree in the wedge between camera and subject gave 2,906 for the Staten Island Ferry view — a meaningless number, because those trees are 10 m tall and the subject is a 541 m tower 2.5 km away, so they screen nothing. A tree only screens if it is near enough to subtend a large angle. Counted properly — within 60 m of the camera and inside a 20° wedge on the view axis — **13 of the 44 scenes with a named subject have any tree in the way, and 9 of those involve a tree the park ingest placed today**: the New York Public Library (29), the North Pool (17), One World Trade Center (15), the Woolworth Building (13), the Manhattan Bridge and City Hall (12 each). Four scenes were screened by census street trees before today and are unchanged. |
| I16 | **The renders are physically lit and the photographs are metered, so every shaded frame compares under-exposed.** The render places the Sun from the photograph's own EXIF instant and then applies a fixed 0-stop exposure on a Filmic transform; the photographer's camera metered the scene in front of it and opened up. On `drive_lower_manhattan_stone_st` — a narrow alley under a 58.6° sun — the render's mean luminance is **0.249 against the photograph's 0.404**, and the area above 0.95 is **0.02 % against 14.66 %**. Worse on the mandated `fifth_ave_42nd_north`: mean **0.227 against 0.455**, median **0.176 against 0.537**, and **52.3 % of the frame below 0.20 against 22.2 %** — half the brightness and a median pixel three times darker. **A second cause compounds it on 5 of the 57 scenes**, that one included: their Sun is an assumption rather than a measurement (`time_source` reads "photograph year only; 21 June 09:30 assumed"), so the shadow pattern is a plausible morning and not the photograph's own instant. The other 52 take it from EXIF. | Nothing is wrong with the lighting model, and this was nearly written up as an emissive-calibration bug before it was measured: the first reading was that the lit shopfront fascias were blowing out, and in fact nothing in the frame clips at all — they only look bright because everything around them is dark. The two images answer different questions, and the sheets say so rather than treating the gap as a fidelity failure. | An auto-exposure that meters the rendered frame the way a camera would, applied per scene and recorded in `render.json` beside the 0-stop default so a reader can tell a metered frame from a physical one. The night frame already does a hand-tuned version of this (`NIGHT_EXPOSURE_STOPS = −1.25`, calibrated against its own photograph's histogram), which is the argument that the general case is worth doing properly rather than per frame. **Characterised properly over the 14 scenes re-rendered in the current pass, this is a dispersion problem and not a bias, which is a different fix:** the renders' mean luminance spans **0.089–0.719 with a standard deviation of 0.164**, the photographs' **0.351–0.617 with 0.074** — **2.21× the spread**. The mean gap is only −0.059 but its own standard deviation is 0.196, so scene by scene it is large in both directions: `drive_midtown_sixth_ave_45th` is 0.364 darker than its photograph and `bethesda_terrace_fountain` is **0.331 brighter**. That is exactly what separates a metered image from a physically lit one — a photographer's camera pulls every scene toward the middle and the renderer does not — so "the renders are too dark" would have been the wrong diagnosis and the wrong fix. **Superseded by J83:** measured over 164 daylight sheets rather than fourteen, the gap is a bias and not a dispersion -- render below photograph on 91 % of frames, median ratio 0.535 -- and the development is now metered on the frame. |
| I14 | **27 of the 57 comparison cameras stand on the carriageway, 24 of them at 1.6 m — standing in live traffic.** Audited by the DoITT pavement polygon under each eye point and shipped as `docs/verification/comparison/camera_surface_audit.json`: roadbed 24, sidewalk 19, crosswalk 3, plaza 3, and 8 on no paved polygon at all (bridge deck, ferry deck, promenade, park path). No camera is inside a building. The three that stand over the roadway rather than in it — Duffy Square at 6.2 m on the TKTS steps, the High Line at 10.7 m, Top of the Rock at 260.7 m — are where they belong. | The camera search optimises for a clear sight line to the subject, and the middle of the street is where one is. It has never had a rule about what a photographer can stand on, and the reference photographs it is compared against were overwhelmingly taken from the pavement. Until this pass it was also invisible: with no vehicles in the scene, a camera in the roadbed looked exactly like a camera on the kerb. | Constrain the search to sidewalk, crosswalk, plaza and unpaved surfaces, and let a roadbed placement through only where the sheet says why — the three elevated ones would still pass. Not done in this pass because it moves 24 cameras and therefore invalidates 24 sheets and their written assessments for a second time in one day. |
| I12 | **Seven of the 57 sheets compared two halves that did not show the same subject; six are fixed and the assumption behind them is gone, but three faults of the same family remain.** Both mandated Fifth Avenue viewpoints now compare views of the same street in the same direction, which is what unblocked two of the nine. | One assumption ran through all seven: the chooser's only evidence that a photograph was *of* something was its association with a *place*. Four rules replace it — subject terms matched against title and description and **never** categories, a `not_of` list on the title, a term preceded by a house number not counting ("325 Fifth Avenue" is a building, not the avenue), and an LCCN in the file name marking an archival scan. That last one caught a **c.1900 photochrom** serving as the Fifth Avenue reference: the only date its metadata carries is the date it was digitised, so it read as 2018. Exactly 1 of the 519 photographs was affected. Revalidated over all 172 subjects with a dry run before every change: **six items moved and nothing else**. ADR-020 turned out not to forbid any of it — it governs keeping the photographs tracked, not how they are chosen. | **Three faults remain, recorded rather than churned.** (1) The chooser still establishes no view *direction*: a photograph looking the wrong way along the right street still passes. (2) `drive_bronx_grand_concourse` is the weakest sheet in the set — Commons has almost nothing free looking along that boulevard near East 165th Street, so its reference is of the right street 3.9 km away. (3) ~~The Washington Square Arch item's recorded `subject` coordinate sits 15.0 m from the Arch model's own origin, which at 39 m aims the render 15.8° off the thing it is a picture of~~ — **this measured the wrong thing and overstates the fault.** The distance that matters is to the *model*, not to its origin: the Arch model's footprint is 22 × 21 m and the recorded subject sits **4.4 m outside it, which at 37 m is 6.7°**, so the arch is framed. An audit of all 49 items whose subject is a landmark the build models (`tools/audit_subject_aim.py`) finds **46 of them exactly on their model's footprint** and three off it, all extended structures: Hell Gate Bridge 250 m (30.6° at 423 m, the one genuine outlier), the Central Park perimeter wall 25.1 m, the Roosevelt Island Tramway 17.6 m (1.1° at 901 m, negligible). Separately, **0 of 138 subjects fall outside the frame the item aims** — the azimuths are derived from the subject and they point at it; aiming at the nearest landmark model instead was measured across the 43 subjects with one in range and **rejected**, because it swings 10 of them by more than 10° — including 174.8° for the Vessel and 174.3° for the Paramount Building, where the nearest model is a multi-building model whose origin is not the subject. A per-subject aim point is a pass of its own, and so is auditing the 73 of 138 point-subject items whose keywords leave part of their subject name unrequired. |

---

## J. The drivable build — the pass that took the project from authored to runnable

Section A1 says the Unreal project is authored and never compiled. Getting it to the point where it
could be is its own body of work, and it found eighteen defects. Every one of them shared a shape:
the code was written, the data existed, and nothing connected the two — so nothing failed, nothing
warned, and the missing piece was indistinguishable from a working one until somebody opened the
artefact and looked. They are listed here rather than folded into the sections they touch, because
what they have in common is more useful than where they happened.

| # | Deviation | Reason | What would close it |
|---|---|---|---|
| J1 | ~~**The city had no road surface.**~~ **Closed.** `data/processed/roads/pavement/` — 972 tiles of real DoITT roadbed, sidewalk, median, plaza, curb, crosswalk and parking-lot polygons — was consumed by exactly one thing, the still-frame renderer. Nothing carried it into the engine, so the car would have driven on bare landscape: no asphalt, no kerb, no crossings, and no surface for `UNYCVehicleMovementComponent`'s friction model, which is built around exactly these materials. | Nobody had looked for the producer. The renderer's `add_pavement` was doing the work and looked like the pipeline. | Done: `blender/roads/build_pavement.py` writes `tile_pavement.glb` per tile, one mesh per (kind, surface), with the `SurfaceClass` each material maps to. |
| J2 | ~~**The player's car was not a vehicle.**~~ **Closed.** `fusion_hybrid.glb` shipped with `skins: 0` and `animations: 0` — 77 unskinned nodes and not one bone — while its catalogue entry read `contract_profile: "full"`, `missing_nodes: []`. Chaos Vehicles needs a `USkeletalMesh` with `Wheel_FL` as a **bone**; without a skin the importer produces a `UStaticMesh` and the pawn is an invisible physics body. | Both statements were true of what they measured. `rig.finalise` checks that a *node* of that name exists, and one did. | Done: `blender/vehicles/vlib/skel.py`, 39 bones, and `tests/test_vehicle_rig.py`, which reads the shipped binary rather than the code that wrote it. |
| J3 | ~~**The centre of mass was measured from an origin the exporter never used.**~~ **Closed.** `NYCVehicleMovementComponent` placed it at `(frontMassShare − 0.5) × wheelbase` under a comment asserting "the mesh origin is at the wheelbase midpoint by the Blender export contract". The contract says the opposite, the test enforces the opposite, and the shipped asset has `Wheel_RL/RR` at x = 0. The centre of mass was **1.425 m** too far back — level with the rear axle on a car that carries 58 % of its mass on the front. | Two documents disagreed and neither was read against the other. | Done, plus `tests/test_vehicle_contract_agreement.py`, which compares them. |
| J4 | ~~**Fifteen of the engine's twenty-four lamp slots were dead**, and two of its five instrument slots.~~ **Closed.** `UNYCVehicleLightsComponent` finds every lamp by material-slot name. The exporter wrote `LIGHT_TURN_*` where the engine drives `LIGHT_IND_*`, joined both high beams into one slot and both DRLs into another where the engine drives pairs, wrote `LIGHT_BRAKE_CHMSL` for `LIGHT_BRAKE_C`, and had no geometry at all for the fog lamps, the side repeaters, the courtesy light or the instrument backlight. Every indicator on the car was dead. | A third hardcoded copy of the contract lived in `tests/test_vehicles.py`, so two of the three documents agreed with each other and with nothing else. | Done. The lists now live in one bpy-free module both sides import, and the agreement test compares that module against the engine headers name by name. |
| J5 | ~~**Twelve of the engine's twenty-two optional vehicle bones are still geometry that does not exist**: the four instrument needles, the interior mirror's own bone, both column stalks, the rear wiper and the fuel flap.~~ **Closed, and the count was wrong in both directions.** Four of the twelve -- `Door_Hood`, `Door_Trunk`, `GearSelector` and `Mirror_Interior` -- were never missing at all: `skel.BONE_OF_OBJECT` has renamed them since the rig was built, and every exported car had those exact bones. The test was reading the exporter's *object* tuples, which is not what the rig writes. | Optional means the vehicle imports; it does not mean the dashboard works. `UNYCVehicleAnimInstance` asked for `Needle_Speed` every frame, got `INDEX_NONE`, and the speedometer read zero at every speed. That the check itself measured something other than the thing it stood for is the same failure this file records a dozen times, made inside the guard against it. | Done: the four needles and both stalks are built, so all 45 bones of the player car resolve. Each needle's axis is measured off its gauge face and pointed away from the driver, and its blade is authored at the rest peg -- -120, -110, -45 and -45 degrees, checked out of the exported binary to within one degree by `test_every_gauge_needle_stands_at_its_rest_peg`. The stalk geometry had existed from the first build, merged into `Interior_Column`. `Door_FuelFlap` and `Wiper_Rear` remain, with reasons, as J42. |
| J6 | **The sign of every vehicle bone rotation is unverified.** The rig is built so that every bone that can be has an identity rest orientation — which removes the *axes* from doubt through the Blender → glTF → Unreal conversion — but Blender's +Y is left and Unreal's is right, so the importer's handedness flip decides whether "+Y is forward roll" and "positive is open" arrive with the sign `NYCVehicleContract.h` names. | It cannot be tested without the engine, and guessing would be worse than saying so. | One look at a running editor and, if it is wrong, a sign flip in `UNYCVehicleAnimInstance`. **Narrowed for the four gauge needles.** Each needle's bone axis is now measured from the gauge face it sits on and pointed forward, away from the driver, and the blade is authored at the rest peg -- half the sweep back from twelve o'clock -- so that a *positive* engine angle sweeps it clockwise as the driver sees it. `tests/test_vehicle_rig.py::test_every_gauge_needle_stands_at_its_rest_peg` reads the exported binary and confirms all four sit at -120, -110, -45 and -45 degrees to within one degree. What that settles is that the geometry and the rig agree with each other; whether the importer's flip then makes the engine's positive angle come out clockwise is still the one look above. |
| J7 | ~~**Nothing was ever put on the screen.**~~ **Closed.** `UNYCGpsWidget` and `SNYCMinimap` have been finished C++ and Slate since Stage 12b. `AddToViewport` and `CreateWidget` appeared **nowhere** in `unreal/`, there was no `AHUD` subclass, and the game mode never set `HUDClass` — although its own header said the HUD class was resolved from the world settings. No speedometer, no minimap, no GPS, no clock. | A widget nobody adds to a viewport is indistinguishable from a widget that does not exist, and no compiler says so. | Done: `ANYCSimHUD`, `UNYCDrivingHudWidget`, and a test that every `UUserWidget` in the module is constructed somewhere. |
| J8 | ~~**93 landmark models imported into a world that spawned none of them.**~~ **Closed.** 927 MB — the Empire State Building, the Chrysler Building, Grand Central, the World Trade Center site, the Statue of Liberty — present in the content browser and absent from the city. `manifest.py` had looked for `data/processed/landmarks/landmarks.json` since Stage 12b; nothing wrote it, so `build_levels.py` had nothing to place from and never grew a `place_landmarks` at all. | The reference pointed at nothing, and pointing at nothing is quiet. | Done. The placement is a translation and **no rotation**: every catalogue entry carries `heading_deg` beside `origin_tm` and using both is the obvious reading and is wrong — the models are built in a frame aligned to their own footprint and rotated back to NYC_TM before export, which their own extras state. The index names the field `heading_deg_not_a_rotation`. |
| J9 | ~~**All seven runtime containers were dropped from the import manifest.**~~ **Closed.** The manifest's NYCB reader unpacked the 24-byte header as 20 bytes with no padding, read `index_offset` out of the padding, seeked to nonsense and reported "not a valid NYCB container" for every file: the road graph the traffic drives on, the signals, the POIs the GPS searches, the tile index, the transit, the density. 130 MB of world, warned about and not shipped. | The test fixture hand-assembled the same wrong 20 bytes, so the reader and its test agreed with each other and with nothing else. The writer, which asserts 24, was never consulted. | Done. The fixture now builds its container through the real writer. |
| J10 | ~~**Four content paths, the HUD font, 74 audio files and 838 MB of skyline went where nothing looks.**~~ **Closed.** The manifest wrote `Vehicles/fusion_hybrid/SK_fusion_hybrid` and `Character/npc/...` where the engine hardcodes `Vehicles/Player/SK_FusionHybrid` and `Characters/Crowd` — Characters, plural; `safe_asset_name` turned `overpass-bold` into `F_overpassmbold` where the settings ask for `F_Overpass`; the 74 licensed `.ogg` files and the 90 merged skyline cells had no import rule at all and were skipped with a warning. | Each is a producer and a consumer naming the same thing differently, with nothing comparing them. | Done, and `tests/test_unreal_pipeline_paths.py` compares them — through the manifest's own resolver rather than a copy of it. |
| J11 | ~~**The radio and the engine derived a track's content path independently and disagreed.**~~ **Closed.** `safe_asset_name` puts an underscore before a leading digit because a UE asset name may not start with one, so `01_gunther_freischutz.ogg` imports as `_01_…` while `UNYCRadioSubsystem` looked for `01_…` and found silence. | Two derivations of one string in two languages. | Done: the manifest writes the path it assigned into the station index and the subsystem prefers it, so nothing derives it twice. |
| J12 | ~~**`M_NYC_Vehicle` and `M_NYC_Character` were named as masters and built by nobody.**~~ **Closed.** Every `SetScalarParameterValue` the vehicle components make — `EmissiveScale`, `GaugeValue`, `PaintColor`, `WindowDown`, `DentAmount` — was called on a material with no such parameter. In Unreal that is silent. | `IMPORT_SETTINGS` named them; `ensure_materials` built five others. | Done, with a test that every parameter in `NYCVehicleParams` is declared on the master. |
| J13 | ~~**`physics_asset: True` was read by nothing.**~~ **Closed.** `AWheeledVehiclePawn` cannot simulate without a physics asset, and Unreal's auto-generated one simulates every bone — including the four wheels, which Chaos drives itself. On a 39-bone rig that is 38 bodies too many. | The setting was in the manifest and in no code path. | Done: `apply_skeletal_settings` makes everything but `Body` kinematic, and names the manual editor step rather than reporting a success it did not achieve if the editor refuses. |
| J14 | ~~**A packaged build would have shipped without the cars, the people, the sound and the landmarks.**~~ **Closed.** `bCookMapsOnly=True` with no `DirectoriesToAlwaysCook` for Vehicles, Characters, Audio, Physics or Landmarks — every one of which is reached at runtime by a path string rather than by a map reference. | Maps-only cooking is a reasonable default and silently drops exactly the things this project loads by name. | Done. |
| J15 | ~~**One core source would not compile as the engine compiles it.**~~ **Closed.** `core/src/util/Log.cpp` handled a null format as `fmt ? fmt : ""`, and an empty literal in a format position is what `-Wformat-zero-length` objects to; clang makes it an error under `-Werror`, which is how the Unreal module is built. | The CMake build does not enable that warning, so it would first have appeared on the user's machine, mid-build, in a file nobody was thinking about. | Done, and the module's 38 wrapper translation units are now committed rather than generated on the target machine. |
| J16 | ~~**The pavement was 3.4 GB of slivers on the wrong surface.**~~ **Closed.** Ear clipping a planimetric road polygon gives triangles that no refinement repairs: on one 11,446 m² roadbed, 133,004 triangles of which **97.4 %** had a shape quality below 0.1 and the smallest edge was 0.0000 m. And the carriageway was drawn on the ground beneath it — **48.2 %** of one Lower Manhattan tile's paved area sits on polygons spanning more than two metres of terrain, against 3.0 % in Midtown, with a maximum span of 12.6 m: the FDR Drive and the tunnel portals on the streets below them. | The triangulation was chosen for correctness and speed and never measured for shape; the elevation was never questioned because a plan view does not look like it is missing one. | Done: cut on a grid (5,050 triangles at a median quality of 0.866 for the same polygon), and the carriageway takes its height from the road network's own 3D centrelines where `z_source` says the segment is not at grade. 221 MB → 17.6 MB on the worst tile, piercing 3.05 % → 0.15 %. |
| J17 | **Bare terrain still rises through the finished pavement over 0.0–0.15 % of it**, by at most a few centimetres on the tiles measured. | The DEM and a triangulated slab are two different surfaces, and the engine collides its landscape as two triangles per 1.98 m quad rather than as the bilinear patch the drape was computed against. The residual is measured per tile and reported in `pavement_manifest.json` rather than assumed away. | Cut holes in the landscape under the paved area — UE landscapes support a visibility mask — which is a feature of its own. |
| J18 | **Twelve fleet vehicles carry no centre high-mounted stop lamp**, and say so individually. | Not an omission: FMVSS 108 has required one on passenger cars since 1986 and on vehicles at or below 4,536 kg GVWR since 1994, and above that line these vehicles do not have one. A Sprinter is tall and heavy-looking and has one; a Grumman LLV is small, under the threshold, and older than the rule. | Nothing. The list and its reasons are in `fleetlib.NO_CHMSL`. |

| J19 | ~~**Not one facade kit instance in the city could be placed.**~~ **Closed.** `kit_placements.bin` stores an integer `kit_id`; `build_levels.py` looked for a `kit_catalog.json` that nothing wrote, and would have keyed it by the *string* ids of `blender_out/kit/catalog/*.json` anyway, and then rebuilt the path as `Kit/{piece category}/SM_{stem}` while the import files a piece under its glb's folder, `Kit/facade/`. Three independent mismatches, each sufficient on its own. **5,713,269 instances in the 47-tile first-drive region alone** — every window, cornice, storefront, fire escape, water tower and rooftop unit — resolved to nothing, and the import reported no error because there was no error to report. | `data/processed/facade/kit_ids.json` has held the correct integer→piece→glb mapping since the facade stage was written, and states its own id formula in a `id_formula` field. Nothing read it. The manifest's own catalogue loader hashed the string ids into the uint32 space, so its "missing from kit catalog" warning fired for all 119 placed ids and was read as noise. | Done: the manifest loads `kit_ids.json`, joins it against its own kit entries and writes `kit_catalog.json` with the **content path each piece actually imported to**; `build_levels.py` reads that path verbatim and reconstructs nothing. 174/174 pieces resolve, 0 warnings. Two tests fail if a placed id has no piece or a piece's path is not imported. |
| J20 | ~~**Not one prop in the city could be placed either.**~~ **Closed.** `props.json` carries `kind` as the int16 code of the props catalogue, and `place_props` read it as a mesh name: `/Game/NYCSim/Props/14/SM_14`. On top of that, `str(row.get("kind") or "")` is the empty string for kind **0**, the street tree, so all 44,427 trees of the region were dropped before the lookup even ran. 129,828 props in the region, none placed. | The mapping from a kind code to an asset needs three lookups and two vocabularies that disagree (`utility_pole` exports as `sign_post`, a tree is chosen by species and height). `blender/verify/scene.py` had worked it out for the renders and was the only place it existed. | Done: the logic is one bpy-free module, `nycsim_pipeline/furniture/assets.py`, read by the renderer, the manifest and the editor. The manifest resolves every row and writes a deduplicated `assets` list with a per-row index into `props.json`; `build_levels.py` groups by that path. `scene.py` now calls the same resolver instead of its own copy. |
| J21 | ~~**13,851 surveyed DOT pedestrian ramps in the first-drive region were absent geometry.**~~ **Closed.** The kerb was a uniform 0.15 m lift along every sidewalk edge and the pavement carried no ramp kind at all, so every surveyed ramp in the city was missing — the most visible thing at a driver's eye height that this build did not have. | The alias table said "modelled by the road stage, not a prop asset". It was not: `pavement/*.parquet` carries roadbed, sidewalk, median, plaza, curb, crosswalk, parking lot and driveway, and none of them is a ramp. The comment was written when the kind was excluded and never checked against the pavement it named. | Done, and from the survey's own measurements rather than a shape: each ramp's run is the kerb reveal divided by its published `running_slope_pct` (1.80 m at the ADA maximum, 1.29 m at the 11.6 % one Boerum Street corner has) and its width is the published `width_m`, both clamped with the clamped count reported. The descent direction is not in the file — every row's `heading` is NaN — so it comes from the pavement: the nearest point on a roadbed boundary is the kerb, and the rectangle is anchored **on that edge** rather than centred on the survey point, which sits a median 0.76 m back on the sidewalk. Anchored, **99 % of ramps meet the kerb**, 93 % of each lies on the sidewalk and 6 % reaches into the gutter. The kerb polygon is cut where a ramp crosses it, because a ramp that ends in a 0.15 m step is not a ramp. **The ramps are cut for 545 of the city's 972 paved tiles**, which is every tile of the delivered first-drive region and 498 more; the rebuild was stopped there because the container ran out of disk, and the remaining 427 tiles carry the pre-ramp pavement until it is re-run — `python3 blender/roads/build_pavement.py --all --workers 2` finishes it and is idempotent. On t_-4_4: **398 of 398 built**, the kerb loses 158 m² of 9,867, and the drape residual and piercing are unchanged at 38.4 mm p99 and 0.0. |
| J22 | ~~**81,684 surveyed rooftop cooling towers stood at street level and were placed nowhere.**~~ **Closed.** The DOITT planimetric cooling towers are digitised on roofs; the furniture stage sampled every prop's `z` from the ground model, which is right for the other 33 kinds and put all 81,684 of these *inside* the buildings they sit on. They were also unplaceable: the prop catalogue has no cooling tower, so the kind was declared assetless. | One ground model for 34 kinds, and no check that a kind's z means what the kind is. The modelled asset existed the whole time — `hvac_cooling_tower` is a facade kit piece — and was placed nowhere, because the procedural rooftop set draws from bulkheads, AC units, exhaust fans, vent pipes and satellite dishes and never picks the cooling tower. | Done: `furniture/rooftop.py` joins each row's surveyed BIN against `buildings_base.roof_z`. **77,829 of 81,684 (95.3 %) now stand on their building's measured roof**, median rise 8.5 m city-wide and 42.4 m in Midtown; the 3,855 whose BIN is not in the buildings table keep their surveyed ground z rather than a guess. The kind resolves to the kit piece, so they are placed. `z_source = 5` is a new declared code. |
| J23 | **1,531 prop rows in the region resolve to no asset**, in eleven kinds — the 13,851 curb ramps that dominated this count are now built as pavement (J21), not as props. Miscellaneous planimetric structures (358), artworks (324), drinking fountains (322), memorials (151), vending machines (147), park buildings (177 across three kinds), payphones (29), billboards (15) and swimming pools (8). | Each is a dataset with no modelled asset. The billboards are deliberate: the kit has `billboard_rooftop` and `billboard_wall_mounted` and both are wrong for a roadside bulletin on posts, so those 15 rows stay empty rather than become the wrong object. The park buildings, swimming pools and misc structures are *footprints*, not points, and want extruded shells rather than a prop. | Model the point kinds (a drinking fountain, a payphone, a vending machine are small and repetitive); give the footprint kinds shells in the structures stage. The count is reported by the manifest as `prop_kinds_without_an_asset` so it cannot quietly grow. **City-wide, now that the whole city is built, the same count is 29,417 rows** — this entry's 1,531 is the first-drive region only, and both are right. Read out of the `unresolved` block of all 1,648 tile `props.json` files: swimming pools **17,989**, miscellaneous planimetric structures 4,335, drinking fountains 1,645, parks buildings 1,487, vending machines 1,003, artworks 956, memorials 860, parks comfort stations 715, billboards 292, payphones 102, parks recreation centres 33. The 216,339 curb ramps are counted apart as `built_elsewhere` and are in the pavement, not missing. **Three of these are mapping, not modelling, and that changes what they cost.** Measured in the OSM extract: **713 of the 1,036 `amenity=vending_machine` nodes are `vending=parking_tickets`** — DOT Muni-Meters, and `muni_meter` is already built to the Parkeon Strada's real dimensions; **83 of the 127 `amenity=telephone` nodes are `operator=LinkNYC`** — and `linknyc_kiosk` is already built. Another 101 vending machines are `public_transport_tickets`, 60 newspaper boxes and 57 dog-waste dispensers. So a third of the "unmodelled" point props already have their asset and want a `vending`/`operator` lookup rather than a mesh, the same shape as J56. **What must not be modelled generically: the 956 artworks and 860 memorials.** Each is a unique commissioned object; one mesh standing in for all of them would be the invention this project's brief forbids, and they stay recorded as absent. |

| J24 | ~~**986 km of surveyed rail structure and 166 ha of waterfront existed in the data and nowhere in the world.**~~ **Closed for the 505 km that stands in the air.** The elevated railways of Queens, Brooklyn and the Bronx — the thing a driver on Roosevelt Avenue, Broadway in Bushwick, Jerome Avenue or White Plains Road is under for kilometres — were not modelled at all, and neither were the piers the Hudson and East River waterfronts are made of. | `transit/rail_structures.parquet` and `water/structures.parquet` had no consumer that produced geometry. | Done: `blender/structures/build_structures.py` writes `tile_structures.glb` per tile — **490.6 km of deck on 75,511 columns, and 52,391 piles under the pier decks**, 2.98 M triangles over 813 tiles, 167 MB. Placed by `place_structures` in the level builder, with collision, on the same `NYCTerrain` profile as the pavement. |
| J25 | **The embankments (314.7 km) and the open cuts (166.5 km) are not built, on purpose.** | Not an omission but a measurement: sampling the landscape the engine builds, perpendicular to 200 structures of each class, an embankment centreline stands **3.69 m above** its flanks (92 % of them over 1 m) and an open cut **3.32 m below** (81 % over 1 m), while an elevated one is level with them (−0.10 m). The earthworks are already in the DEM; building them again would put a berm on a berm and a trench in a trench. | Nothing for the earthwork itself. The open cuts' retaining walls are missing — the DEM renders them as a smooth slope — and are not modelled because nothing surveys where the wall face stands. |
| J26 | ~~**A deck elevation per segment is not a deck.**~~ **Closed.** At the joints where two elevated structures meet end to end, **31.5 % disagreed by more than a metre** about how high the deck is there, 20 % by more than two and 7 % by more than five. Built as written, the elevated network would have been a staircase. | Each segment takes its deck elevation from the bridge elevation points near it or, failing that, from its class's median height, and neighbours drawing on different evidence disagree. Nothing joined them, because nothing had ever drawn them. | Done: every shared endpoint gets one elevation — the weighted mean of what the structures meeting there say, a surveyed deck counting four times a class-median one — and each deck ramps between its own two ends. Discontinuity is now zero by construction, and 400 passes of grade limiting bring the **maximum deck grade to exactly 4 %**, the steepest the subway climbs. The median structure does not move at all; the 95th percentile moves 1.68 m. |
| J27 | ~~**1,441 elevated structures had a deck height, a ground elevation and no deck elevation.**~~ **Closed.** 36 % of the elevated and viaduct network, and every one of the 1,308 rows that come from OSM rather than the planimetric survey. | `deck_heights()` fills the class-median fallback into `deck_height_m` and stopped there; `deck_z` stayed NaN. A consumer reading `deck_z` lost every one of them silently, because NaN is a number. | Done: the fallback is carried through to `ground_z + height`. A test fails if any built structure has no absolute deck elevation. |
| J28 | ~~**Whole lines in the New Jersey highlands were built 45–50 m under the hillside they cross.**~~ **Closed.** Measured against the landscape along all 3,986 built structures, **834 (21 %)** had a `ground_z` more than 5 m from the ground under them; two thirds of those west of x = −15 km. | The transit stage runs before the per-tile terrain rasters exist — that is why it samples a ground model of survey points at all — and that model has no reference points in New Jersey, so it extrapolates. A class-median deck *height* riding an extrapolated *ground* is wrong by the whole extrapolation. | Done: `transit/deck_geometry.py` runs after the terrain exists and rebases every class-median deck on the landscape (954 rows); a surveyed deck elevation is kept even where it disagrees — over the East River the DEM is not a river bed and the bridge is right — unless keeping it would bury the soffit, in which case it is lifted just clear and flagged (852 rows). Deck samples sitting below the terrain: **6.60 % → 1.06 %**. |
| J29 | **1.06 % of deck samples still sit below the terrain**, worst case −7.7 m on one tile. | A deck ramps linearly between its two joints and the ground between them does not. Where a structure crosses a rise mid-span the linear deck dips into it. | Break the deck at the terrain's own high points rather than only at the joints. Measured and reported per tile in `structures_manifest.json`; a test fails above 2 %. |
| J30 | ~~**34 of the 93 landmarks published no plan bounds**, every bridge among them.~~ **Closed.** Any consumer needing a landmark's extent — streaming, culling, or deciding what geometry belongs to it — had nothing for a third of the city's landmarks. | The bounds are written by the model's own build script into its catalogue, and 34 scripts do not write them. | Done: `landmarks_index` reads them out of the glb instead, from the `POSITION` accessors' own `min`/`max`, which glTF requires every file to carry. Checked against the 59 catalogues that do publish bounds, this reproduces all 59 to within 0.01 m. All 93 now have bounds. |
| J31 | **The Manhattan, Williamsburg and Hell Gate bridges carry rail that this stage does not build.** | All three are modelled as landmarks, with their tracks: the Manhattan Bridge catalogue declares 4 subway tracks, the Williamsburg 2, the Hell Gate 4 (3 in service). OSM tags those crossings `bridge=yes`, so the rail structure table has them too, correctly, as elevated railway 39–44 m over the East River. Both statements are right and only one may be built. | Nothing. The landmark is the one with the towers and the cables, so the structures stage cedes to it — the same rule as I11b for ground — and reports what it ceded per tile. |

| J32 | ~~**1,285 surveyed subway and railway stations were in the planimetric survey and in nothing else.**~~ **Closed.** `plan_railroad_structure.geojson` carries five classes and the furniture stage consumed two of them as point props; the other three are structures. 972 elevated stations (50.7 ha of roof outline) and 313 at grade (15.2 ha), and only 74 and 76 of them respectively fall inside a building footprint the buildings stage would have modelled. Every station on the Astoria, Flushing, Jamaica, Jerome Avenue, White Plains Road, Brighton and West End elevateds was missing — from on top of the 490 km of elevated structure this build now has. | The two classes the furniture stage wanted were points; nobody looked at what else was in the file. | Done: `transit/stations.py` writes `stations.parquet` and the structures stage builds it — **1,053 elevated station placements and 277 at grade, on 11,229 canopy columns**. An elevated station takes its elevation from the railway under it (930 of 972 from a deck this build measured, 42 from the elevated class median where no built structure is within 60 m); the 150 already inside a building footprint are left to the buildings stage. |
| J33 | **A station's height above its own platform is nominal, not surveyed.** The platform is 1.143 m above the rail — the R-series car floor height, which the platform is level with — and the canopy 4.0 m clear above that; a stand-alone station house is 4.5 m, one storey. | The planimetric survey publishes the roof **outline** and no elevation for these features. | Model the canopies from street-level imagery. The three numbers are named constants in `stlib` with their basis, and `height_source` in `stations.parquet` says which rows are nominal (all 313 at-grade ones). |

| J34 | ~~**1,033,416 buildings — 95.42 % of the city — carried a `roof_mesh_ref` pointing into a file that does not exist.**~~ **Closed.** DATA_CONTRACTS §4 and §5 specified `tiles/{tile}/roofs.glb` and a Blender stage that would write it from the CityGML LOD2 solids. The stage was never written and the file exists for none of the 1,399 tiles that hold buildings. | Opening the solids says why it should not be written: **every CityGML roof triangle is horizontal**. The NYC 3-D Building Model is flat multi-level massing, not roof pitch, so a `roofs.glb` would carry level outlines and nothing else — and `blender/buildings/roofsteps.py` already recovers those same outlines from those same triangles and builds them into the tile shells (Stage 16). The roof *shape* was never missing; the file was, and a million rows pointed at it. | Done: the contract is amended to withdraw `roofs.glb` and say where the roof geometry is, and the reference now names the shard that really holds the building's triangles — `buildings/citygml/da{N}.parquet#bin_{bin}`, which is the file `roofsteps.py` opens. All 1,033,416 resolve to a file that exists; a test fails if any does not, or if the withdrawn reference comes back. `nycsim_pipeline.buildings.roof_ref` repairs a table written before the change. |

| J35 | ~~**Twelve of the twenty landmark sheets that can be matched to a catalogue entry could not contain their own subject.**~~ **Closed in the camera; the sheets re-render behind it.** The Chrysler Building at 72 m needs 77.1° of elevation and a level 18 mm frame reaches 45°, so that sheet showed an anonymous Midtown street. | Three individually sensible rules composing: a level optical axis for proportional comparability, an 18 mm lens floor against distortion, and an aspect ratio taken from the reference photograph. Nothing was wrong with the world in those frames — every element was correctly placed and out of shot. | Done in `render_sheets.containment_pitch`: the lens is widened first, because a level axis is what makes the two frames comparable; only where the widest lens this build will use still cannot contain the subject does the camera tilt, by **the minimum needed** and never past 70°, and the caption then says the verticals converge and that the frame is no longer comparable on proportion. **37 sheets change**, from +1.4° on the Municipal Building to +35.2° on the Rockefeller rink. The **seven mandated viewpoints never tilt**: they are the frames the whole comparison is judged on, and they are judged on proportion, so a tilt would cost exactly the property they exist to have — Duffy Square would have taken +5.5° to contain One Times Square and does not take it. A landmark sheet that shows its subject with a caveat beats a frame that does not show it; a mandated frame that stays comparable beats a taller subject. |

| J36 | ~~**Every vehicle in the city had untextured tyres, seats, carpet and trim.**~~ **Closed.** `materials.py` asks the shared texture library for five material families by name — `tyre_rubber`/`rubber`/`rubber_black`, `leather`/`leather_black`, `fabric`/`fabric_grey`/`cloth`, `carpet`/`carpet_black`/`fabric_dark`, `brushed_metal`/`metal_brushed`/`brushed_steel`/`steel_brushed` — and the catalogue holds none of the fifteen names. Every request fell through to a flat Principled colour, on all 31 vehicles: the tyres of every car on every street, and the seats, floor and trim of the one the player sits in. | The library was built for the city's surfaces — brick, limestone, asphalt, glass — and the vehicle stage was written against names nobody added. It fails softly by design (`using Principled fallback`), so the only trace was a warning line per vehicle in a build log. | Done: five CC0 ambientCG materials added to `texture_catalog.json` with their measured physical sizes — `Rubber004` at 0.25 m (the scale of a real tread block on a 205/55R16), `Leather026` at 0.5 m, `Fabric020` at 0.3 m, `Carpet013` at 0.3 m, `Metal007` at 0.35 m — and the fifteen names aliased onto them. Licences recorded per asset as the library always does. The fleet rebuilds with zero fallbacks. |

| J37 | ~~**117,930 named places were extracted from OpenStreetMap and searchable from nowhere.**~~ **Closed.** `osm/pois.parquet` — shops, restaurants, schools, hospitals, stations, parks and civic buildings, with their real names, brands and opening hours — had **no consumer anywhere in the repository**. The GPS destination index was fed by the PLUTO/PAD address list alone, so it could find "350 5 Ave" and could not find Katz's Delicatessen. | `pois.nycb` is named after the table and is not built from it: it is built from `buildings_base.address`. The name collision hid the gap, and the orphan-table test recorded it as a known deviation (H6) rather than closing it. | Done: a `places` section in `pois.nycb` carries the **85,023** rows that have a name — the other 32,903 are unnamed amenities, correctly dropped — in the same record and the same string table as the addresses, so `RoadNetwork::loadPois` reads both and a container written before it still loads. `SearchEntry::Kind::Place`, counted in the load statistics, displayed as "Place" by the GPS widget, which needed no change because its label switch already had that default. Checked in three places: the §15 contract table, a reader-side C++ test over the shipped file, and the pipeline test that compares the container against the source table. |

| J38 | ~~**The terrain was the only thing under a park, and three friction classes were unreachable.**~~ **Closed.** Two planimetric layers — 27,493 polygons, **130 km²** of park boundary, greenstreet, court, ball field, pool, running track, skating rink, cemetery and vacant ground — were downloaded at the start of this build and read by nothing since. Central Park, Prospect Park, every schoolyard court and every ball field was drawn as the same material as a vacant lot. And `SurfaceClass` declares `Grass`, `Water` and `Ice` while **no surface in the world produced any of them**: three rows of the tyre friction model that could not be reached. | The layers were listed in `sources.py`, downloaded, and never given a stage. The friction classes were declared with the enum and nobody checked which of them anything writes. | Done: `parks/surfaces.py` classifies them from the Capture Rules' own feature codes (4980 park boundary, 4910 court, 4950 pool, 4970 rink, 2500 cemetery, …), cuts each facility out of the park ground it stands in, and `blender/parks/build_parkground.py` drapes them per tile. A pool is `Water`, a rink is `Ice`, a lawn is `Grass`. In the first-drive region: 43 tiles, 2.15 M triangles, 45 MB, 2,204 surfaces including 16 pools, 3 rinks and 3 running tracks. Extended for J86 to the 133 tiles the canopy stage lists (`blender_out/tiles/*/tile_parkground.glb`): 10.17 M triangles, 224,531,932 bytes (224.5 MB), 8,391 surfaces including 44 pools, 4 rinks and 9 running tracks, summed from the 133 `parkground_manifest.json` files. Since J86 the builder also subtracts the hydrography's open water: a park property polygon is a jurisdiction boundary and Central Park's holds the Lake, the Reservoir and the Pond whole, which had been drawn as lawn over the terrain's water plane. |
| J39 | ~~**A draping residual that reported 50 % piercing on a perfectly draped surface.**~~ **Closed.** My own metric, written for this stage and read carelessly: it measured the *drape error* — how far the triangulated surface sits from the landscape it was interpolated against — and called its sign "pierced". An unbiased interpolation is below its own reference half the time by definition, so the first run reported 47.8 % of Central Park's grass under the ground. | Two different questions with one number. The drape error is about the triangulation; whether bare terrain shows through is about the **lift covering that error**, which is a different quantity. | Done: the two are reported separately, and the rule they imply is now a test — **a kind's lift must be at least the tolerance it is draped to**. Raising park grass from 0.03 m to 0.20 m against a 0.15 m tolerance takes the real piercing to **0.54 %**, and the clearance is reported as its own figure: median 120 mm, 1st percentile 66 mm. |
| J40 | **The open-space surfaces are flat colours, not textures.** Grass, court acrylic, infield dirt, polyurethane track, pool water and ice are painted as measured base colours with a roughness, because the texture library has no entry for any of them. | Adding five vehicle materials to that library took an afternoon (J36); these six are the same job and were not done in the same pass. | Add them the same way — CC0 sources with their measured physical sizes and recorded licences. A wrong texture reads worse than an honest colour, which is why they are colours rather than the nearest brick. |
| J41 | **The instrument cluster is a parametric layout, not a replica of any one car's.** Every enclosed vehicle gets the same binnacle: two round dials 140 mm across, a strip display between them, and small fuel and coolant gauges under it. A 2019 Fusion Hybrid's real cluster is part digital and has a hybrid power/charge gauge where this one has a tachometer. | One interior serves 93 vehicles, and its geometry is driven by the cabin dimensions rather than by a per-model dashboard drawing; the alternative is 93 dashboards. The gauge *faces* are texture slots the engine writes to, so what a dial reads is the engine's business either way. | A per-model cluster for the player car, from a dashboard photograph, would close it for the one car the player looks at. Not done. |
| J42 | **Two of the engine's optional bones still have no geometry: `Door_FuelFlap` and `Wiper_Rear`.** The other ten that were missing are built -- four gauge needles, two column stalks, and four that were only ever a naming mismatch. | They are not oversights. The filler flap needs the filler's side and height, which is a per-model fact none of these bodies carries as a parameter, and putting it on the driver's side for all of them would be right for about half. The rear wiper belongs only to the body styles that have one; a saloon, a coupe and a pickup have none, and the fleet is mostly those. | Add a `fuel_side` field to the blueprint and build the flap as a panel of the quarter it replaces, on the declared side only; build the rear wiper for hatchbacks, wagons, SUVs and vans. Both are declared per vehicle in the catalogue's `rig.contract_bones_without_geometry`, so no vehicle claims to have one. |
| J43 | **545 of the city's 972 paved tiles have a road surface; 427 do not, and this container cannot build them.** Every built tile has its curb ramps -- the count is 545/545, not 545 of 972 with ramps and 427 without -- so what is missing is the pavement mesh itself, for tiles running tx 1..21 and ty -17..23: 165 in eastern Queens, 145 in the north Bronx, 67 in south Brooklyn and 50 elsewhere. The delivered first-drive region and all of Staten Island are complete. | Disk, and the arithmetic is not close. The 545 built tiles occupy 7.46 GB, a mean of 13.7 MB and a median of 15.5 MB each; 427 more is 5.8 to 6.6 GB against **1.1 GB free** in a container whose writable allowance is already spent (37 GB used, `blender_out` 13 GB of it, `data/processed` 5.8 GB). There is no slack to reclaim: nothing in `blender_out` is stale, and the 1.87 GB of git objects are 0.7 MB of commits and trees plus blobs that a re-clone would have to fetch straight back. CPU is not the constraint -- at a 44 s mean the 427 tiles are 5.2 hours, 2.6 on two workers. | Build them where there is room for them. The stage is idempotent and takes a tile list, so a machine with 8 GB free finishes it in an afternoon: `python3 blender/roads/build_pavement.py --all --workers 2`. They could also be built here one at a time and streamed straight into a content package the way `update-02` was, which needs no local disk at all -- but that is another 4 GB pushed permanently into the user's repository on top of the region package they agreed to, so it is their call and not mine. |
| J44 | ~~**Every rigged vehicle in every verification render carried an untextured 2 m sphere at its rear axle.**~~ **Closed.** Blender's glTF importer builds one mesh object called `Icosphere` for any rigged file and hands it to each imported bone as a `custom_shape` -- a viewport display helper that is not in the file. `scene.import_glb` asked `bpy.data.objects` what had *appeared* rather than what the file *declared*, so the helper came back with the car and `AssetLibrary.get` instanced its mesh. Measured: a LOD2 glb declaring one mesh imported as two objects, and the 98-agent test scene carried 19 of the spheres, each reaching 1.0 m below the car it belonged to. | Nothing failed. The sphere has no material, so it renders as default grey rather than as an error, and at the distances the fleet is usually seen it reads as a shadow. It was found by measuring the floor of every placed agent against the pavement it stands on -- one car sat 1.15 m off its surface against a 0.5 m tolerance -- and only then by asking what the extra object was. | Done: `import_glb` drops the objects an imported armature names as a bone display shape, identified by what they are for rather than by name, so it stays right if Blender renames the sphere. `tests/test_agents.py::test_importing_a_rigged_vehicle_yields_only_the_meshes_the_file_declares` counts the mesh nodes the glb declares against the objects the import returns, so a glb with one mesh has to import as one mesh. |
| J45 | **The Bethesda Terrace sheet cannot show its subject.** Measured off the model and the render record, not judged by eye: the camera stands on the upper terrace at 24.22 m with the deck's balustrade topping out at 24.05 m, and because that balustrade runs away from the lens to 53.7 m its far end crosses the sightline at **-0.2 deg**. The Angel of the Waters spans -0.7 to +1.2 deg and the basin, the pedestal and all four cherubs span -5.9 to -1.8 deg, so everything below the rail line is hidden and about 1.4 deg of the angel -- some 26 px of an 854 px frame -- stands clear. The aim is not the fault: the bearing to the angel is 13.7 deg and the camera is on 14.2 deg. | The eye point is walked along the view azimuth to the first place the deck stops carrying it, which is 12 m out at the near terrace's own parapet. The photograph is taken 50 m further on, at the parapet that overlooks the plaza. Two other things compound it and neither is the cause: the eye height is taken from the 2 m heightmap (22.622 m) rather than from the deck the camera is standing on (22.90 m), a 0.28 m error that matters when the rail is 0.17 m below the lens; and Central Park has no canopy in this build, so the middle distance is bare terrain either way. | Not fixed, and deliberately not fixed during a render pass over all 172 sheets -- the walk is shared by every viewpoint and changing it would move 56 cameras that are currently right. The model is not at fault: `b_bethesda_terrace.glb` carries the lower plaza, the basin, the stem, four cherubs and the angel, 10,808 triangles of it. The fix is to walk to the *last* supporting point along the azimuth rather than the first gap, and to measure eye height from the surface `_standing_on` actually returns. |
| J46 | ~~**Two runtime sections the C++ casts to were described nowhere.**~~ **Closed.** `density.nycb` ships `cells` (32 B) and `nta_polys` (12 B); `core/src/traffic/Density.cpp` reads both. The roadgraph and transit exporters each write a `*.layout.json` beside their container recording every field's offset, which is what the C++ struct is checked against. The density exporter never did, so those two records existed only inside the C++ and there was nothing to compare them with. The deterministic fixture also had no `places` section, so the only thing exercising that reader path was the real container -- which the test is allowed to shrug at as "an older container". | The same shape as the rest: a producer and a consumer that agree by habit rather than by a document either of them checks. It surfaced from the other end -- `test_nycb_layout_json_documents_the_cpp_structs` went red on a missing `places` key -- and only then from asking which *other* sections had no layout. | Done, instance and class. `write_density_nycb` emits `density.layout.json`; a new test walks every shipped `.nycb`, subtracts the string table and fails on any section no layout describes, so the next one cannot appear silently. The fixture carries two named places, which makes `kFixtureCounts` assert the section exists, is 8-aligned, holds two records and that each is 12 bytes. ctest is 11 of 11 before and after. |
| J47 | **The camera clearance rule constrains 5 % of the frame, and two of the first four drive sheets rendered a wall.** The rule is two thresholds: the view axis must be clear for `min_view_m` = **20 m**, and nothing opaque may sit closer than `min_clear_m` = **8 m** inside a cone of **half-angle 6 deg** about that axis. A drive frame is 42.2 deg wide by 54.4 deg high, so the cone covers 28 % of its width, 22 % of its height and **5.2 % of its solid angle** (5.8 % on a landscape frame). The other 95 % is unconstrained. `drive_bronx_grand_concourse` passed with `view_m: 60.0` and `nearest_obstruction_m: 20.0` and renders a building face about 5 m away across the right half of the picture, its air-conditioners legible, a pedestrian's back at 3.9 m. `drive_brooklyn_bed_stuy_stuyvesant_ave` passed with `view_m: 60.0` and `nearest_obstruction_m: 10.7` -- above the 8 m floor, so accepted -- and renders a brownstone stoop at a few metres filling the entire frame, with no street in it at all. | The same shape as the tilt (I18) and the Bethesda parapet (J45): a correct measurement of something adjacent to the thing it stands for. The probe answers "can this camera see up the street", which it does in both cases; the caption offers it as "the nearest solid thing in the view cone", which a reader takes for the nearest solid thing in the picture. Both cameras had also been *moved* -- 4.9 m and 17.4 m onto the nearest roadbed, because the recorded eye point was inside a roof membrane -- so the position that failed is one the stage chose rather than one it inherited. | Widen the probe to the frame's own half-angles instead of a fixed 6 deg; report the nearest hit with the off-axis angle it was found at, so the number says where it is; and let the pavement-snap search rank candidates on the frame rather than on the axis. **Done, and the pass restarted over it.** `camera.frame_fan` returns the camera's own half-angles from `hfov_deg` and its resolution, and the clearance probe now sweeps 7 yaws across the full frame width by 5 pitches across its full height, **down as well as up** -- the old fan sampled 0 to +24 deg and never looked below the horizon. Both candidate searches rank their fallback on `(near_m, view_m)` rather than on `view_m` alone, which is what let a camera hard against a block face win by seeing 60 m up the street past the corner of the building filling the rest of its picture. The record now carries `nearest_obstruction_at_deg`, so the number says *where* it was found. Stopping the pass at 13 of 172 to fix this cost about 50 minutes against 13 hours of evidence taken through a rule that constrains a twentieth of the frame. |
| J48 | ~~**Two roadway viewpoints stood about 100 m from the street they name, and one of them looked 68 deg across the street it did stand on.**~~ **Closed.** `drive_brooklyn_bed_stuy_stuyvesant_ave` names "Stuyvesant Avenue at Decatur Street, roadway centre, looking north". Its recorded point was **102 m** from that crossing and 22 m from Decatur Street, whose bearing is 81.4 deg, aimed at 13 deg -- so the render was the brownstone row on the far side of Decatur and contained no street at all. `drive_brooklyn_park_slope_7th_ave` stood **98 m** from Seventh Avenue at Garfield Place; its azimuth was nearly right (30.0 against the avenue's 32.4) and its position was not. | **This is not the clearance fault it looked like.** J47 was found first and fixed first, and re-rendering Bed-Stuy through the corrected probe moved the camera not at all: the wall is 10.2 m away at 18.1 deg off-axis, which clears the 8 m floor honestly. The probe was measuring a twentieth of the frame *and* the frame was pointed at a wall, and only the second of those put the wall there. An audit of all 172 viewpoints against the road graph is what separated them: of the 26 items whose note says "roadway centre" or that sit in the drive-through and streetscape groups, **24 look within 29 deg of a street in range and these two looked 68 and 87 deg across one**. | Done. Both viewpoints are corrected to the crossing their own note names, taken from `data/processed/roads/segments.parquet`, and both azimuths snapped to that street's measured bearing -- 351.4 deg for Stuyvesant Avenue, 32.4 deg for Seventh Avenue. Each `meta.json` keeps `corrected_from`, `corrected_why` and `corrected_source`, so the change is auditable rather than silent. Both now stand 0.3 m from their street and 0.0 deg off it. `tools/audit_viewpoint_aim.py` runs the audit over every item, and `tests/test_comparison.py::test_a_roadway_viewpoint_stands_on_its_street_and_looks_along_it` keeps it true. |
| J49 | **The camera's clearance probe could not see the simulation's traffic and crowd at all, and they could blind it to the buildings behind them.** `_opaque` counts building shells, landmark models and props; `add_agents` names everything it places `agent_veh_*` or `agent_ped_*`, so no vehicle and no pedestrian has ever been able to be the nearest obstruction. The record said so anyway: Arthur Avenue's note reads "the nearest solid thing **anywhere in the frame** is prop_tree_sophora_small_9 18.8 m away" over a render whose own caption records 281 placed pedestrians, two of them at the lens. | Two faults, one cause. The first is the sentence: a true measurement of the built city, written as a claim about the whole frame -- the same shape as I17, J44 and J47, this time inside the caption rather than the probe. The second is worse and was found while fixing the first: `Scene.ray_cast` returns the **first** hit and nothing else, so `if hit and _opaque(ob)` asks "is the nearest object of any kind one that counts?" and not "what is the nearest object that counts?". A pedestrian standing between the lens and a wall made that ray report nothing, and the frame read as clear. `view_distance` stepped past what it did not count, but with a budget of eight steps written for a scene with nothing moving in it -- a street holding 89 vehicles and 281 people can spend all eight on their front and back faces and then report 150 m of open air with a wall 20 m ahead. Both faults arrived with Stage 21, which put the agents in the scene **before** the camera decision rather than after it. | Done. `_ray_past` restarts the ray a millimetre past every hit that does not count, so both probes answer the second question; `view_distance`'s budget is 48 steps and its rule is named (`_closes_view`) rather than inlined. `nearest_obstruction` takes the predicate as an argument, and `frame_clearance` reads the frame twice -- once for built fabric, once for agents -- so the record carries `nearest_agent`, `nearest_agent_m` and `nearest_agent_at_deg` beside the built numbers and the note names both. **The rule that moves the camera still counts built fabric only, deliberately:** a viewpoint that moved because a simulated pedestrian walked past the lens would land somewhere else on the next seed, and the sheet would stop being a comparison of one view. Two tests in `tests/test_comparison.py` hold it: a wall at 8 m behind a person at 1.75 m is found at 8 m by the built probe, at 1.75 m by the agent probe, and the street measures 8 m rather than 150. |

| J50 | ~~**Every exported model carried its own copy of every texture it used, and 88.5 % of those bytes were duplicates of another file's.**~~ **Closed.** A `.glb` holds one binary chunk, so Blender's exporter writes each image a material references into it in full. Measured over the 541 non-tile files: **2.950 GB of image data in 2,847 references to 486 distinct images -- 2.610 GB of it byte-for-byte duplicate**. `Leather026_2K_normal.jpg` (7.37 MB) was written into 41 files, `Concrete030_1K_normal.jpg` into 42, `Carpet013_2K_normal.jpg` (10.33 MB) into 16. The tiles are unaffected: they carry 12.7 GB of geometry and no embedded images at all. | Not a disk problem -- an import problem. Unreal's glTF importer creates one `UTexture2D` per *reference*, so the cooked build would have held 2,847 texture assets for 486 images and the streaming pool on the 8 GB card would have paid for every copy. It also broke the LOD chains it touched without changing a single triangle: `arrow_ebike.glb` was 45.2 MB, its LOD1 45.1 MB and its LOD2 44.9 MB, because the decimated meshes still carried all nine 2 K maps -- a level of detail that is 99 % of the level above it is not one. And it made the delivery package the user downloads 2.5 GB larger than the content in it. The reason it survived this long is that nothing measured it: every check on these files counted nodes, triangles, materials and bones, which the duplication does not change. | Done. `blender/common/glb_textures.py` writes each distinct image once into a `textures/` directory **beside** the `.glb` and points `images[i].uri` at it. The URIs descend and never climb (`textures/x.jpg`, not `../textures/x.jpg`): a single shared directory would have saved a further 10 MB and would have required every consumer to follow a path out of the file's own directory, which the spec allows and this container cannot test Unreal's importer against. Removing an image's buffer view shifts every later one, so the rewriter renumbers **every** `"bufferView"` key in the whole JSON tree rather than the four places today's exporter puts them, keeps any view something other than an image also reads, and re-reads the written file to compare the bytes of every accessor before it replaces the original. Measured result over 457 files: **3.434 GB of `.glb` -> 0.484 GB plus 359 MB of images, 3,374 MB of directory -> 874 MB**, and the e-bike LOD chain reads 0.38 / 0.30 / 0.12 MB. `nycsim_bpy.export_glb` runs it on every future export; `glb_summary` reports the URIs, the manifest carries them per entry as `sidecars` and warns about any image a file names and disk does not have, and `tools/package_content.py` ships them -- a `.glb` is no longer self-contained, and a package without them would deliver untextured meshes with no error to say so. Nine tests in `tests/test_glb_textures.py`, one of which walks the built tree and refuses any `.glb` naming an image that is not beside it. |

| J51 | **Every window, door and storefront in the city is built, placed, counted -- and then hidden behind an unbroken wall.** The building shells have no openings cut in them. The facade kit's window pieces stand **0.050 to 0.065 m** proud of a wall whose surface sits within a centimetre of the placement anchor (median +0.003 m over 311 measured), and extend **0.610 m into** the building behind it. Approaching each piece from the street along the wall's own outward normal, the first thing hit is the shell for **273 of 300** windows sampled, **129 of 143** door entries and **46 of 94** storefronts -- against **8 of 189** window accessories and **2 of 90** cornices, which stand proud and are visible. Windows are **68.7 %** of all 42,546,790 kit placements, doors 6.9 %, storefronts 2.3 %: about **33 million pieces, four fifths of the entire facade kit, render as nothing**. | This is what the Bed-Stuy sheet shows and it is the largest photorealism deficit in the build. The near walls are unbroken planes carrying a regular grid of small pale bars -- the 5 cm lips of the window frames, all that stands in front of the wall -- with fire escapes, arch hoods and through-wall air-conditioners floating on them and no aperture, reveal, frame or glass anywhere beneath. Nothing measured it because nothing was looking: the kit lane checks that each piece is built to size and anchored to its contract, the placement lane checks that each placement resolves to an asset and lands on a wall run, the shell lane checks that the solid closes -- and every one of those is true here. **The missing stage is the one between them**, and it is the same shape as J21: the curb ramps were surveyed, built and placed for months before anyone noticed the pavement they belonged in had no ramp cut into it. | **Half fixed, and the half that is not is priced.** The openings are **not** cut, because cutting them is not affordable: `mapbox_earcut` gives a wall rectangle with N rectangular holes **4N + 4** triangles (measured: 1 hole 8, 2 holes 12, 16 holes 68, against 2 with none), and a hole with no reveal leaves four boundary edges used once each, so `is_closed` fails and `build_shell` silently rebuilds the whole building **without** the openings -- which means the reveal box (4 jamb quads and a back cap, 10 triangles) is not optional. **32.1 M openings x 14 triangles = 449 M triangles against the shells' 38.6 M, and at their own measured 106.6 bytes per triangle, +48 GB on today's 4.12 GB.** That is not a budget this build has, here or on the target machine. What *is* done: every window, glazed door and shopfront is now **mounted its own glazing setback clear of the wall**, so the opening it models -- reveal liner, sash, glass, interior -- stands in front of the wall instead of behind it. The distance is the piece's own measured `glazing_setback_m`, 0.063 to 0.28 m, not a constant; 42 of the 174 pieces move and nothing else does. The rule is `nycsim_pipeline.facade.kit_ids.mount_offset_m`, shipped per piece in the registry so `build_levels.py` applies the same offset in the engine as `scene.py` does in the renders -- a fix that lived only in the renderer would be a fix that is not in the game. **What this is not:** a punched opening. The reveal projects from the wall rather than being cut into it, so a raking sun shades it from the wrong side and the surround stands 0.06 to 0.30 m proud of a facade where it should be flush. The Bed-Stuy frame that showed a blank salmon plane with floating lintels now shows a tenement facade with windows, arch heads, sills and the air-conditioners sitting under the openings they belong to. Cutting the holes for real stays Stage 34, with the arithmetic above as its price. |

| J52 | **There is not one road marking in New York.** The pavement carries seven kinds -- roadbed, sidewalk, median, plaza, curb, crosswalk, parking lot -- and none of them is a marking. No centre line, no lane line, no edge line, no stop bar, no parking-lane T, no bike or bus lane, no turn arrow, no legend, nowhere in the 972 pavement files or in anything that reads them. The crosswalks that do exist are **one solid painted rectangle 3.66 m deep** across the whole carriageway (`pavement.CROSSWALK_DEPTH_M`), where a New York crossing is a continental bar pattern with gaps you can see the asphalt through. | At eye level this is the second most visible thing after J51. The Bed-Stuy frame is a flat grey plane from kerb to kerb with a white slab across it -- 309 roadbed polygons in that frame and not a line on any of them. It is also a driving problem, not only a looking one: the player has no lane to follow and no stop line to stop at, on a road network whose simulation is driving 381,971 lanes that the surface does not show. | **Open.** Stage 35. Nothing has to be invented: `lanes.parquet` has 381,971 rows carrying `index_from_center`, `direction`, `width_m` and `offset_m` per lane, and `segments.parquet` 122,235 rows carrying `traffic_dir`, `travel_lanes`, `park_lanes`, `bike_lane` and `bus_lane`, so the boundary between two same-direction lanes, the boundary between opposing directions and the parking lane's edge are all measured positions. The MUTCD and the NYC DOT Street Design Manual fix the widths and patterns (4 in white lane line, 10 ft line / 30 ft gap; double 4 in yellow centre line on a two-way street; 12 in stop bar; 12-24 in continental crosswalk bars), so the marking geometry follows from the lane geometry rather than from a guess about where a line looks right. **Measured after building it**, over 15,489 stop bars in a 120-tile sample: 15,488 pair with a crossing on the same leg and the median, p10 and p90 distance between them are all **1.22 m** -- the 4 ft the standard asks for, reproduced rather than assumed. **411 of them (2.7 %) fall outside 1.0 to 1.5 m, and 409 of those 411 are on a segment with a bend in it.** That is not the stop bar being wrong: the bar is laid across the lane by walking back along the segment's own polyline, while `pavement._crosswalks` builds each crossing as a straight rectangle extrapolated from the approach heading at the node, so on a curving approach the crossing is the cruder of the two and the gap opens to at most 6.87 m. Recorded rather than hidden; straightening it means giving the crossing the polyline too, which is a change to the surveyed pavement stage and not to the paint. A second audit checked every written lane, centre, bike and turn-line piece against the rule that produced it -- **32,392 pieces, zero faults**: each one is a boundary `markings.boundaries` actually returns for its segment, at the recorded offset and with the recorded kind. **The first version of that audit reported 616 lane lines between opposing directions and 599 not between two travel lanes, and all of them were false.** It found each marking's neighbouring lanes by its own nearest-edge search instead of asking the rule, and on lanes whose float32 edges do not exactly abut it paired different lanes than the rule did. A correct measurement of something other than the thing it stood for, in the audit written to catch exactly that. **Closed: the paint is in the engine, not only in the still-image renderer.** All 546 tiles that carry a road surface were rebuilt with it. Every marking mesh reaches Unreal as its own material slot with `surface_class 5, PaintedMarking` -- `t_1_-3` alone carries 26,023 white and 7,691 yellow triangles under `pave_marking_white_asphalt` and `pave_marking_yellow_asphalt` -- and `unreal_manifest.json` lists all 546 among its 8,170 entries with **0 warnings**. **522 of the 546 carry markings and the other 24 carry none, which was checked rather than assumed**: not one of those 24 has a single lane geometry intersecting it, so there is no lane there to mark; they are New Jersey and outer-edge tiles whose roadbed comes from planimetric data the CSCL lane graph does not cover. Over the whole city the gap is **79.2 m of 40,320 km of lane, 0.0002 %** -- four lane stubs of 16 to 24 m in `t_-7_3`, the one tile that holds a lane centroid and has no markings file. Recorded rather than rebuilt for: at that size a re-run costs more confidence than it buys. |

| J53 | ~~**New York's crowd never puts a coat on, and the coats exist.**~~ **Corrected, and the real fault is narrower and worse.** The first version of this entry said the simulation "is given the weather and throws it away" and that "no part of the crowd's appearance reads any of it". **That is wrong.** `PedSim::assignAppearance` reads it twice (`GameplayPedSim.cpp:592-598`): above 0.3 mm/h of rain it sets `kPedUmbrella`, and **below 12 degrees it sets `kPedCoat`**. The umbrella works end to end -- `NYCPedestrian.cpp:127` toggles an umbrella mesh on that flag. **`kPedCoat` is set and read by nothing.** Not by the engine's pedestrian actor, not by the verification renderer (which reads `flags` only for bit 4, "in the carriageway"), not by the choice of body. The body is a uniform draw over all 24 (`p.rng.below(kArchetypeCount)`, `GameplayPedSim.cpp:569`) and never consults the flag it was just given. | So the city has 15 outerwear garments including a wool coat, a trench coat and three puffers; **21 of its 47 garments are worn by nobody and every cold-weather one is among them** (`coat_wool`, `coat_trench`, `puffer_black`, `puffer_olive`, `puffer_red_long`, `jacket_denim`, `jacket_leather`, `hoodie_black`, `kids_hoodie`); it has a flag that says correctly who should be wearing one; and it has no body that wears one. Of the 24 baked bodies, seven wear any outerwear at all and all seven are suit jackets, field jackets or work vests. The Bed-Stuy sheet is the result: bare arms and tank tops on both sidewalks at 10:22 on 30 November 2019, under 24.9 deg of winter sun, against a reference photograph of that same morning -- at a temperature where every one of those agents is carrying `kPedCoat`. Counting cannot catch it: 281 pedestrians placed, every one from the simulation, every one on a walkable surface, and the count is right. **One link in a five-link chain is missing, and the entry above first blamed the whole chain -- which is the same error it exists to record.** | **The missing link is joined; the cast is still thin.** `unreal/tools/gen_ped_wardrobe.py` derives the table from the generator's own published contract -- which bodies wear a garment the wardrobe declares `slot == "outerwear"` -- and writes it as `GameplayPedWardrobe.h`. `assignAppearance` now reads the flag it computes: below 12 degrees it never draws a summer body and prefers a warm one. **Measured over a 3,000-person crowd at the same corner and seed: at 5 degrees 60.6 % wear a coat or a jacket and 0.0 % are in summer dress, across 21 distinct bodies; at 25 degrees 21.9 % and 11.9 %, across all 24.** Before this, both figures were the same at every temperature. Two things are stated rather than measured and say so in the generated header: that a hi-vis or delivery vest is a work layer and not warm clothing, and the half-and-half preference itself -- New York's real clothing distribution at 8 degrees is measured nowhere in this project. ~~**What is still open:** the cast has only 5 warm bodies of 24~~ **Closed, and the sentence that closed it was wrong twice.** Twelve cold bodies are baked (`npc_generator.py --season cold --first-index 24 --count 12 --keep-existing`), so the cast is **36 bodies, 17 of them warm**, and **every cold-weather garment is now worn**: `coat_wool`, `coat_trench`, `puffer_black`, `puffer_olive`, `puffer_red_long`, `jacket_denim` and `jacket_leather` each on one body. Outerwear worn by nobody falls from 12 garments to 4, and those four are work and formal layers -- `suit_jacket_db`, `suit_jacket_womens`, `vest_conedison`, `vest_delivery` -- not warmth. But **"the mask grows from the same contract and the code needs no change" was false, for two independent reasons, and baking the bodies is what exposed both.** First, `kPedWarmArchetypes` is a bitmask and the generator wrote `0xFFF409024u` -- **36 bits into a `uint32_t`**. It compiles: g++ warns `conversion changes value from 68706930724 to 4282421284` and carries on, dropping archetypes 32, 33, 34 and 35 exactly. Second, and larger, `PedSim::kArchetypeCount` was the literal **24**, written by hand, so `allMask` covered bits 0-23 and **twelve of the seventeen warm bodies were unreachable whatever the mask said** -- the five the engine could draw were precisely the five the old cast had. So the twelve bodies would have existed on disk, been named in a generated table, passed every test, and never appeared in New York. The masks are now `uint64_t`, `pickArchetype` scans 64 bits, the generator **refuses** to emit a mask past bit 63 rather than truncate again, and `kArchetypeCount` is `static_cast<uint8_t>(kPedWardrobeArchetypes)` -- read from the generated table, because a count that has to be hand-edited to follow the cast is a count that will not. Verified by compiling the header against the arithmetic that reads it: 36 archetypes, 17 warm, highest warm bit 35, and `pickArchetype` reaches archetype 35. **Reopened once more, and closed again, by J62.** This entry was closed on the strength of the baked bodies and the C++ draw, and neither is the picture. The verification renderer drew its crowds from **12** bodies of the 36, and the twelve are indices 0 to 11 -- the warm-weather half. So every coat this deviation is about existed, was correctly chosen by the simulation, and **could not appear in a single one of the 172 comparison sheets**. The evidence that closed J53 could not have shown J53 failing. Fixed under J62; the closure stands only from the eleventh render pass onward. |

| J54 | **The clearance record named an agent the very next step removed.** J49 gave the record a `nearest_agent` field, and on the first street sheet rendered with it (`drive_bronx_grand_concourse`) it read *"the nearest simulated agent is agent_ped_1913.1 1.5 m away"* -- over a frame that does not contain that pedestrian. `cull_near_camera` had already been written for exactly this problem and works: the record's own `dropped` block says **6 pedestrians over the observer** were culled after the camera moved 5 m onto the nearest roadbed. The fault is the **order**: `clear_of_geometry` measures the frame, then the cull removes what is standing in it, and the measurement was never taken again. | A true measurement of a state that no longer holds by the time it is written down -- the same family as J49 itself, introduced by the fix for J49, one release later. It is the cheap half of the family and the easy one to miss: nothing is wrong with the number, the probe, the cull or the accounting; only with when the number was taken. | Done. The reading is taken again after the cull, over the crowd the render will actually contain, and the record carries `nearest_agent_measured: "after the cull over the observer"` so a reader knows which side of the cull the number is from. `nearest_agent_probe_m` is recorded too, so "no agent" says how far it looked and the second reading uses the same distance as the first. The three sheets rendered before this was found are re-rendered with the rest. |

| J55 | **Nothing ever checked that a comparison sheet shows the thing it is a comparison of.** Bethesda Terrace is the case that found it, and it is one of the seven viewpoints §12 condition 3 names. Its render is a flat grey slab, a balustrade and Belvedere Castle on the skyline: **no terrace, no stairs, no Lake and no fountain.** Every number on the sheet is correct. The viewpoint stands where its note says (the upper level by the 72nd Street transverse); the camera walked 12 m to the parapet as it should; the Bethesda Terrace model **is** in the scene, 10,808 triangles, 51.4 m away, carrying its own fountain (`fountain_height_m` 7.92, `angel_height_m` 2.44, `basin_diameter_m` 29.26); and the fountain, 68.7 m away, sits at about -7 deg in a frame that spans -25.7 to +20.7 deg -- **inside the picture's cone**. Something is between the two, and **the first version of this entry named the terrace's own arcade wall, which was a guess, not a measurement.** What the probe measures is a cobra-head lamp standard 2.4 m from the lens. | Six checks guard these frames -- is the viewpoint on its street (J48), is the frame clear (J47), is an agent on the lens (J49, J54), is the subject in frame, is it on its model -- and every one of them asks about the *camera*. None asked about the *line between the camera and the subject*. The two audits that come closest, `tools/audit_subject_aim.py` and `tools/audit_viewpoint_aim.py`, test the subject against the frame's angles and the viewpoint against the road graph; both pass here. It is the cheapest possible question -- one ray -- and it is the only one that asks whether the sheet is evidence at all. | Done, as a measurement on every sheet rather than a one-off audit. `camera.subject_sightline` casts a ray from the eye to the subject, steps past everything that is not built fabric, and the record carries `subject_visible`, `subject_blocked_by`, `subject_blocked_at_m`, `subject_range_m` and what it aimed at. A tree counts as a blocker here although `view_distance` steps past one: a photograph of the Flatiron taken through a plane tree is not a photograph of the Flatiron. **Two versions of the probe lied before this one worked.** A single ray is stopped by that 0.2 m lamp standard and reports a subject standing wide open beside it as invisible. A fixed +-1.5 deg fan is worse: 0.13 m across at 2.4 m, so the same pole takes all five rays and the lie is unchanged. The fan is therefore a **width at the subject** -- 12 m, which is a stated choice and not a measurement, because the item's metadata does not carry the subject's width -- so it narrows with range: at 62 m it is 5.5 deg, the pole spans 2.4 deg of it and takes one ray of five, and the subject behind it is still reported as there. A wall across the street takes all five and it is not. **The Bethesda viewpoint itself is not yet corrected** -- that is a separate change to `meta.json` of the J48 kind, and it should be made against the measurement this now produces rather than by eye. |

| J56 | **Every street lamp in New York is a fixture nothing measured, and 16,938 of them are the wrong one twice over.** Two independent faults, found from the lamp standing 2.2 m in front of the Bethesda Terrace camera in J55. **First, the tag that says what a lamp looks like was never read.** `LAMP_VARIANT` searched the `support` and `subtype` columns for the substrings `cobra`, `bishop`, `historic` and `pedestrian`; measured against the extract, **not one of those four strings occurs on any of the 16,971 OSM lamp nodes**, so every one of them fell through to variant 0. The key that does describe the fixture is `lamp_mount` -- present on 1,712 nodes as `straight_mast` 561, `bent_mast` 392, `ground` 263, `high_mast` 177, `lamppost` 129 -- plus `light:mount` on 116 more and `light_source=lantern` on 230. `lamp_type`, which the old code did read, carries `led` / `electric` / `fluorescent`: the light source, not the mast. **Second, the resolver treats the variant code as a list index.** `assets.resolve` returned `choices[v % len(choices)]` over the id-sorted assets, while the catalogue entries carry their own `variant:N` tag. For `street_lamp` the two agree on 1 and 3 by coincidence of the alphabet and disagree on the rest: variant 2 is declared `lamp_bishops_crook` and the modulo returns `lamp_highmast`; variant 4 is declared `lamp_highmast` and **wraps round to 0**. Variant 0 lands on `lamp_bishops_crook`, so **all 16,938 OSM lamps rendered as ornamental Bishop's Crooks -- including the 177 that OSM explicitly tags `high_mast`.** | Four lamp meshes are built and two of them stood nowhere in the city: `lamp_bishops_crook` and `lamp_park_twin` had **zero** placements between them, and the census that would have shown it was never taken -- the build summary counted lamps by kind and by source, never by fixture. The visible consequence is a park one: the road rule walks the CSCL centreline, so Central Park's drives and the Bethesda Terrace get the NYC DOT octagonal pole with a cobra head on a 12 ft davit arm, and Central Park's lighting is a cast-iron post-top lantern that is a listed part of Olmsted and Vaux's landscape. **10,652 lamps stand on park ground; 7,520 of them are rule-placed cobra heads and 629 of those are in Central Park.** Every check that touched lamps passed: the rule places them at the right spacing behind the right kerb, the dedupe suppresses them within 25 m of a surveyed one, the manifest resolves every row to an asset that exists. All of them ask *whether* a lamp is there, none asks *which* lamp. | Done, in three parts, and one of them is a rule that says so. **(1)** `assets.resolve` reads the variant map the assets declare (`declared_variants`, from their own `variant:N` tags) and no longer indexes a sorted list; a code outside the map takes the kind's stated default and returns `variant_default:N` as its reason, and a kind that declares nothing keeps position but **no longer wraps** -- an out-of-range code is reported, not folded back to 0. **(2)** `datasets.lamp_variant` reads `lamp_mount`, then `light:mount`, then `light_source`, from the node's own tags: **1,125 lamps are now measured as mast-arm cobras, 234 as post-top lanterns and 177 as highway high-masts**, and 323 name a mount this build has no mesh for (`ground`, `bollard`, `wall`, `suspended`) and are counted as such in the build summary rather than silently made into a pole. **(3)** `DEFAULT_VARIANT["street_lamp"] = 1`, the DOT cobra head, for the 15,112 nodes that say nothing -- **a rule, not a measurement**, and the honest default for the city's ordinary street. **What OSM cannot tell us:** there is no tag anywhere in this extract that distinguishes a Bishop's Crook from a DOT davit -- the words `bishop` and `crook` appear zero times in 16,971 nodes -- so **variant 2 is still placed nowhere**, and NYC's few thousand restored historic fixtures are absent by admission rather than by guess. `furniture/park_lamps.py` is the second stated rule: a lamp whose fixture nothing identified, standing on walkable park ground (`park_ground`, `grass_field`, `recreation`, `golf`), becomes the post-top park lamp. It will not touch a fixture read from a node's own tags, and it will not touch a pole on a road a person may not walk on. **Measured city-wide: 7,433 lamps stand inside walkable park ground, 2,160 are held back, 5,273 move -- 572 in Central Park, 249 in Prospect Park, 209 in Riverside Park, 119 in Washington Square Park.** The road rule now records the `rw_type` **and** the `nonped` flag it placed each pole against, instead of letting a later stage ask which segment is nearest -- a different question with a different answer, since the pole sits half a carriageway plus 0.6 m off its own centreline. Both are needed: **1,384 vehicles-only segments are typed as plain street**, among them the Grand Central and Cross Island Parkway mainlines, and reading `rw_type` alone put park lamps on 312 of them. Neither `nonped` code is taken from a data dictionary this build does not hold; both were read off the segments carrying them -- the ten commonest names among the 10,160 `V` segments are Long Island Expy, Belt Pkwy, Cross Bronx Expy, Grand Central Pkwy, BQE, Van Wyck, Gowanus, FDR Drive, Bruckner and Major Deegan, and the commonest among the 3,983 `D` are Clove Lakes Park Path, the Hudson River Greenway, the Randalls Island and Bronx River Greenways and Central Park's own West Drive, so `V` is excluded and `D` deliberately is not. The avenue malls are NYC Parks property and arrive as `park_ground` like any lawn, so park *interior* means a polygon at least 15 m across its own short side: measured, Tudor Malls 2.1 m, Kings Highway 2.7, Flatbush 4.3, Broadway 6.7, Park Avenue 7.2, Ocean Parkway 10.1, against Riverside Park 27.4, Central Park 34.1 and Grand Army Plaza 46.4 -- 15 m sits in the empty band between the two groups, and it removed the Park Avenue malls entirely and took Broadway's from 38 lamps to 2. **What is left over, named rather than rounded away:** of the 2,791 rule-placed poles the rule moves, **705 stand in a parcel named for a parkway** -- SHORE PKWY 89, DRUMGOOLE RD E and W 117, GRAND CENTRAL PKWY 74, CROSS ISLAND PKWY 53, HUTCHINSON RIVER PKWY E 24 -- and after the vehicles-only exclusion every one of those is on the service road or greenway beside the mainline rather than the mainline itself. Whether a Belt Parkway service road through Shore Park takes a park lamp or a cobra head is a judgement, not a measurement, and no source in this build settles it. |

| J57 | **Two comparison sheets aimed a camera at a coordinate where the subject is not, and the check written to catch exactly that reported both as fine.** `landmark_kings_theatre` put its subject at 40.6497, -73.9578 -- **414.6 m north of the Kings Theatre**, four blocks up Flatbush Avenue. This build's own `c_kings_theatre` model and OSM way 250132955 agree with each other to 7.5 m and both are 411-415 m from where the item pointed. `bethesda_terrace_fountain` put the Bethesda Fountain **24.6 m** from OSM way 958635828, the very footprint the `b_bethesda_terrace` model was built on and sits on to within a millimetre -- outside its own 29.26 m basin, on the plaza pavement. And **J55's sightline probe called the Bethesda fountain visible, 5 rays of 5 clear, over a frame that shows almost none of it**: the probe cast to the subject's *coordinate*, met nothing there because nothing is there, and could not tell an unobstructed view of the subject from an unobstructed view of thin air. | Every downstream guard passed on the Kings Theatre, and each one was measuring something true: the viewpoint sat on a real Flatbush Avenue sidewalk, the azimuth was the exact bearing to the subject, the subject was inside the frame's cone, and the photographs were geosearched in a 250 m circle around it. **All of them take the subject's coordinate as given.** A sheet built from that would have been a photograph of the theatre beside a render of a different street, captioned with the theatre's name -- the one failure mode §12 of the brief calls the worst thing in this project, and it would have been produced by a pipeline in which nothing was broken. The Bethesda case shows the second half: J55 built a probe *specifically* to ask whether the sheet shows its subject, and the probe inherited the same unchecked assumption, so the fix for a lie reported a new one. | Done, in three parts. **(1) The probe now distinguishes three states, not two.** Each ray is cast *past* the subject, and the record says whether something built stands closer (`subject_blocked_by`), whether the ray lands on geometry within `subject_reach_m` of the coordinate (`subject_visible`), or whether **nothing is there at all** -- in which case `subject_lands_on` names what the ray found instead and how far past, and `subject_note` says the line is open and the subject is not on it. 25 m of reach is a **choice**, stated as one: wide enough that a landmark modelled on its real footprint is not missed because its metadata names the plaza rather than the basin, narrow enough that the ground 200 m behind cannot pass for it. **(2) Both coordinates are corrected at the source**, in `fetch_photos.py`, not in the fetched artefact: the Kings Theatre subject becomes the model origin that agrees with OSM, and its viewpoint is derived from the CSCL centreline it is described as standing on -- 17.1 m of carriageway, so half of that plus 3 m of sidewalk, giving 81.8 m from the theatre's centre at azimuth 87.5 deg, which is the "about 60 m from the front" its note claims. The Bethesda subject becomes the OSM fountain. Both items were re-fetched so the sheets and the printed distances agree with them; the Bethesda caption that read "27 m from Bethesda Fountain" now reads 51 m, which is what it always was. **(3) `pipeline/tests/test_reference_subject_positions.py`** makes the audit permanent. It is deliberately narrow so that it cannot become the kind of loose matcher it exists to catch: a subject is compared only against an OSM feature whose name matches **exactly** after case, punctuation and parentheticals are normalised. **A looser first version paired "New York State Pavilion" with "Staten Island Skating Pavilion" 39 km away** -- a correct measurement of something other than the thing it stood for, produced by the audit written to catch that, which is the third time that shape has appeared in this file. So it covers 43 of the 138 subjects and claims nothing about the other 95. It fails past 150 m, which is further than any NYC subject's own extent reaches from its node, and **prints without failing the 16 that sit 30-150 m out**: the Oculus 129.6, the Javits Center 97.5, Chelsea Market 88.3, the Vessel 74.8, 10 Hudson Yards 50.0, the Whitehall and St. George ferry terminals 49.6 and 44.4, Port Authority 47.7, the Unisphere 44.4, the Brooklyn Museum 39.2, the Wonder Wheel 38.0, the Metropolitan Opera House 36.5, One World Trade Center 33.4 for three separate items, and the Dakota 30.7. Most are explained by their own size -- Chelsea Market's block runs 250 m, Port Authority covers two -- but **the Oculus at 129.6 m and the Vessel at 74.8 m are larger than the buildings are**, and they are recorded here as open rather than quietly rounded into the acceptable band. The probe in (1) is what will settle them without a name match: an item whose subject coordinate has nothing on it now says so on its own sheet. **And the first render against the corrected coordinate found a third fault, in the fix itself.** The probe reported *nothing* within 25 m of the fountain over a scene that has the fountain exactly there, because it was aiming into open sky: `subject_top` returns an **absolute** NYC_TM elevation -- `choose_lens` has always read it as one, `rise = top_z - cam_z` -- while its docstring said "above the camera's own eye level", and the new probe believed the docstring and added the camera's own elevation to it, putting the aim point 25 m above the angel. Aiming at the *top* was wrong even with the right datum: a ray sent at the highest point of a subject grazes it, and a 7.92 m fountain whose top 2.44 m is a thin bronze angel is missed by a ray a centimetre high. The aim is now the subject's **mid-height in absolute elevation** -- the same point `aim_pitch` already tilts the camera towards -- and the docstring says what the function returns. With that corrected the sheet reads: range **81.4 m**, aimed 3.9 m above the fountain's ground, **1 ray of 5 lands on `lm_b_bethesda_terrace.1` at 80.8 m** and **3 are stopped by `lm_b_bethesda_terrace.20` at 14.7 m**. So the fountain is there, it is found, and the terrace's **own lower balustrade** is what hides four fifths of it from the upper level -- which is what the picture shows, and which no version of this probe had said before. `subject_visible` stays false on a one-of-five majority; the counts are beside it so a reader can see that "false" here means *mostly* hidden rather than absent. **One side effect, measured and left alone:** moving the Kings Theatre subject onto the building put the geosearch circle inside a famously restored auditorium, and the re-fetch returned three interiors for an item that is a view of the marquee. `INDOOR_WORDS` catches "lobby", "hallway" and "corridor" and knows nothing of theatres; widening it would re-select the photographs of all 172 items to fix one, so the theatre vocabulary is item-local. Two exteriors came back. The third candidate is an auditorium during a concert whose title, description and Commons categories contain no indoor word at all, so no text rule can reject it -- and it does not reach the sheet anyway, because it was taken at 22:59 and the day/night gate in `pick_reference_photo` keeps a night frame out of a daylight item. |

| J58 | **One prop in five is the alphabetically-first asset of its kind rather than the object that is there.** J56 fixed this for street lamps and stopped there; the same fault is live on eight more kinds and is larger. Every prop row outside `street_lamp` carries `variant` 0, and `assets.resolve` answers 0 with `choices[0]` over the id-sorted assets, so a kind with several assets shows exactly one of them across the whole city. Measured over the 1,648 tile `props.json` files against 1,528,008 placements: **`manhole` 288,174 rows, 2 assets, all `manhole_coned` and zero `manhole_dep`**; **`bus_stop_sign` 13,341 and `rtpi_sign` 491, 7 assets, every one drawn as `sign_nyc_parking_18`**; `waste_basket` 5,611, all `litter_basket_betterbin` and zero wire baskets; `citibike_dock` 2,507, all `citibike_bike` with no dock rail and no kiosk; `flagpole` 1,655, all `flag_nyc_pole` and zero `flag_us_pole`; `steam_vent` 1,402, all 3 m and zero 6 m; `utility_pole` 924, all 2 m and zero 3 m. **314,105 placements in total.** | Two of them are wrong in *kind*, not merely monotonous. A bus stop sign is a tall MTA blade on a pole and an 18-inch NYC parking regulation plate is a small sheet-metal rectangle: 13,832 of the first are drawn as the second. And a Citi Bike station is a dock rail, a payment kiosk and the bikes in it — this city has 2,507 bikes standing in the air. The reason it survived is the same as J56's: every check counts *whether* a prop resolved to an asset, and `prop_kinds_without_an_asset` is 0 for all eight of these because one asset always resolves. Nothing anywhere asked **which** asset, and the id-sorted list makes "wrong" look like "chosen". | **Open — Stage 38, and deliberately not started mid-pass**, because it changes `props.parquet` and the sheet pass would have to restart a ninth time. The shape of the fix is J56's and is already built: a declared `variant:N` map on the assets, honoured by `resolve`, with a per-kind default that says it is a rule. What must *not* happen is a fabricated distribution. NYC's covers are DEP, Con Edison, Empire City Subway and half a dozen others, and **this build holds no source that says which is which** — the manholes are rule-placed to begin with (`rule:manhole_*`) — so inventing a split would be a procedural result dressed as a survey, which is the one thing §12 of the brief forbids outright. Where a source does speak it is used: OSM `operator` and `support` tags, planimetric feature codes. Where none does, one honest cover stays. **A ninth asset is placed nowhere at all and has no kind to be placed by:** `muni_meter`, built to the Parkeon Strada's published dimensions, while 713 of the 1,036 OSM `amenity=vending_machine` nodes carry `vending=parking_tickets` and want precisely it. **Which of the eight can be fixed honestly was then measured, one attribute at a time, rather than assumed:** **`flagpole` can** — OSM `subtype` is populated on 931 of the 1,655 and reads `national` **798**, `regional` 83, `municipal` 17, `advertising` 8, `religious` 4, which is exactly the flag the pole flies, so **798 poles should carry the Stars and Stripes and all 1,655 currently carry the city flag**. **`citibike_dock` can** — each of the 2,472 stations carries its real `capacity`, min 4, median 23, max 123, **74,189 dock points in total**, so the city should show 74,189 dock units and 2,472 kiosks and instead shows 2,507 bicycles standing alone. **`manhole` cannot** — 47,427 of the sampled rows are rule-placed and 182 come from OSM, of which **4** name an operator. **`waste_basket` cannot cleanly** — `material` reads `metal` 675 and `plastic` 127, and both the wire basket and the Better Bin are metal, so nothing separates them; `subtype` (`trash` 2,193, `recycling` 47, `dog_excrement` 28) describes the contents, not the basket. **And `utility_pole` turns out to be worse than a collapse:** its 924 rows carry `height_m` **11.0 m** and `material` `wood`, and the kind is aliased to `sign_post`, so **924 eleven-metre wooden poles carrying power and telecom are drawn as a 2 m u-channel sign post** — not the wrong variant, the wrong object, and the alias is the fault rather than the variant map. **That last one was mechanically checkable from the day both catalogues existed** — the kind declares its dimensions and the asset carries its measured ones, and nothing ever compared the two. Compared now, exactly three kinds are drawn by an asset more than twice or less than half their declared height: `utility_pole` 11.0 m against 2.44 m, `bus_stop_sign` and `rtpi_sign` 3.0 m against 0.46 m. Two more sit just inside the threshold and mean the same thing: `citibike_dock` declares 1.9 m and is drawn by a 1.14 m bicycle, and `flagpole` declares 10.0 m and is drawn by a 6.21 m pole. `test_no_prop_kind_is_drawn_by_an_asset_of_the_wrong_size` holds all of it. **Partly closed, and the two that were the *wrong object* are the ones that went first.** **`bus_stop_sign` now has its own asset.** `sign_mta_bus_stop` is a 12 x 42 in blade on a 10 ft galvanised U-channel post, blade bottom 2.0 m above grade, in MTA blue with a bus pictogram and BUS STOP; route numbers are **not** drawn, because they differ per stop and this build has no per-stop artwork, so putting any on 13,341 signs would be a fabricated legend. **13,341 placements move off the parking plate.** **`rtpi_sign` (491) and `utility_pole` (924) are now unplaced**, which is the rule `billboard` already followed: a real-time information screen and an 11 m wooden distribution pole have no asset, and an absence is honest where a 0.23 m parking plate and a 2.44 m sign post are not. **`flagpole` is split from the tag that says which flag flies.** The earlier reading of 798 `national` was wrong -- counted over the extract now: `national` **941**, blank 764, `regional` 86, `municipal` 18, `advertising` 9, `national;organisation` 9, `governmental` 8, `organisation` 7, `religious` 4, `university` 2, `military` 1, `national;national` 1. OSM joins values with `;`, so a list containing `national` flies the national flag; after dedupe the city has **812 US poles and 843 city poles** where it had 1,655 city poles. Everything else stays on the city pole rather than have a state or a university flag drawn as either. **And the aggregate catalogue nothing wrote is why the new sign nearly did not count:** `props_asset_catalog.json` is what the placer reads, `build_props.py` only ever wrote the per-asset files beside it, and a `--only` build left `sign_mta_bus_stop` exported, catalogued, and invisible to the city. It is now rewritten on every props run, partial ones included. **Verified by the accounting identity over all 1,648 tiles rather than by searching:** 1,773,764 rows = 1,526,593 placed + 247,171 unresolved, difference **0**. **Closed for `citibike_dock` on 2026-09-09 (Stage 40), and the closure is one row per part rather than a variant pick.** `furniture/citibike.py` replaces each of the **2,507** station rows, after dedupe, by **2,507 kiosks** (`variant` 1, one per station, carrying the station's `capacity` and GBFS attributes), **74,189 dock units** (`variant` 2, one per dock, `sum(capacity)` exactly) and **35,325 bicycles** (`variant` 3) -- **112,021** rows of kind 7 where there were 2,507, and the three assets now declare `variant:1/2/3` so the resolver has no `choices[0]` to fall back to (0 is never written; a unit test and the world test hold it). Read back from the written tiles: every neighbouring pair of docks is **0.900 m** apart and every kiosk is 0.900 m past dock 0; over all 1,648 tiles kind-7 rows = 2,507 + 74,189 + 35,325 = 112,021, `sum(capacity)` = 74,189 = dock rows, distinct `attrs.station_id` = 2,507, `variant` 0 rows = 0; the other 33 kinds carried over **1,771,257** rows, unchanged to the byte -- **315** tiles were rewritten (the 314 that held a station and `t_2_2`, which the run of *Page Pl & Grand Ave* crosses into) and the other **1,333** `props.parquet` files are byte-identical to the pre-run copies, which is what `write_tiles` now measures rather than asserts. **Every number that is a rule is named as one** (`build_summary.json` `citibike.rules`, `attrs.rules` on every part): a single straight run centred on the GBFS point, because GBFS documents lat/lon as the station location and nothing says where the run's centre is; the kiosk one pitch beyond the last dock at the axis+180 (south/west) end; docks and kiosk facing the same side of the kerb, and **which side is the sidewalk is in no source of this build**, so about half the city's kiosks stand at the wrong end and no number can be given for which half; **0.90 m is the catalogue's *observed* pitch and the asset's `tile_pitch_m`, not a Lyft specification**, and every station length inherits it linearly. **The measured cost of the straight-run rule:** run length median 20.7 m, p95 62.1 m, max 110.7 m; **609** runs exceed 30 m, **296** exceed half their nearest CSCL segment and **66** are longer than the whole segment, so the largest stations run through intersections where the real station is a double row, an L or two runs. **The axis is the local bearing of the nearest CSCL centreline** (`citibike.station_axes`, the same tangent `rules._densify` takes, reduced mod 180) accepted within 25 m -- the value that separates the two populations, nearest-segment distance p50 5.7 m, p95 17.8 m against the 113 beyond -- and **113 stations have no derivable axis: all 78 Jersey City (region 70) and all 28 Hoboken (region 311) stations, for which `roads/segments.parquet` has no roads at all, plus 4 NYC and 3 unregioned**; they are laid east--west with `heading` NaN, which is the direction both consumers draw a NaN heading in, so the file says what the picture shows. 79 stations' parts fall in two tiles; identity is `attrs.station_id`, never `prop_id` contiguity. **The 35 capacity-0 stations are their kiosk alone** and are named in the summary. **The bicycles are one minute of one day, and say so.** No `station_status` was in the repository, so the source was registered (`citibike_gbfs_station_status`) and fetched through the project's download stage: `last_updated` **1788951780 = 2026-09-09T11:03:00Z**, ttl 60 s, GBFS 2.3, fetched at 11:04 UTC, four days after the `station_information` snapshot of 2026-09-05 (all 2,507 ids join). It reports **35,399** bikes available; **35,325** are placed because the count is capped at capacity, and the 74 dropped are four stations reporting more bikes than docks (*Cherry St* 25 in 5, *Central Park W & W 76 St* 29 in 4, *23 St & 31 Ave* 14 in 0, *53 St & Roosevelt Ave* 15 in 0). 16,018 of them are e-bikes and are drawn by the one bicycle asset. Which docks are occupied is a fill rule (from the kiosk end); the snapshot says how many, never which. Every bike row carries `dataset_id` `citibike_gbfs_station_status` and `attrs.snapshot_last_updated`, so the instant can be found from any row; 79 stations the snapshot marks `is_installed` 0 and 95 `is_renting` 0 are placed because `station_information` lists them (a rule, counted). A build without the file gets **no** bikes and a summary that says so and how to fetch one -- never an invented occupancy. **One thing the rebuild exposed rather than fixed:** the per-part ground sample from the spot-elevation IDW (`elevation.py`) puts steps into a straight rail. Over the 71,717 neighbouring dock pairs |Δz| is 0.005 m at the median and 0.029 m at p90, but **2,855 pairs step more than 0.10 m, 590 more than 0.30 m and 93 more than 1 m**, in 1,419 of the 2,472 stations with docks; the worst is *E 60 St & York Ave*, 33 docks whose z climbs from 7.2 m to 26.6 m with a 9.7 m step between two neighbours, then *South St & Pike St* (1.5 to 11.7 m, step 9.4 m) and *Pearl St & York St* (step 6.1 m) -- stations beside grade-separated structures whose spot elevations the IDW reaches. It is recorded here and not smoothed, because a slope invented to hide it would be exactly the substitution this register exists to name; the fix is the §3 terrain raster the props were meant to sit on. The engine sees the same parts: the manifest was re-run in full and over all `props.json` `SM_citibike_kiosk` receives 2,507 placements, `SM_citibike_dock_unit` 74,189 and `SM_citibike_bike` 35,325, with no `variant_unmapped` / `variant_out_of_range` reason anywhere; the accounting identity is IDENTITY_PLACEHOLDER. What is still open under this entry: `manhole` (288,174) and `waste_basket` (5,611) stay on one asset because no source in this build separates them, as measured above. `muni_meter` is still placed nowhere. And the kerb-axis helper written here is the piece every other kerb kind lacks -- that is J84. **Follow-on from J84's closure (Stage 56, 2026-09-09): the side this entry said no source holds is in the geometry after all.** The sign of the cross product of the segment's local tangent with the vector from the foot of the perpendicular to the GBFS point says which side of the centreline the station stands on (`kerb.side_of_centreline`), and the roadway is toward the centreline; `citibike.station_facing` now writes `heading` as the perpendicular pointing *away* from it -- the kiosk terminal and the dock forks to the footway, the bikes extending toward the roadway, which is the one thing here that is still a rule, because the asset docstrings do not say which way a dock's fork faces the kerb. Measured on the rebuild: of the 2,394 stations with an axis, **1,223 flipped by exactly 180 deg** (1,223 kiosks, 36,246 docks, 17,302 bikes) and 1,171 already faced away; `side` left 1,185 / right 1,209 / on 0, with **31** stations within 0.5 m of the centreline where the sign is a coin (counted in `citibike.side_from_offset_under_0_5m`); the 17,302 bikes of the flipped stations moved by exactly 1.010 m (twice the 0.505 m setback), which is what put the one Citi-Bike-only tile `t_4_-2` among the rewritten tiles; dock positions, kiosk positions and every count are unchanged. `citibike.RULES`, DATA_CONTRACTS §8.1 and the catalogue entry now say the side is read, not ruled. |

| J59 | **The sheet printed three different distances to one subject, and the one labelled "from the camera" was measured from a position the render had rejected.** Bethesda Terrace's caption read *"Subject: Bethesda Fountain, **51 m** from the camera"* on one line and *"aimed at Bethesda Fountain **93 m** away"* five lines below, while the sightline block recorded **81.4 m**. The 51 m is the distance from **photograph 1's EXIF GPS** to the fountain -- a position the render examined, found to be under 1.8 m of paving, and abandoned in favour of the item's own viewpoint. `subj_dist` was taken once at the top of the render from the origin `view_origin` first returned, and the fallback that replaces that origin updates the camera, the azimuth and the record's `camera_origin` and never re-measures it. | The stale value did not stay on the caption. It also sized `min_view_m`, the clearance rule that decides whether a viewpoint has enough open air to be usable at all: **the frame was required to be clear for 25.7 m where the rule asks for half the subject distance, 46.7 m**. So a measurement of a rejected position was quietly setting the acceptance threshold for the position actually used. It is J54's family exactly -- a true number about a state that no longer holds by the time it is written down -- and the third time that family has appeared in this pass. | Done. The distance is re-measured from the origin the fallback settles on, and the record keeps all three readings under names that say what each is: `distance_m` **81.4 m** from the placed camera *after* the clearance walk (which is what "from the camera" means and matches `sightline.subject_range_m` exactly), `origin_distance_m` **93.4 m** from the origin the aim was decided at, and `rejected_origin_distance_m` **51.4 m**, kept so a reader can see what was discarded. `min_view_m` becomes 46.7. **The first attempt at this fix was itself an instance of the fault:** it measured from `placement` before `clear_of_geometry` had walked it and labelled the result "after the clearance walk", giving 93.4 m under a name that promised 81.4. `clear_of_geometry` mutates the placement it is handed, so the reading has to come after the call, and it now does. |

| J60 | **A drive-through sheet compared one block against a photograph of another, and its caption called the photograph mis-tagged.** `drive_bronx_grand_concourse` renders Grand Concourse at East 165th Street and its reference photograph is the **Dollar Savings Bank at East Fordham Road, 3,854 m up the same avenue**. The sheet's own caption says the photograph's GPS was *"rejected as mis-tagged"* — and the GPS is correct. The photograph is exactly where its title says it is; **the pairing is what is wrong**, and the render blamed the evidence for the mismatch. `drive_brooklyn_bed_stuy_stuyvesant_ave` is the same shape at 1,808 m (Nostrand @ Greene, a different Bed-Stuy corner) while a photograph taken **46 m** from its viewpoint sat unused in the same folder. | `pick_reference_photo` orders on lighting, whether a real EXIF timestamp exists, the sun's height and the azimuth error. **There is no distance term in the key at all.** For a landmark that is right — you photograph the Verrazzano Narrows Bridge from 930 m and the Chrysler Building from 257 m — and for the `streetscape` and `vehicle` items, which are pictures of a *type*, distance means nothing. For a drive-through it is the whole point: the sheet is a comparison of one block's width, storey height and shopfront rhythm, and a photograph of a different block cannot be that however good its clock. Measured across the catalogue: **128 of 475 photographs with GPS (26.9 %) stand more than 250 m from their item's viewpoint** — legitimate for 100 % of the vehicle items and 21.8 % of the landmarks, and **11 of the 29 drive-through photographs (37.9 %)**, which is where it does damage. | Done, and two attempts were needed because the first was too crude. A **binary gate** at 250 m does not fix Grand Concourse: all three of its photographs are beyond any sane radius, so the gate ties them and the timestamp decides again. So the distance is **graded** into bands (250 m, 600 m, 1,500 m) and the band ranks **above** the timestamp for this group — a stated judgement, that a photograph of the wrong block cannot be repaired by a precise clock while an assumed hour is an approximation the sheet already declares. **The second attempt then picked a FreshDirect delivery truck**, because a photograph carrying no GPS was scored as band 0: an unknown position was being treated as evidence of nearness. Unknown now ranks third of four — worse than known-near, better than known-far — and the Stuyvesant Avenue sheet takes the Decatur Street photograph 46 m away that was there all along. Every drive-through item now uses a photograph of its own block: **16, 290, 46, 37, 56, 20, 35, 567, 222 and 209 m**. Three of the ten change (Grand Concourse, Bed-Stuy, Park Slope) and are re-rendered by name rather than restarting the pass. Every other group keeps the old ordering exactly. |
| J61 | **The sheet caption introduced its scene counts with the words "In frame", and the landmark count is two and a half times what a frame can hold.** Broadway at Wall Street builds **14 landmark models** into its scene and captions them *"In frame: ... 14 landmarks"*. Nine of the fourteen stand behind or beside the camera: Trinity Church at **54 m** and 99&deg; off the view axis, Federal Hall at 131 m and 85&deg; off, the Equitable Building at 107 m and 135&deg; off, the Brooklyn Bridge at 1.2 km and 102&deg; off. Measured over every sheet that carries landmarks at all: **449 models placed, 180 that can fall inside a frame (40 %), and 47 of the 52 sheets overstate.** The same word covers the pavement and prop counts, which are radius counts too -- this sheet's *"12,518 marking_white"* is over **776 m**, while **3** marking polygons lie within 30 m of the camera inside the cone and 51 within 60 m. | It is J54's family again and the fourth appearance in this pass: a correct measurement of something other than the thing it stands for. The damage is not the caption but what is written from it -- an assessment quoting *"14 landmarks in frame"* describes a picture that shows at most five, and the reader has no way to catch it because the number is real and the render did place them. The pavement figure misleads in the same direction: it reads as a densely marked road and the road under the camera carries three painted polygons, which is **correct** for this block -- 4 in dash paint on a 12.2 m period -- and looks like a failure only because the caption invited the wrong comparison. | Done, and without a re-render. The counts were never wrong; the sentence that introduced them was. The line now opens *"Built into the scene within N m of the camera, not all of it in frame"* and the landmark count carries its in-cone figure beside it: *"14 landmark models of which 5 can fall inside the 54.4 deg frame"*. The frustum arithmetic lives once, in `tools/sheet_facts.py`, and both the caption composer and the assessment facts call it -- `--compose-only` rebuilds a sheet from its existing `render.json` for no render time, so all 172 sheets take the new wording whenever they were rendered. It is a cone test, not a visibility test, and says so: a nearer building can still hide a landmark that crosses the field of view. **The same record carried a second trap for a writer**, found while reading it: `agent_request.dow` is not a day of the week. `DensityTable::kDows` is **3** -- one density profile for weekdays, one for Saturday, one for Sunday -- so this sheet's Sunday photograph records `"dow": 2`, which transcribes as Tuesday. That is exactly the "Monday for Saturday" error the adversarial verify pass caught in an early assessment. The facts now spell the day type out (**Sunday**) and check it against the frame's own clock. |
| J62 | **Every comparison render in this project drew its entire crowd from twelve bodies out of thirty-six, and the twelve exclude every coat.** `blender/verify/render_sheets.py` held `AGENT_NPC_ARCHETYPES = 12`, justified in its own comment by *"the 24 exported bodies through three walk phases already give more distinct figures than a frame ever holds"*. Both halves of that sentence are false. The wardrobe bakes **36** bodies, not 24 -- J53 added twelve outerwear bodies, indices 24 to 35, every trench coat, wool coat, puffer, leather, denim and field jacket in the project. And a frame holds **229 people**, not 36. `PedLibrary` then clamped again with `min(24, ...)`, so even correcting the constant to 36 would silently have kept 24. The simulation emits all thirty-six: **989 of the 2,999 people in the Broadway/Wall St snapshot (33.0 %) carry archetype 24 or above**, and every one of them was folded onto a body 0 to 11 by `arch % 12`. | The frame is the evidence, and the evidence was of a city that does not exist. With twelve bodies over 229 people **each body stands in the same frame about nineteen times**; **two of the twelve wear medical scrubs**, so one person in six on Broadway and Wall Street is dressed as hospital staff; and the mustard jacket that is body 2 appears nineteen times, which is what makes a Financial District sidewalk read as a work crew. The fold is uniform rather than skewed -- 36 archetypes over 12 bodies is exactly 3 each -- so nothing looked statistically odd; **24 of 36 bodies were simply absent from every sheet.** The worst of it is what this does to J53: that deviation is closed as *"the crowd never puts a coat on -- done"*, and the only stage in the project that can show a coat could not draw one. A fix verified by a renderer that cannot render it is not verified. | Done, and **the pass was restarted a eleventh time at 9 of 172** -- this changes the image on every sheet, which is the standing bar for a restart. `AGENT_NPC_ARCHETYPES` is now `None`, meaning every body the wardrobe baked; `PedLibrary` takes its cast from `npc_variety.json` rather than a literal, and clamps to what is on disk. The fold survives -- a snapshot from a newer simulation must not crash a render -- but it is **counted and published**: `scene.agents.snapshot.npc_archetypes_available` states the cast on every sheet and `npc_archetypes_folded` names any archetype that was substituted, so this can never again be discovered by looking at a picture. `test_the_verification_render_can_draw_every_body_the_wardrobe_bakes` holds the constant, the clamp, the glb on disk for each baked body, and every shipped sheet's own report. **A second thing was measured while confirming this and is a stated rule, not a defect:** above 12 degrees the archetype draw is uniform over all 36 bodies, so a **15 degree** May afternoon puts a third of the crowd in outerwear -- correct for a denim or leather jacket, wrong for a wool coat and a puffer. The weighting in `GameplayPedSim::assignAppearance` is a two-state rule (below 12 degrees, prefer warm) and has no mild band. Named here rather than fixed silently. |
| J63 | **The whole streamed city -- every building, every roadway, every elevated structure and every park surface -- is painted in flat colours and carries no texture at all.** Scanned over every exported `.glb`: **`tile_buildings` 920 files, 4.12 GB, 8,722 materials, 0 with a base-colour texture. `tile_pavement` 546 files, 8.34 GB, 5,177 materials, 0. `tile_structures` 845 files, 1,804 materials, 0. `tile_parkground` 43 files, 188 materials, 0.** That is **12.71 GB of geometry and 15,891 materials without a single image**. Everything authored by hand is textured -- landmarks 234 of 932, props 224 of 383, the facade kit 441 of 630, the characters 442 of 545 -- and everything generated at city scale is sixteen solid RGB values: `NYCSIM_red_brick` is `[0.42, 0.16, 0.12]` and nothing else. | This is the largest single gap against the brief's photorealism requirement in the project, and it went unrecorded because **it was measured and read as good news**. J50 states it outright -- *"The tiles are unaffected: they carry 12.7 GB of geometry and no embedded images at all"* -- as evidence that the duplication problem did not touch them. The same sentence is the finding. It is visible in every sheet: on the Broadway/Wall St frame the facade kit mounts real window geometry within **176 m** of the camera and the scene reaches **776 m**, so everything beyond the kit radius is an untextured extrusion, and the render's mean chroma is **0.023 against the reference photograph's 0.112** -- a fifth of the colour variation of the street it is a picture of. **Nothing was missing to fix it.** The catalogue `blender/common/texture_catalog.json` already holds all sixteen names plus `asphalt` and `concrete_sidewalk`; all eighteen are **already downloaded, CC0, with measured physical sizes, 251.7 MB on disk**, and are used today by the landmarks and the kit. The shells already carry `TEXCOORD_0` **in metres** (U = arc length along the facade, V = height above ground), which is exactly what a texture with a known `physical_size_m` needs. And a correct material builder already exists -- `blender/buildings/render_verify.py::_material` resolves the name, reads `physical_size_m`, wires colour, roughness and normal through a Mapping node and adds the per-building variation from the shell's own `_LIT_SEED` attributes. The buildings stage wrote a textured renderer for its own verification and **the comparison renderer, which is the project's evidence, imports the exported `.glb` and uses whatever flat material the file carries**. | **The render half is done and the engine half is now built too; what remains open is one measurement nobody can take in this container.** *The renders* can be fixed for no disk at all: the exported file's job is to carry the material **name**, and the consumer resolves the name -- so `blender/verify/scene.py` dresses each `NYCSIM_*` slot from the catalogue on import, one shared material datablock across every tile so the images load once. That is the next thing built. *The engine* is the expensive half: Unreal resolves nothing either -- no material instance anywhere in `unreal/` names `red_brick` or any other -- so a player sees the same flat colours. Fixing it in the exported files is **not possible in this container**: J50's sidecar rule writes each image into a `textures/` directory beside its own `.glb`, so eighteen materials across 1,554 tile files would be about 250 MB times 1,554, and there are **1.9 GB of writable disk**. The engine-side fix is therefore a set of Unreal material instances keyed on the sixteen names, shipped once with the content package -- named here, sized, and not pretended to be done. **And the texture library was built for exactly this and never asked.** J36 says so in its own words while closing a different fault: *"The library was built for the city's surfaces -- brick, limestone, asphalt, glass -- and the vehicle stage was written against names nobody added."* The vehicles were the ones missing from the catalogue and were added; the city's surfaces were in it from the start, and the city is the one consumer that never called it. **The first attempt at freeing disk for the render pass then deleted three of these surfaces, and the test caught it by name.** `tools/trim_texture_cache.py` kept a hand-written list of the city surfaces, read off **one** tile. There are **nineteen** `NYCSIM_*` names across the 2,848 exported tiles, not sixteen: the trim removed `precast` (396 tiles), `stucco` (156) and **`vinyl_siding` (1,087 tiles, the third most common material in the city)**, and the next render reported `precast` as flat with the reason *'no colour map on disk'*. A list of names is a claim about 2,848 files; the tool and the test now both read every material name out of every tile, and the test asserts the *reason* a surface stayed flat rather than its identity -- analytic is allowed, a missing file is not. The three are re-fetched. |
| J64 | **The road is buried under the terrain for the first thirty metres of every street-level frame, so the foreground of every drive-through sheet is bare ground.** Ray-cast straight down from points along the Broadway/Wall St view axis, the asphalt sits **below** the terrain the whole way: **-0.47 m at 1 m ahead, -0.52 at 2, -0.56 at 3, -0.62 at 5, -0.64 at 8, -0.51 at 12, -0.21 at 20, -0.17 at 30**, crossing over to +0.03 m only at 50 m. Cast along the view direction instead, every ray from 45 degrees down to 3 degrees hits `verify_terrain / verify_ground` -- the flat 0.26 brown the ground mesh is painted -- out to 29 m. The markings, the crosswalk and the kerb that are visible in the frame are all beyond that. | Not a lift and not z-fighting: a **tessellation** fault, and the docstring states the assumption it breaks. `add_pavement` says *"each vertex is lifted to the heightmap surface plus the kind's own offset, so the pavement follows the real grade"* -- true of the vertices it has, and it only has the polygon's **ring**. It ear-clips the raw outline and drapes the boundary, so the interior of a polygon is a flat fan between its edge heights. The roadbed polygon that contains this camera is **2,843 m2**, about 50 x 57 m, and Broadway rises more than half a metre across it; the terrain, sampled every 2 m, follows that rise and comes out on top. The exported tile does not have this problem, which is the measurement that settles it: **`t_-6_0/tile_pavement.glb` carries 801,235 pavement triangles, 71,591 of them roadbed asphalt**, against the **25,555** the renderer built for a whole 200 m disc spanning several tiles -- about thirty times finer, because `build_pavement.py` cuts every polygon on a 2.5 m grid before draping and the renderer re-derives it without that step. **So the comparison sheets have never shown the road surface that ships**, and the one they do show is a coarser object that the ground pokes through. It survived because nothing compared the two: the sheet reports `pavement.placed` and `per_kind`, both of which are polygon counts and both of which are right. | To do. Importing `tile_pavement.glb` would be the architecturally correct fix -- the sheet should show what ships -- and it does not fit: six tiles at 800 k triangles each is 4.8 M against a 4.5 M scene budget, and the renderer's own coarse pavement path exists for exactly that reason, undocumented until now. So the renderer keeps building its own and gains the step it was missing: cut each polygon on a grid before draping, fine near the camera where the burial is visible and coarse beyond it, which is what `build_pavement.py --grid 2.5 --refine 0.5` already does for the tiles. Sized from the geometry above and then built. Each polygon is cut on a grid before draping, graded by distance -- **3 m within 60 m of the camera, 6 m to 150 m, 16 m to 400 m, none beyond** -- because the burial is only visible in the near field and a fixed 2.5 m grid over a 900 m disc is millions of triangles a frame has no budget for. Paint is left uncut: refining a 0.1 m line costs and changes nothing. **Measured again the same way afterwards, and the sign is reversed everywhere**: the asphalt now stands **+0.091 m** above the terrain at 1 m ahead, **+0.081** at 2, **+0.092** at 3, **+0.099** at 5, **+0.078** at 8, **+0.090** at 12, **+0.106** at 20 and **+0.095** at 30 -- the roadbed's own declared 0.10 m lift, with the residual being the terrain grid's interpolation -- and the sidewalk at **+0.23** against its declared 0.25. The 200 m disc costs **25,555 -> 31,324 triangles**, 23 % more, and the roadbed goes from 59 polygons to 1,881. The sheets still show a re-derivation rather than the shipped `tile_pavement.glb`, and that remains true and remains stated: what changed is that the re-derivation now follows the ground. |
| J65 | **The Bethesda Terrace sheet does not show Bethesda Terrace.** The frame is a concrete deck, a roadway with a yellow centre line, a wall and a distant skyline; the photograph is the Angel of the Waters, the fountain basin, the Lake and the Ramble. The record says so before a reader has to: **`sightline.subject_visible` is `false`**, one of five rays lands on the subject, and the line of sight is closed at **14.7 m** by `lm_b_bethesda_terrace.20` -- part of the structure the camera is standing on. Frame mean **0.7329** against the photograph's **0.391**, chroma **0.0644** against **0.1435**, and a 5th percentile of **0.5113** against **0.0933**: nothing in the render is dark, in a picture whose subject sits under a tree canopy. | A camera-placement failure, and each step of it is defensible on its own. The item's recorded viewpoint is **one lat/lon for a two-level structure**, so the eye landed on the *upper roof* of the terrace model, 93.4 m from the fountain. The photograph's own EXIF GPS is 42.7 m from that point and **51.4 m from the fountain** -- close to where the picture was actually taken -- and the render **rejected** it because at that position the eye point is under `verify_pavement`, with **1.8 m of ground or paving directly overhead**. That overhead surface is the terrace's *upper deck*; the photographer was standing on it and the render was standing in the arcade beneath it. The rule that picks an eye height reads a heightmap and a pavement surface, and has no way to express "the deck, not the vault under it", so it discarded the right position for a reason that is true of the wrong storey. The clearance walk then did what it is for -- 12 m along the azimuth to the last point the roof supports -- and made the framing worse, because it was walking along the wrong surface. | **Done, after the v15 pass, in `camera.deck_underfoot`.** The rule looked for a landmark surface *directly* over the eye and within one eye height, and both were true of the wrong storey: the up-ray met the plaza polygon draped over the terrace's upper deck first, and the deck was 1.8 m up. It now looks **through** a pavement or terrain skin for the landmark deck under it (`_first_landmark_deck_above`), follows a slab from its soffit to its **top** face rather than standing the eye one eye-height above the soffit -- inside a 2 m slab -- and accepts a level deck up to `DECK_RISE_MAX_M = 8` m overhead, a storey and a half: a terrace step is about 5 m and the Hudson Yards platform about 7 m over the rail yard's DEM. Open sky over the lifted eye is what tells a deck from the floor of a building; a floor with a ceiling over it fails `_blocked` at the lifted height and is left alone. `test_an_eye_under_a_landmarks_deck_is_lifted_onto_it_even_through_its_paving` holds all four cases. **Amendment, from the first re-render:** the photograph's GPS was still rejected, because the thing overhead at that point is not the landmark model at all -- it is `verify_pavement`, the DoITT plaza polygon that bridges the two levels as one surface at the upper one, with no terrace geometry between it and the eye. Measured on the heightmap: the point itself reads **17.76 m** (the arcade), the upper terrace **23.17 m** within 5 m, the paving 1.8 m over an eye at 19.36. So the deck rule accepts a *paved* surface overhead as the deck **only when the viewpoint note names a raised surface** (`names_a_raised_surface`, the same word list that already chooses the ground mode) -- the note is the source that says the photographer stood on the upper level, and the geometry says a level paved surface stands a step overhead; without the note an eye under paving is under a road and is left to the sideways snap. Held by the fifth case of the same test. The photograph's GPS -- 51.4 m from the fountain, on the upper deck -- is therefore usable, and the sheet is re-rendered from it. **Superseded text follows for the record:** Open, and **narrow**: censused over every shipped sheet, exactly **2** put the eye on a landmark model at all (`bethesda_terrace_fountain` and `landmark_911_memorial_pools`) and exactly **1** records `subject_visible: false`. So this is re-rendered by name rather than restarting the pass, which is the standing rule. The shape of the fix is a multi-level viewpoint: the reference item needs to name the *deck* it is taken from, or the ground rule needs to accept a walkable surface **above** the eye when the surface is a landmark model's own deck and the photograph's GPS agrees with it -- which here it does, to 51.4 m of a subject the recorded viewpoint puts 93.4 m away. Until then the sheet is published with an assessment that says in its first line that it is not a comparison. |
| J66 | **Giving the city its textures made every daylight street render at half the brightness of the photograph it stands beside, and I did it this afternoon.** Measured on the first four drive-throughs of the pass, render mean against reference mean: Arthur Avenue **0.58x**, Grand Concourse **0.37x**, Bed-Stuy **0.49x**, Park Slope **0.51x**; chroma **0.54x, 0.41x, 0.21x, 0.34x**. The cause is in the same place: J63 pointed each shell material at a photographic colour map and **took the scan's own level with it**. Over the twenty city surfaces the AmbientCG colour maps average **0.52** of the albedo the material's own stage declares, and nine of the twenty are more than twice off -- `wood_clapboard` **0.07x** (a painted clapboard house at seven per cent of its albedo), `roof_membrane` **0.09x**, `concrete` **0.22x**, `brownstone` 0.37, `stone_rubble` 0.41, `metal_panel` 0.50, and `cast_iron` **3.09x** the other way. | A scan's exposure is a photographer's choice, not a measurement of anything, and it was being allowed to set the brightness of New York. The authored class colours are the opposite: `shellmat.py` states them per facade class and defends them, and B12 already records that they are analytic shading rather than measured reflectance. The photograph measures one thing the class colour cannot -- the *texture*: grain, mortar lines, the variation between one brick and the next, and the hue. So the two were being combined the wrong way round. **The confirmation is independent of the arithmetic**: the correction the four photographs ask for (about 2x) is the correction the albedo audit predicts (1/0.52), from two measurements that share no input. | Done. The colour map is scaled by a **single scalar** so its mean linear albedo is the one the stage authored -- `shellmat.spec` for the sixteen shell classes, `PAVEMENT_KINDS` for asphalt and the sidewalk, because `shellmat` returns its 0.6 grey DEFAULT for anything that is not a shell class and would have brightened the road six-fold. Scalar rather than per-channel on purpose: a per-channel correction forces the scan's hue to the class hue and throws away the one thing about colour the photograph does measure. Measured results: asphalt **0.926** (already right, which is why the road stays dark), concrete_sidewalk 1.153, limestone 1.128, red_brick 1.411, vinyl_siding 1.402, cast_iron 0.323. **The correction is capped at 4.0**, two stops, because past that the scan is a different material rather than a different exposure and multiplying an sRGB image by fourteen bleaches out the grain it was fetched for. Three bind the cap and each publishes its residual in the sheet's own record: `concrete` **1.135x** short, `roof_membrane` **2.852x**, `wood_clapboard` **3.498x**. **Those last two are open and are a different fault**: `WoodSiding003` is a dark stained board where NYC's frame houses are painted pale, and `Rubber004` is black rubber where a roof membrane is anything from black EPDM to white TPO. They are the wrong source, not the wrong level, and the fix is a different entry in `texture_catalog.json` chosen by measuring candidates rather than by name.  **Amendment, from the v13 pass.** The three capped surfaces are now isolated twice over, on two sheets where exposure is not available as an explanation. `drive_lower_manhattan_broadway_wall_st` takes both its clock and its position from the photograph -- the Sun from EXIF `DateTimeOriginal` at 69.5 deg, the camera on the photograph's own EXIF GPS, not moved -- and reads chroma **0.321x**. `drive_queens_forest_hills` does the same and additionally *matches on brightness*: mean **0.925x** and standard deviation **0.768x** against the photograph, with chroma at **0.254x**, the lowest ratio in the set. When a frame is within eight per cent of the photograph's luminance and carries a quarter of its colour, no amount of stopping down accounts for it. The remaining fault is exactly the one this entry already names as open -- `roof_membrane` and `wood_clapboard` are the wrong source material, not the wrong level -- plus the separate and larger fact that a facade class states one material for a whole typology while a Forest Hills Gardens wall carries four brick colours at once. Fixing the two texture sources needs candidates measured rather than chosen by name, and it changes every daylight frame, so it belongs after this render pass and not during it. **Second amendment: the first half is done and the second is measured and has no source.** The two wrong texture sources are replaced by measuring every candidate rather than choosing by name (see J73's neighbours in this file and the `albedo_source` fields the catalogue now carries): `wood_clapboard` goes from WoodSiding003 to WoodSiding013, residual **3.498 -> 1.384**, after measuring all thirteen AmbientCG wood-siding scans and finding every square one to be a dark stained board; `concrete` goes to Concrete037 at x1.78 and **no longer binds the cap at all**. `roof_membrane` stays and the catalogue records why. **Re-rendering the first three sheets after that moved their statistics by nothing**: Arthur Avenue still 0.1481 mean and 0.0329 chroma, Bethesda still 0.5343, Grand Concourse still 0.1734 and 0.0509. So the chroma gap those sheets carry -- 0.357, 0.421, 0.376, and 0.254 on Forest Hills Gardens where the brightness matches to eight per cent -- was never the capped textures. **What it is, precisely: `shellmat.shaded` varies a building's tone and not its hue.** It scales all three channels by `1 + tone_amp * k`, `tone_amp` running 0.06 to 0.14 across the twenty shell materials, so a Bed-Stuy row comes out as one hue at four brightnesses where the photograph has terracotta, cream, plum and rust in four adjacent houses. **And there is no source to fix it from**: of 1,083,026 buildings, **1,717 take a material from an OSM `building:material` tag, 412 from `building:colour`, 33,689 from an LPC designation report and 1,047,208 -- 96.69 % -- from the class rule.** The whole OSM extract holds 2,381 colour tags for the city. A per-building hue would therefore be invented for nineteen buildings in twenty, and this entry records that rather than inventing it. |
| J67 | **Every daylight frame in the project is lit like an overcast day, and the same function that lights it already says so.** `setup_world_and_sun` computes an exposure from `key = dni * sin(elevation) + DIFFUSE_KEY_W` with `DIFFUSE_KEY_W = 90` W/m2 -- so the model states that diffuse light is **90 W/m2 against roughly 880 direct** at a high Sun, near **10 : 1**. Two lines earlier it sets the sky to a bare constant, `SKY_STRENGTH_DAY = 0.25`, carrying no calibration at all. Measured on a 0.18-albedo lambertian plane, sky only and Sun only, no bounces, Standard transform, four stops down so nothing clips: the renderer configures a direct-to-diffuse ratio of **0.63 : 1** at a 69.5 deg Sun -- the sky delivering **1.6 times** the Sun -- and **0.04 : 1** at 5 deg. The Sun path checks out against the same method to four decimals (`irradiance * albedo / pi` = 0.0932 measured 0.0931), which is what makes the sky reading trustworthy. | It is the flatness, not the darkness, that gives it away, and the shape of the histogram says it rather than the mean. Across the first four drive-throughs the renders' **5th percentile is as dark as the photographs' or darker** (0.000, 0.018, 0.052, 0.042 against 0.052, 0.144, 0.058, 0.048) and their **95th percentile is half** (0.497, 0.500, 0.324, 0.454 against 0.875, 0.918, 0.997, 0.964). Nothing in these frames is *lit*. Bed-Stuy is the extreme: standard deviation **0.095** against **0.299**, an almost tone-free image. A sky delivering more light than the Sun, from every direction at once, is exactly what produces that -- and it is why the J66 albedo correction, which is right on its own terms, moved Arthur Avenue only from 0.58x to 0.60x: the facades whose albedo it fixed are not in sunlight in the first place. **The bounce count was ruled out by measurement rather than by argument**: at 2, 3, 4, 6 and 10 diffuse bounces the frame mean is 0.2441, 0.2449, 0.2451, 0.2451, 0.2451 -- converged at two, because an even sky leaves inter-reflection nothing to add. | Done, and **not fitted to a photograph**: the sky is set to deliver the `DIFFUSE_KEY_W` the exposure model on the next line already assumes. What a strength of 1.0 actually delivers was measured across eight Sun elevations (`SKY_IRRADIANCE_AT_UNIT_STRENGTH`) and the strength is interpolated from it, so the constant is replaced by the number the rest of the function asks for. The resulting ratios are **9.76 : 1 at 69.5 deg**, 6.81 at 45, 4.28 at 30, 2.48 at 20 and 0.23 at 5 -- the elevation dependence falling out of the air-mass model rather than being fitted, since the direct beam attenuates with air mass while the diffuse term does not. **The bounce count was then re-measured under the corrected sky**, because the first convergence could have been an artefact of a sky that did all the work: at 2, 3, 4 and 6 diffuse bounces the frame mean is **0.1271, 0.1280, 0.1283, 0.1284** -- one per cent from two to six, converged again. It stays at 2, and that is a measurement rather than a budget decision. **The Sun constant had been absorbing the error and had to be solved again.** `SUN_CALIBRATION` read **540**, and the docstring states what it is for: *"a 0.26-albedo sunlit ground at about 150/255 through the Filmic view transform"*. That target has not changed; what changed is that it had been solved with a sky delivering 1.6x the Sun. Measured on the same 0.26 plane with Sun and sky exactly as the renderer now configures them: **540 -> 118/255**, 300 -> 147, 200 -> 167, 140 -> 183, 100 -> 197, 70 -> 211, 50 -> 221, crossing the stated 150/255 at **286**. The sky strength is derived from this constant, so the re-solve moves the level and leaves the direct-to-diffuse ratio alone. **What the first corrected frame looks like is the evidence that matters**: Arthur Avenue now has hard-edged dappled tree shade across the roadway and the footway, sunlit patches on the far facades, and legible awnings, shopfronts and storey lines -- none of which existed in any earlier render of it. **The residual gap to the photograph is not a fault to tune away**: a camera's automatic exposure lifts a shaded street, and this renderer deliberately never stops down (`exposure = min(3, max(0, log2(681 / key)))`), so a frame of a tree tunnel will read darker than a photograph of the same tunnel. That is a stated difference between a physically lit render and a camera's rendering of a scene, and it is left in place. **Night is untouched**: `SKY_STRENGTH_NIGHT` and the A/B-calibrated `NIGHT_EXPOSURE_STOPS` are both unchanged, and no Sun lamp exists below the horizon. **Amended by J83:** the decision that the renderer never stops down is withdrawn; the physical Sun and sky stay as calibrated here, the development is metered, and the stops it takes are published on every sheet. |
| J68 | **A spire's height became sixteen storeys of windows on a parish church, and the same defect put 91,217 window openings on elevations that are not storey stacks.** `buildings_base.floors` is a real PLUTO count where PLUTO has one and, for the rest, the derivation `height / class storey height`, published honestly as `floors_source = SRC_HEIGHT_TO_FLOORS`. Old First Reformed Church, Seventh Avenue and Carroll Street in Park Slope (BIN 3020373, OSM way 248159667, matched at IoU 0.916), carries a LiDAR `heightroof` of **63.07 m** -- the top of its steeple. Divided by the class storey height that gave **16 floors**, and `window_grid` then drew **15 rows x 14 columns = 210 window bays**, `water_towers` hung a **wooden 10,000-gallon gravity tank** on the roof and `rooftop_unit_count` added an **elevator machine room and a stair bulkhead**. 1,058 of the 1,061 arched-tenement windows in the `drive_brooklyn_park_slope_7th_ave` frame stand on that one church, 128.5 m from the camera and spanning the middle of the view. City-wide the same arithmetic put **147,856 window openings** on the five facade classes whose elevation is not a stack of storeys, the worst single case being **97 rows x 31 columns = 3,007 openings** on a building classed `subway_elevated_station_steel_1915`. | The classifier was **right**: it assigned `facade_class 36`, *"Romanesque Revival brick church, round arches"*, whose own record in `facade_classes.json` says `floors_typical: [1, 2]` and `roof: pitched_steep`. Nothing downstream reads that field. `window_grid` takes `rows = floors - 1` and `water_towers`/`rooftop_unit_count` take thresholds on `floors`, so all four derivations consume a storey count that, for these classes, counts storeys that do not exist. **The massing is a separate and unfixed thing**: the shell extrudes the whole 1,190.6 m2 footprint to 63.07 m. The project holds NYC's LoD2 CityGML for 1,033,416 buildings and it says the bulk of this plan stops far lower -- two roof levels, **1,157.3 m2 at 55.99 m and 33.3 m2 at 68.43 m** -- and the disagreement is already published per building as `roof_attrs.dz_vs_footprint_m = -23.59`. `tiledata._steps_for` refuses to step the mass on a disagreement that large (`STEP_DZ_MAX_M = 15`), which is correct: shifting the CityGML levels up by 23.6 m would put the nave roof at 79.6 m. So the two sources genuinely disagree and the pipeline declines to invent a reading. | **Half done.** `derive.elevation_storeys` now bounds the storey count an *elevation* is drawn from by the class's own published `floors_typical` maximum, for the five classes named in `NOT_A_STOREY_STACK` -- 35 `church_stone_gothic_1880`, 36 `church_brick_romanesque_1890`, 47 `big_box_retail_precast_1990`, 48 `gas_station_canopy` and 55 `subway_elevated_station_steel_1915`. **This is a rule, not a measurement**: nothing in the sources says how many tiers of glazing a particular church carries, only that its elevation is not a storey stack. Every other class keeps its derived count untouched, because a derived third storey on a Staten Island house is a plausible reading of a real LiDAR height and clamping it would replace one derivation with a worse one. Measured: **3,149 buildings change**, **91,217 window openings** come off (147,856 -> 56,639 on the five classes; 8,830,957 -> 8,739,740 city-wide), **247 gravity tanks** come off churches, and the church in the Park Slope frame goes from 210 bays to 14. What is **not** fixed is the massing: **3,595 buildings** have a measured top roof level covering under a quarter of their plan and standing more than 5 m above the level below (404 of them more than 10 m), and every one of those is still extruded to a single height. That needs the CityGML level *polygons*, which are on disk in `tri_xyz` and not yet read for massing. Guarded by `tests/test_world_integration.py::test_no_elevation_carries_more_tiers_of_windows_than_its_own_typology_allows` and two unit tests in `pipeline/tests/test_facade.py`. |
| J69 | **A verification camera can stand in open air, with 150 m of clear street ahead of it, and render a frame with no light in it -- and nothing in the placement rule looks up.** `drive_midtown_sixth_ave_45th` stands on Sixth Avenue at 45th Street, on the photograph's own EXIF GPS, in a roadbed polygon, with the view azimuth clear for 150 m and the nearest built thing 6.0 m away. It rendered at **mean 0.033**, and after the forced clearance correction at **0.037** -- both below the `FRAME_MEAN_MIN = 0.06` the renderer requires of evidence, so no sheet was published for the Midtown drive-through at all. | Read at six times its own brightness the frame is legible and the cause is plain: a pin oak crown fills the upper half and the right of the picture from 8.2 m away, a body stands 3.5 m from the lens, and the avenue beyond is in the shadow of its own east wall at 09:30 -- a 200 m tower casts 211 m at a 43.5 deg Sun. Every one of those is a real thing in a real place; the frame is physically consistent and useless as a comparison. The rejection is correct behaviour and the record says so. | **Measured, half fixed, and the remainder left standing.** `camera.sky_fraction` now casts 17 rays in a 35 deg cone about vertical, counting foliage (which `first_solid_above` deliberately steps past) and stepping past simulated agents (J49), and the *retry* -- the path taken only when the luminance gate has already rejected a frame -- requires 0.30 of that cone and ranks its candidates on it. Confined to the retry on purpose: Arthur Avenue's tree tunnel is what that street looks like and a camera must not be walked out from under it to make a brighter picture. **It did not rescue this sheet**, and the number it published is why: `sky_fraction 0.412` at the corrected eye point, so the sky over the camera was never the problem -- the canopy is over the *street it is looking at*. What the reading buys is that the record now carries a measurement where it carried an assumption. The Midtown drive-through therefore has **no usable comparison frame**, `test_the_mandated_viewpoints_and_drive_areas_all_have_a_usable_comparison` fails on it, and that failure is left in place rather than dissolved by widening the gate: the remaining lever is the assumed instant (the three photographs carry a year and nothing finer, so 21 June 09:30 is a project-wide constant), and moving a clock until a frame brightens is fitting the evidence to the render. |
| J70 | **2,248 km of surplus canopy over the city: 651,023 street trees are drawn at whatever height the kit exported, not at the height the census measured them.** The 2015 street tree census records a trunk diameter and `furniture/allometry.py` turns it into a height by a published per-species curve -- a real measurement through a real model. That height then chose one of three exported sizes on bin edges of **7 m and 12 m** and was discarded, and `scene.add_props` places a prop *by translation and yaw alone*, as its own audit docstring states. But the kit's three sizes do not stand at 7 and 12 m: a pin oak's are **11.8, 18.3 and 22.6 m** and a plane tree's are **20.7, 24.0 and 27.7 m**. A tree measured at 8 m -- between the edges -- was drawn with the *medium* asset and stood **18.3 m** over the pavement, or **24.0 m** if it was a plane. Measured over every tree that carries both a species and a height: median drawn height **1.26x** the measured one, **185,837 (28.5 %)** at 1.5x or more, **62,997 (9.7 %)** at twice or more, 90,971 (14.0 %) drawn shorter than measured, and the summed absolute error **2,248 km**. | The bin edges' own comment claimed they were *"the bin edges the props kit exported its three sizes against"*. They never were: they are plausible small/medium/large thresholds for a street tree and the kit's assets are mature specimens. This is the shape this project has now recorded a dozen times -- **a correct measurement standing for something other than the thing it measures** -- and it is the most consequential instance of it, because the canopy is the largest single influence on the light in every street frame the project renders. Arthur Avenue's assessment named 198 trees closing over the carriageway and asked whether the real allee is that dense; it is not, and this is why. | **Done, using the number that was already in the row.** `PropAssets.tree_asset` reads the exported heights out of the props catalogue's own bounds, picks the size **nearest the measured height** (so the scale stays as near 1 as the kit allows -- median **0.953**), and returns the uniform scale that puts the tree at the height the census measured. `resolve_scaled` carries it for every prop kind, 1.0 for all 123 non-tree assets, and the three things that draw a prop all go through it: the verification renderer multiplies it into the instance matrix, the Unreal manifest writes it per row as `s`, and `build_levels.place_props` applies it to the spawned transform -- so the engine cannot keep a canopy the renders no longer have. The scale band **0.30 to 2.20** is set from the requirement rather than from taste: the scales the real rows ask for run 0.156 to 2.765, and the band covers all but **2,922 rows (0.45 %)**, which keep the asset's own size, say so in their reason (`:scale_out_of_band`) and are counted per frame as `tree_scale_out_of_band`. Every sheet now publishes `tree_instances_scaled` and `tree_mean_scale`. Guarded by `tests/test_world_integration.py::test_the_citys_canopy_stands_at_the_height_the_tree_census_measured` and two unit tests in `pipeline/tests/test_furniture.py`, one of which asserts that **no non-tree prop is ever scaled** -- a hydrant's size is a measurement in its own glb and `audit_prop_assets` depends on nothing scaling it. |
| J71 | **A sheet's reference can be a photograph of a different kind of place, and nothing tests for it.** `drive_queens_bayside` is named *Queens residential block: Bayside*, its fetch recorded its own queries as *"Bayside Queens houses residential street"* and *"Bayside, Queens" house*, and the three photographs it kept are **Interstate 295 seen from a pedestrian overpass**, a Flickr picture titled *"Needs Work"*, and *Lawrence Cemetery*. The sheet shows the Interstate. Screening every item's kept photographs against the words of its own recorded queries finds **6 of 172** that kept nothing answering any of them, and the six are worth reading individually because two are the screen's own failure and four are real: `landmark_queens_midtown_tunnel_portal` asked for a tunnel portal and kept **three photographs of Saint Vartan Park**; `vehicle_nypd_car` asked for an NYPD Ford Interceptor and kept *"Empty Road on a Snowy Day"* and *"Brazilian Day 2018"*; `landmark_bank_of_america_tower` kept *"Ocean blue mirror dimension"* and a view taken **from** the Empire State Building; `street_newsstand` kept two street corners. `fifth_ave_42nd_north` and `landmark_53w53` are false positives -- *43rd St 5th Av* is Fifth Avenue and *53W53* is the subject -- the screen simply tokenises their digits away. **6 is a lower bound and not a defect count**: Bayside itself does not appear in it, because the Interstate's title shares the word *Queens* with the query that asked for houses. | Two filters run and neither asks the question. `fetch_photos` screens on **keywords and geosearch radius**, so a photograph taken near Bayside with *Queens* in its title passes a query for Bayside houses. `pick_reference_photo` then ranks the survivors on lighting, whether a real EXIF timestamp exists, the Sun's height, the azimuth error and -- since J60 -- a graded distance band. **Every one of those is a property of the photograph as a photograph**; not one of them is a test of what it is a picture of. J60 fixed the case where the *right kind* of photograph was chosen from the wrong block. This is the case where the kind itself is wrong, and the distance band cannot catch it: Bayside's Interstate is correctly refused as a camera *position* at 566.6 m and still shown as the sheet's reference. | **Open.** Recorded rather than fixed, because every repair I can see either invents evidence or throws real evidence away. Rejecting a photograph whose title fails its item's queries would discard *43rd St 5th Av* from Fifth Avenue and *53W53* from 53W53 -- the screen is not accurate enough to gate on. Re-fetching with tighter queries needs network access this container's proxy policy does not grant for Commons bulk search, and would change which photographs 172 sheets are compared against, which is a decision about the evidence base rather than a bug fix. What is done instead is that the affected sheets **say so in their own assessments**, in the verdict rather than in a footnote, so no reader takes the pairing for a comparison: Bayside's opens *"the item asked for a residential street and its reference is a six-lane Interstate"*. The four named items are the ones to re-fetch when the catalogue is next rebuilt. |
| J72 | **A sheet said its subject was not visible over a frame with the subject in the middle of it, because the height it aimed at was invented.** `dumbo_washington_st_manhattan_bridge` names the Manhattan Bridge's Brooklyn tower, 106.68 m of steel, and its record read `subject_visible: false`, `0` of 5 rays on the subject, the line closed at **4.6 m** by `prop_lamp_cobra_davit_1` -- a street lamp 1.2 m from the lens. The tower is plainly in the picture, framed between the warehouses exactly as the photograph frames it. `subject_top` looked for a landmark model within **120 m of the subject's coordinate**; the Manhattan Bridge model's origin is 281 m away, down the span, so nothing was found and the function fell back to *"a nominal 10 m subject"*. The rays were then cast at **4.8 m above the water** and met the lamp. **Every one of the four rendered sheets that tests a sightline reported its subject invisible**, and the other three fail the same way from the other end: `landmark_911_memorial_pools` aims **164.6 m** up, because the nearest model within 120 m of the sunken pools is One World Trade Center. **38 of the 172 sheets** take the nominal height. | The invented number does not stop at the sightline. `subject_top` feeds three things: `choose_lens`, which widens the focal length until the subject's top is in frame; `containment_pitch` and `aim_pitch`, which tilt the axis at the subject's mid-height; and the sightline probe. So a 106.68 m tower was framed, aimed and tested as a 9.6 m object. Matching the subject to a model by *name* instead was tried and measured and is worse: the 93-entry catalogue holds district and site models, so "Central Park Tower" matches `b_central_park_walls_gates`, a **3 m** park wall **2,368 m** away, and "Queens Museum" matches `c_brooklyn_museum` in another borough. A confident wrong height is worse than none. | **Done, by deleting the fallback rather than replacing it.** `subject_top` returns `None` for the height where no model stands within 120 m of the subject, and each of the three consumers now says so instead of working from a number nobody measured: the lens is not widened (*"...so the lens was not widened to contain it"*), the axis stays level (*"no landmark model stands within 120 m of X, so its mid-height is not known and there is nothing to tilt towards"*), and the sightline reports **`subject_visible: null`** with the reason rather than `false`. `false` is a claim that the subject is hidden; `null` with a reason is the truth. The image changes on the affected sheets only where the invented height had tilted the axis -- up to 3.9 deg, and to level. What this does **not** do is make those 38 sheets show their subjects: that needs models the catalogue does not have, and the sheets now say which. |
| J73 | **Three city surfaces are drawn from a 2:1 scan tiled as if it were square, so their content is twice as tall as the material it stands for.** `uv_scale_m` is a single scalar applied to both axes of the mapping node. AmbientCG ships some tiles at 2048x1024, and three of this catalogue's materials use one: `stone_rubble` (Bricks102), `precast` (Concrete034) and **`vinyl_siding` (WoodSiding009), the third most common surface in the city at 1,087 tiles**. Every one has been stretched 2x vertically since J63 gave the city its textures. On vinyl siding that is the most visible thing there is -- the lap spacing -- drawn at twice its width. | Found while measuring replacement scans for J66, not by looking for it: the best-measuring painted siding on the provider turned out to be 2:1, which raised the question of what the pipeline does with such a tile, which is when the three already in the catalogue turned up. `nycsim_bpy.pbr_material` is the shared builder for every textured material in the project, and its docstring is exact about what `uv_scale_m` means -- *"the physical size (m) one texture tile covers"* -- and says nothing about the tile being square, because until a non-square one arrived nothing had to. | Done. The builder reads the colour map's own pixel dimensions and scales v by the aspect, so a tile covers `uv_scale_m` across and `uv_scale_m * h / w` up. Every sheet now publishes `tile_aspect` per dressed surface and `tests/test_comparison.py::test_a_scan_that_is_not_square_is_not_tiled_as_if_it_were` checks the published value against the file on disk, so the shape and the mapping cannot part company again. The kit baker (`kitlib._kit_maps`) has the same fault by a different route -- it resizes every source to a square `KIT_TEX_RES` -- but no kit material resolves to a non-square scan today, so it is recorded here and not changed. |

| J74 | **The height a sheet aims and frames its subject by was the height of the nearest landmark model's *origin*, and an origin is a model's own centre.** For anything long or wide that is nowhere near the part of it a photograph is of: the Manhattan Bridge's Brooklyn tower is **281 m** from `b_manhattan_bridge`'s origin and **0 m** from its steel, so the 120 m radius found nothing and J72 -- correctly, on the rule it had -- reported the height unknown, left the axis level and never widened the lens, over a frame with 106.68 m of tower in the middle of it. Measured over the whole reference set: **138 items name a subject, 100 have a landmark origin within 120 m of it, and 38 do not**. Every one of those 38 stands inside some landmark model's *bounding box*, and for at least 19 of them a model's actual mesh is within 60 m -- a lower bound, because the catalogue records a `lod0` path for only 34 of its 93 entries and the other 59 could not be measured this way. They include both bridge-tower sheets, all four Hudson Yards towers, five Times Square subjects, four tunnel portals, both Governors Island forts, 3 and 4 World Trade Center, Central Park Tower and 432 Park Avenue. | The same shape as J57, J60, J68, J70 and J72: **a correct measurement of something other than the thing it stands for.** The distance to a model's origin is a real number, and it is not the distance to the model. Two cheaper repairs were tried on paper and both are worse than the fault. Matching by *name* gives "Central Park Tower" the 3 m `b_central_park_walls_gates` 2,368 m away and "Queens Museum" `c_brooklyn_museum` in another borough. Matching by *bounding box* is worse still, because the boxes of long models overlap everything near them: `b_brooklyn_bridge`'s box contains the **Manhattan** Bridge's tower, and `c_times_square`'s 365.8 m box contains the TKTS booth -- a 5 m kiosk that would be framed as a skyscraper. | Done. `camera.subject_height_probe` **measures** instead of matching: rays fall from 1,000 m onto a ring of 17 points about the subject's own coordinate (0, 3 and 6 m, 8 spokes) and the highest **built** surface any of them finds is the height. Foliage is stepped past; props, agents, terrain and pavement are never counted, because a photograph's subject is built fabric. The record publishes the measurement and not only its conclusion -- `subject.height_probe` names the object hit, how many of the 17 rays landed on built fabric, the ground under it, and what the old origin rule would have said -- so a reader can see when the coordinate is on the wrong thing. Where nothing built stands there the height is still `None` and the aim, the lens and the sightline all say so, exactly as J72 left them. **Amendment, measured over the first nine sheets of the v15 pass that name a subject.** Eight of the nine measure a height with **16 or 17 of 17 rays** landing on built fabric, and the ninth is the only subject in the set smaller than the probe's own sampling: **Charging Bull**, 4.9 m long, whose recorded coordinate is **4.0 m** from the model's origin and **3.2 m** from its nearest vertex. `SUBJECT_PROBE_RINGS = (0, 3, 6)` samples three rings and not a disc, so the r=0 ray passes 3.2 m clear of the sculpture, the r=3 ring just misses it and the r=6 ring overshoots onto its base plate: **4 of 17 rays land, all on the base**, and the reported top is 0.6 m below the street-mode ground sampled 15 m around it. The record's sentence -- *the highest built thing at the subject's coordinate is `lm_charging_bull`, -0.6 m above the ground there* -- is true of the rays it cast and false as a description of the sculpture, **which stands correctly in the frame**. The bull is not buried: its mesh spans 0 to 3.4 m local and is placed at its origin's 5.486 m, and the render shows it on the plaza with pedestrians around it. This is the probe's sampling geometry, not the world. **Fixed after the pass:** the rings are now every 1.5 m (`SUBJECT_PROBE_RINGS = (0, 1.5, 3, 4.5, 6)`) with 1, 6, 8, 12 and 16 spokes -- 43 rays, no two neighbours more than about 2.4 m apart anywhere inside the disc -- so a 4.9 m sculpture cannot fall between them; the sheet is re-rendered under it. It is recorded here rather than left for a reader to trip over on the one sheet in the set where a landmark's own height reads as negative. |

| J75 | **Three reference items place their subject off the structure it names, and the camera azimuth derived from it is 4-13 deg off with it.** Measured against the city's own road network -- a bridge tower stands on the bridge -- the Manhattan Bridge's Brooklyn tower stood **29.7 m** from the Manhattan Bridge's carriageway, the Williamsburg's **24.2 m**, the Throgs Neck's **47.5 m**, against **0.9 to 6.5 m** for the six bridge subjects that were right. The Manhattan Bridge case is the sharpest: its coordinate is 62 m from the tower, 29.6 m off the bridge, and bears **357.1 deg** from the photographer's own GPS where Washington Street -- the street the photograph is taken along, with the tower centred between its warehouses -- bears **2.6 deg**. It affects two sheets, one of them `dumbo_washington_st_manhattan_bridge`, a viewpoint the brief names. | **Nothing caught it because every check agreed with itself.** The item's recorded `azimuth_deg` pointed at its own subject coordinate to within **0.1 deg** on all three, so the aim audit, the frame-cone audit and the sightline probe were all comparing a wrong coordinate with an azimuth derived from that same wrong coordinate. J57's subject-position test does catch this shape, and deliberately only where an OSM feature's name matches exactly -- 43 of the 138 subjects -- and "Manhattan Bridge Brooklyn tower" is not an OSM name. **It surfaced because J74's height probe reported `rays_on_built_fabric: 0`**: nothing built stands within 6 m of the coordinate, which for a 107 m steel tower is a statement about the coordinate. | Done. The three subjects are moved onto their own towers -- each now **0.8 to 1.9 m** from its carriageway -- and the four azimuths derived from them are recomputed (345.8 -> 352.8 deg, 74.0 -> 60.7, 199.4 -> 208.0, 25.0 -> 21.0). Each `meta.json` keeps `lat_was`/`lon_was` and `azimuth_deg_was` beside the correction and states its evidence, and `pipeline/tests/test_reference_subject_positions.py::test_a_subject_that_names_a_bridge_stands_on_that_bridges_own_carriageway` holds all ten road-carrying bridge subjects to 15 m of their own deck -- a threshold in the gap between the six that were right and the three that were not, chosen and not derived. Hell Gate (rail) and the High Bridge (an aqueduct) carry no roadway and are explicitly not claimed. **A fourth is corrected and six more are measured and left alone.** The Soldiers' and Sailors' Memorial Arch stood **90.1 m** north of the model built on its own OSM footprint (way 20679503), corroborated by OSM's own Grand Army Plaza station at the model's latitude and not the old subject's; its coordinate is moved and its azimuth left, because 170.0 deg already agrees with the corrected bearing to 1.8 deg. The six left as they are, with their numbers rather than a guess: the **RFK Bridge East River suspension span 152.0 m** from any of that model's geometry (and its recorded azimuth 19.7 deg off the span, which slipped under `view_azimuth`'s 20 deg discard by 0.3 deg), St. George Ferry Terminal 75.5 m, Fort Jay 55.1 m, the Verrazzano-Narrows 29.6 m, the Central Park perimeter wall 29.2 m, the Hugh Carey portal 25.0 m and the High Bridge 20.8 m. **None of these is corrected**, and the reason is the rule this project runs on: the only anchor available for them is the model itself, and moving a reference onto a model is validating the render against itself. Each now publishes `nearest_built` on its sheet -- the object, the distance and the bearing -- so a reader sees the gap rather than a silent `null`. |

| J76 | **A 107 m bridge tower was reported not visible because a street lamp 5.6 m from the lens covered the 0.30 m the sightline fan spans there.** The fan that answers "is the subject in the picture" is a width **at the subject** -- and that width was a flat **12 m** for everything in the city, from the Charging Bull to One World Trade Center. At the Manhattan Bridge's Brooklyn tower, 226 m away, 12 m is **1.52 deg**; 1.52 deg is 0.30 m at the 5.6 m where `prop_lamp_cobra_davit_1` stands, so the lamp took **all five rays** and the record said `subject_visible: false` over a frame with the tower in the middle of it. | The docstring already argued its way from one ray to a fan and from a fixed angle to a fixed width, each time for the right reason, and stopped one step short: a width is only the right shape of rule if it is the *subject's* width. Until J74 there was no measurement to use -- the item's metadata does not carry a subject's size, and the entry says so in as many words: *"Nothing in the item metadata gives the subject's size, so there is no measurement to take here."* That sentence was true when it was written and stopped being true the moment the height probe existed. | Done. `subject_sightline` takes the height J74 measures off the geometry standing at the subject's coordinate and spans the fan across it, keeping the 12 m floor for small subjects and the existing 15 deg cap. At DUMBO that is 13.4 deg instead of 1.52, and the rays clear the lamp. Every sheet publishes `subject_fan_from`, which says whether the fan was sized to a measurement or to the default because none was taken, and `tests/test_comparison.py::test_every_sightline_says_what_its_fan_was_sized_from` refuses a published `subject_visible` that does not say. |

| J77 | **The statistics an assessment quotes were measured off a previous image, and the checker that exists to catch unsourced figures was sourcing them from it.** `frame_stats.json` carries the two comparisons every assessment reaches for first -- how much darker the render is than its photograph, how much less colour it carries -- and it was written **only when somebody ran `tools/frame_stats.py` by hand**. Nothing in the render pass wrote it. So a re-render replaced `render.png` and left the statistics of the old one in place: after the v15 pass re-rendered them, Arthur Avenue's file still measured the render of **20:00:53** beside a render from **21:39:02**, Bethesda's the render of **18:05:53** beside one from 21:34:54. | The same shape as J57, J60, J68, J70, J72 and J74, and the sharpest instance of it so far, because this one is in the **verification apparatus**: `tools/assessment_check.py` reads `frame_stats.json` as one of the artefacts a quoted figure must exist in, so a figure describing a picture that no longer exists passed the check that exists to stop exactly that. The staleness was detectable the whole time and nothing looked: the file copies `rendered_at` from the render it measured, and the render beside it carries its own. | Done. `render_sheets.write_frame_stats` measures both halves of the sheet in the same call that writes it, so one render and one measurement are the same event; every sheet from here on logs its own `render mean` and `reference mean` as it is written. `assessment_check.py` gains `stale_frame_stats`, which compares the two `rendered_at` stamps, **drops the file from the haystack** when they differ so its numbers cannot silently source a quotation, and reports the slug. `tests/test_comparison.py::test_no_sheets_statistics_describe_a_previous_image` fails on any sheet whose statistics and render disagree. |

| J63b | **The engine half of J63 is built: 23 material instances, 60 textures and a binding for every material name the 2,840 exported tiles carry.** J63 left this as *"named here, sized, and not pretended to be done"* -- Unreal resolved nothing, so a player saw nineteen solid RGB values where the sheets show brick, limestone and asphalt. It is now a contract the pipeline computes and the editor consumes: `pipeline/nycsim_pipeline/unreal/city_surfaces.py` reads **every** material name out of **every** tile (53 distinct names -- 19 `NYCSIM_*`, 25 `pave_*`/`struct_*`/`park_*`, and the rest), resolves 21 surfaces of which **20 are textured and one, `glass_curtain`, stays analytic on purpose**, and publishes them in `unreal_manifest.json` with the tile size, the 2:1 aspect where the scan has one, and the albedo scale that takes the scan to the level its own stage authored. **15,331 tile material slots bind; 3,208 do not and are shipped as refusals with their reasons.** | The disk objection J63 raised still stands and this is the shape that answers it: J50's sidecar rule would put 250 MB beside each of 1,554 tile files -- 388 GB -- so the textures ship **once**, to `/Game/NYCSim/Textures/City`, and every instance points at the same assets. The correctness proof is that the pipeline computes the numbers with the same arithmetic the renderer publishes on every sheet: cross-checked over **313 dressed-surface records**, every `asset_id`, `physical_size_m`, `tile_aspect` and albedo figure agrees, and the only five disagreements are records rendered **before** J66's texture swap -- the newest disagreeing record is 19:21:26 and the oldest agreeing one 20:47:39, which is the swap itself. | Done, with one thing found in the building that would have shipped as a fault. **An instance is keyed on the (surface, *surface class*) pair, not on the surface alone.** Five pavement names resolve to `concrete_sidewalk` and they do not share a class: a curb ramp and a sidewalk are class 9, a median, a plaza and a curb are class 2. One instance per surface carries one `phys_material`, so it would have given one of those groups the other's tyre friction -- every curb ramp in the city gripping like a median, in a build whose whole point is that you drive on it. So `concrete_sidewalk` splits into `MI_NYC_concrete_sidewalk_sc2` (1,274 slots) and `_sc9` (994). **What cannot be closed here: none of this has been run in Unreal.** There is no engine in this container, so what is verified is the contract, the arithmetic and the Python -- 41 translation units compile, the five new tests pass, and the manifest regenerates -- and what is not verified is the editor's own behaviour when it consumes them. That is stated rather than implied. |

| J78 | **The sightline probe reports `subject_visible: false` for a subject that fills the upper middle of the frame, and J76 is why.** On `dumbo_washington_st_manhattan_bridge` -- a viewpoint the brief names -- the Manhattan Bridge's Brooklyn tower stands centred between the warehouse blocks, and the record says **0 of 5** rays reach it, nearest blocker `prop_lamp_cobra_davit_1` at **3.8 m**. Every number in that is correct. J76 sized the fan to the subject's own measured height, which here is **107.4 m across, a half-angle of 13.35 deg**; at the subject's 226 m that fan spans **+-53 m**, and Washington Street is about **twenty metres** wide. The two lateral rays meet the warehouse walls by construction, the upper ray meets a parapet, and a lamp standard three metres from the lens takes another. | The third fault this one sheet has found in the same probe, and each was real: before J72 it aimed at an invented 10 m subject height; before J74 it took the height from a model's **origin** 226 m away instead of from the steel; now it is sized correctly and to the wrong *shape* of question. **The fan was never the measurement -- the fraction was.** "Is the subject visible" answered by a majority of five rays is a claim about the rays; a subject seen down a canyon, through a gap, or past a lamp is in the picture and the boolean says it is not. Widening the fan made the canyon worse, and narrowing it back would reinstate the fault J76 fixed, so neither direction is the answer: the *threshold* is. | **Done, after the pass, in three parts, each with a synthetic test in `tests/test_comparison.py`.** (1) A hit nearer than `span - reach_m` on the **same fabric** as the object the height was measured off (`_same_fabric`: the model for `lm_*`, the whole name for a tile mesh), with that fabric **continuous in plan** from the hit to the coordinate (`_fabric_continuous`: rays dropped every 5 m, each landing on the same fabric at least 2 m above the subject's ground), is a ray that has found the subject; the plaza between the Bethesda arcade wall and the fountain, or the street between two buildings of one composite, breaks the run and the hit stays a blocker. (2) The fan is the subject's measured **width** across -- the plan extent of the probed object's bounding box, for a landmark model; a tile mesh's box is the tile and is declared unusable -- and its measured **height** up, each clipped to the camera's own frame, and the record says what each dimension was sized from. (3) `subject_visible` is **"at least one ray lands on the subject"**, over 13 rays instead of 5, and the record publishes `subject_visible_fraction`, `subject_clear_fraction`, the own-fabric hit count and the nearest own-fabric object beside it. Measured over the finished pass before the fix: 93 of 138 subject sheets reported `false`, 20 of them with a ray already on the subject. Every subject sheet is re-rendered under the new probe. **Superseded text follows for the record:** Open, and deliberately not fixed during this render pass. The pass was already eleven sheets in when this was found and a third restart would cost more than the fault does: this is a record that misstates a frame, not a frame that misstates the city, and every sheet publishes `subject_rays`, `subject_rays_clear`, `subject_rays_on_subject`, `subject_fan_m` and `subject_fan_from` beside the boolean, so the counts a reader needs are already there. **What the fix is, when the pass is done**: report the fraction rather than a majority verdict -- a subject one ray lands on is in the picture -- and cast the fan within the subject's own angular extent *and* the frame's, so a canyon cannot spend rays on its own walls. The affected sheets are then re-rendered. The DUMBO assessment states the fault on the sheet itself rather than leaving a reader to trust the boolean. **Amendment, the complete breakdown over eleven blocked sightlines, and it is worse than the entry above says.** Not one of the eleven is the plain statement `subject_visible: false` claims to make, and they fall into three kinds. **Three are blocked by the subject's own model** -- `lm_flatiron.2` closing the line to the Flatiron, `lm_equitable_building.8` to the Equitable, `lm_b_bethesda_terrace.20` to Bethesda. The cause is arithmetic: the probe calls a hit closer than `span - reach_m` a blocker, `span` is the distance to the subject's **recorded coordinate** -- its centroid -- and a large building's near face is legitimately much closer. The Flatiron's centroid is 151.0 m away and its prow is at **111.3 m**, forty metres nearer, so the ray that lands squarely on the building is counted as stopped by something. **A ray that lands on the subject's own model has found the subject.** **Five are blocked by a prop** -- a lamp standard or a street tree taking rays from a fan wider than the street (DUMBO, Barclays, the Brooklyn Bridge from DUMBO, City Hall, Federal Hall), which is this entry's own fault. **Three are blocked by another building** and are the only ones where the boolean is right: 40 Wall Street, the Chrysler and the Empire State Building, which is J79. So the repair is three things and not a threshold: count a hit on the subject's own model as finding it, size the fan to the subject *and* the frame, and report the fraction. All three land together after the pass. |

| J79 | **The camera walk optimises a clearance the frame does not need and never checks the one it does, so a camera aimed at the Chrysler Building renders a wall.** The placement rule moves a boxed-in viewpoint onto the nearest real pavement and accepts it when the **view azimuth is clear at eye level**. That is a different question from whether the subject can be seen, and on `landmark_chrysler_building` the two disagree completely: the camera was walked 22.9 m onto a crosswalk, the clearance probe reported the azimuth **clear for 96 m**, and the sightline is closed at **24.0 m** by `t_-3_5_glass_curtain` -- a curtain-wall tower whose face fills the near frame. The subject is 249.8 m away and the axis is tilted **+20.8°** to reach it, so the ray climbs into a building the level probe passes under. **The Chrysler Building is not in the frame.** It is one of the most detailed models in the build -- its catalogue entry states seven Nirosta arch tiers with triangular windows, the 31st-floor hubcap frieze with radiator-cap gargoyles, eight 61st-floor eagles and setbacks at floors 16/24/31/61/71 -- and none of it is on the sheet. | Measured over the nine v15 sheets that name a subject: **seven report a blocked sightline, and six of those had a clearance probe reporting more than twice the blocker's distance as clear.** The blockers split the seven cleanly into two different faults. **A prop blocker is J78** -- a lamp standard or a street tree taking rays from a fan sized to a 100 m subject: DUMBO (a lamp at 3.8 m), Barclays (a lamp at 37.3 m), the Brooklyn Bridge from DUMBO (a honeylocust at 127.6 m), City Hall (a honeylocust at 40.7 m). **A shell or landmark blocker is this entry** -- the camera genuinely looking at a wall: Bethesda (the terrace it stands on, at 14.7 m, already recorded as J65), 40 Wall Street (`t_-6_0_limestone` at 37.5 m) and the Chrysler. Three sheets of nine, and the fault is in the *rule*, not in the three. | **Done, after the pass.** `_move_clear_of_geometry` takes the subject -- coordinate, aim height, measured extent, probed object -- and every candidate it evaluates runs `subject_sightline` from where it would stand; a candidate that cannot see the subject does not pass, and the fallback ranks on how much of the subject it sees before it ranks on clearance. The record carries `scored_on_subject_sightline` and `subject_sightline_at_choice`, so a reader can see what the walk knew when it chose. A viewpoint that is not being moved is still not moved for this: the recorded position is evidence, and a subject a model hides from it is a statement about the model, which the sightline field makes. `test_the_camera_walk_chooses_a_point_that_can_see_the_subject` holds it on a synthetic boxed-in viewpoint. Measured over the finished pass before the fix: 71 of 172 cameras were moved, and 27 of the 93 blocked sightlines were closed by a building shell. **Superseded text follows for the record:** Open. The fix is that the walk has to score candidates on the subject's own sightline and not only on eye-level clearance, which means the sightline probe has to run *inside* `_move_clear_of_geometry` rather than after it -- the same probe, called earlier and used to choose rather than to report. That is a change to camera placement, so it reaches all 172 sheets and belongs after this pass with J78 and the J74 ring fix, not during it. Until then each affected sheet says on its own face that the subject is not in the frame and which object closed the line, which is what the sightline probe was added for and is doing. **The pass then produced the purest case of it, and the frame gate caught it.** `landmark_equitable_building` is **refused**: mean **0.0453** against the 0.06 floor, rendered twice, the clearance correction forced and the camera walked 12.4 m, and no better. Every other input on that item is right -- the heading is the photograph's own, the instant is the photograph's own at 902 W/m2 of direct normal irradiance, and the height probe reads **164.61 m** against the catalogue's published **164.6 m**, the closest agreement between measurement and catalogue anywhere in this pass. What is wrong is only the position: **the sightline is closed at 26.6 m by `lm_equitable_building.8`, a piece of the very building the sheet is a picture of**, from a camera 74 m away with 40.7 m of level clearance. A position 74 m from a 164 m slab passes the eye-level test and cannot produce a picture of the building; the lens was already at the 18 mm floor and the axis tilted the remaining 32 deg, and neither can fix being too close. The same repair fixes the light as well as the framing, because a position from which a 164 m building is visible necessarily has sky in it. |

| J80 | **A quarter of the comparison sheets are lit by an arbitrary constant, and it is the worst constant available for Manhattan's grid.** Where a photograph carries no time the Sun fell back to **09:30 on 21 June**, which puts it at azimuth **95 deg** -- almost due east, and therefore across every north-south street in the city. **42 of the 172 reference items carry a year only** and take that fallback. Measured over the sheets rendered so far, a frame on an assumed instant has a median luminance of **0.1204** against **0.2940** for one on the photograph's own instant: **41 %**. It has now produced outright refusals rather than merely dim frames -- `landmark_federal_hall` at mean **0.021** on Wall Street, the darkest frame in the set, and `drive_midtown_sixth_ave_45th` at 0.037. | The hour was never a measurement and could not be: there is nothing in the file to measure. So the choice was between an *uninformative* arbitrary value and an *informative* one, and 09:30 was chosen for no stated reason at all. A sheet whose street is in its own shadow tests the shadow rather than the city, which is the opposite of what a comparison sheet is for. The objection to choosing a better hour is that it looks like selecting the evidence, and that objection is answered by saying so on the sheet: the record prints the hour, the Sun's bearing, how far off the view azimuth it is, and the words **"chosen, not measured"**, and every assessment of such a sheet states that its luminance comparison is therefore not evidence about the render. | Done. Where the photograph carries a time, nothing changes and nothing is chosen. Where it does not, the **date** is kept as before and the **hour** is picked from those that put the Sun above 20 deg -- below that a street floor is shadow whatever the bearing -- as the one whose solar azimuth comes closest to the item's own **recorded** view azimuth, so the Sun is behind the camera and lights what it looks at. The recorded azimuth and not the final one, because the final azimuth is not known until after the clearance walk and a Sun that moved with the walk would make the instant depend on where the camera happened to stand. Measured on the three cases that drove this: Wall Street looking north goes 09:30 -> **18:30** (100 deg off the view -> 71 deg, which is the best a northward view can do on the solstice, when the Sun never passes north of 58 deg or 302 deg), Fifth Avenue north -> 08:30 (100 -> 56), Arthur Avenue south -> **13:30 (100 -> 14 deg off)**. With no azimuth recorded the highest Sun of the day is used and the record says that instead. **Amendment, measured:** the entry's *42 of 172 items carry a year only* counts each item's first-listed photograph; the chooser prefers a timed photograph, so the sheets actually use a photograph without a time on **19** items (17 year-only, 2 date-only) and only **12** items have no timed photograph at all. And the hour chooser had no night branch: `street_times_square_wet_night`, a night item with a year-only photograph, was given a **chosen 08:30 Sun at 32 deg** with the night sky strength and a Sun lamp both switched on, and came out at 2.05 times its photograph's mean -- the brightest ratio in the set. Fixed: a night item with no time is given 22:00 local, after civil dusk on every date of the year in New York, and the photograph chooser evaluates its day/night gate at the same instant the render is lit at, not at the old 09:30. |
| J81 | **The Barclays Center's oculus canopy was built on the opposite end of the arena from its entrance, and the rule that put it there is a correct measurement of the wrong thing.** The oculus is the single feature the building is known for: an 82 ft cantilever with a 9.1 m LED-ringed opening over the triangular plaza where Atlantic Avenue meets Flatbush Avenue. The generator found that corner by taking **the footprint vertex with the smallest interior angle** -- the rule a wedge deserves. The OTI outline (BIN 3398156) is not a wedge: **27 of its 28 vertices are right angles**, and the twenty-eighth is an **86.6 deg** corner at the **6th Avenue** end. So the canopy was cantilevered there. Measured against the road network's own crossing node `ATLANTIC AVE x FLATBUSH AVE` at NYC_TM (-2321.2, -1769.7): the chosen corner is **275.9 m** from it and **61.2 m** from `6 AVE x ATLANTIC AVE`; the corner the entrance actually faces is **161.8 m** from the crossing. The canopy stood **114 m** away along the building, at the far end, where the arena's own mass hides it from every viewpoint on Flatbush. | Found on `landmark_barclays_center`, whose viewpoint is recorded as *"looking south-east at the oculus entrance"* and whose frame shows no canopy at all. The frame was the evidence and the frame was not the fault: the arena is in the middle of it at close to the photograph's scale, the three weathering-steel bands and their glazed slots read correctly, and the height probe lands **17 of 17** rays and reads **40.36 m** against the catalogue's published **42.1 m**. The canopy was 276 m away behind the building. The same shape as J57, J60, J68, J70, J72 and J74 -- **a correct measurement of something other than the thing it stands for.** The smallest interior angle is a real property of the polygon and it is not the entrance. The script's own header said the entrance is at the Atlantic/Flatbush corner; the code went looking for it with a proxy instead of the coordinate, and no view of the model was ever taken from the street the plaza is on -- the catalogue's `atlantic_flatbush` render used a hardcoded 45 deg azimuth, which pointed at a flank. | Done. The entrance end is now the **convex** footprint vertex nearest the Atlantic x Flatbush crossing, and that crossing is a measured coordinate -- the road network's own intersection node, named in the source and recorded in the catalogue entry as `oculus_corner_from`. Convex, because the canopy cantilevers outward and a reflex vertex would throw it into the building; the polygon's winding is computed rather than assumed. The named catalogue view now takes its azimuth from the corner the canopy points out of instead of a constant, so a wrong corner cannot render as a right one again. Rebuilt: the canopy's centre now stands at NYC_TM (-2184.7, -1849.7), **158.2 m** from the crossing, and from the sheet's own camera it is **148.4 m** away at bearing **113.3 deg** -- inside the 27.2 deg half-frame and nearer than the arena behind it, where before it was 276 m away and occluded. **The sheet itself is not re-rendered yet**: the v15 pass had already passed this item and re-rendering it now would race the pass, so it is re-rendered in the v16 pass (`docs/verification/RENDER_PASS.md`) and its assessment is written against the new frame, not the old one. **Confirmed on the first re-render:** the canopy stands at the near corner and the sightline finds the arena over its own fabric, 8 of 13 rays. |
| J82 | **Both Brooklyn Bridge subjects name a tower and stand a hundred metres from it, on the deck, and J75's own test passes them for it.** `landmark_brooklyn_bridge_from_dumbo`'s subject is called *Brooklyn Bridge Brooklyn tower* and stands **105.2 m** from the nearest tower geometry in `b_brooklyn_bridge`; `landmark_brooklyn_bridge_walkway`'s is called *Brooklyn Bridge Manhattan tower* and stands **103.6 m** from one. Measured the same way, the four bridge-tower subjects that are right measure **1.2, 1.2, 3.7 and 9.7 m**. Both coordinates are out on the main span, which is why the height probe at the DUMBO one lands **17 of 17** rays and reads **44.2 m** off `lm_b_brooklyn_bridge.82` -- the deck -- where the model's towers top out at 85.0 m and the catalogue publishes **84.3 m**. | **J75's test asks whether the subject is on the bridge, and both of these are.** That was the right question for a coordinate that had fallen into the river -- the Manhattan Bridge's was 29.7 m off its own carriageway -- and it is the wrong question for one that has fallen onto the roadway a hundred metres from the tower. The same shape again: **a correct measurement of something other than the thing it stands for.** What it costs on the DUMBO sheet is the whole frame: the recorded azimuth **288.3 deg** is derived from that coordinate and agrees with it to 0.1 deg, while the tower it names bears **273.4 deg** at 199.3 m, so the aim is **14.9 deg** off the subject and the tower lands at the left edge of the frame with its top cut off -- 22.2 deg above the lens against a 21.1 deg half-frame. The sightline then reports `subject_visible: false` with **4 of 5 rays into nothing out to 685 m**, and it is right: it aims at *the subject's mid-height*, 22.1 m, which for a deck at 44.2 m standing on piers is open air under the bridge. The tower is in the picture. The record's subject is not. | **Measured, published, not corrected**, on the rule J75 states for its own six: the only anchor for a suspension tower is the model built to stand for it, and moving a reference onto a model is validating the render against itself. The independent sources have nothing to offer here -- OSM carries a single `Brooklyn Bridge` node at NYC_TM (-3917.4, 632.8), mid-span, which is the model's own origin to within a metre, and no tower node at all; the OTI footprint table does not carry bridges. **What is added is the check that was missing.** `pipeline/tests/test_reference_subject_positions.py::test_a_subject_that_names_a_bridge_tower_stands_on_that_tower` holds the six bridge subjects whose model carries tower geometry to **30 m** of it -- a threshold chosen in the gap between 9.7 and 103.6, not derived -- and pins these two at their measured distances, so the fault cannot silently get worse and cannot be silently repaired without this entry and the test disagreeing. A second test asserts that anything pinned is declared here. The Queensboro is a cantilever bridge whose model names no tower, and the Bronx Whitestone, Kosciuszko and Throgs Neck have no hand-built model, so none of those four is claimed. Both sheets say on their own face that the frame contains the tower and the record does not. |
| J83 | **Every daylight comparison render was developed about a stop and a half under the photograph it was paired with, at every tone level, and the constant that set the level was solved to a convention the photographs do not follow.** Measured over the 164 daylight sheets of the v15 pass (`frame_stats.json`, display luminance): render/photograph median ratios **0.36 at p05, 0.52 at the median pixel (0.243 against 0.454), 0.62 at p95 (0.554 against 0.875), 0.44 in chroma**; a per-sheet fit `render = gain * photo^gamma` gives **gamma 1.07 and gain 0.59** -- a level, not a contrast. The level is `SUN_CALIBRATION = 286`, solved so that a sunlit 0.26-albedo ground lands at 150/255 through Filmic, and the renders do land there: on the fourteen best-lit frames (Sun behind the camera, above 30 deg, view clear 120 m) render p95 is **0.592 = 151/255**, the target to a pixel -- while the photographs' p95 sits at **0.830** and their median pixel at 0.454, which is 0.18 in linear light: middle grey, where average metering puts a scene. The sky's *level* moves with the rest (render sky 0.36 against 0.63 in the photographs on the skyline frames), and whether its level *relative to sunlit surfaces* is also short is **not settled**: sky over sunlit surface reads 0.61 in the render against 0.75 in the photographs at the stated sky criterion and 0.58 against 0.61 at a stricter one, a selection-dependent number that is recorded here as open rather than claimed either way (the diffuse constant, `DIFFUSE_KEY_W = 90` W/m2, has no measured source in the repository). It is not the bounces (two diffuse bounces cannot make a 2.8x floor deficit), not clamping (none set) and not the denoiser. The shadow floor is a second, separate thing: the photographs' JPEG floor sits at 0.05-0.10 whatever the scene, the render's Filmic floor at 0.008, and that gap is confined to p05/p01 and scales with how much of a frame is in shade. | The calibration measured something true -- a 0.26-albedo patch at 150/255 -- that is not what a photograph of a street exhibits, because every reference photograph was **metered by its camera**, and the comparison was between a metered picture and a fixed-exposure one. It survived because every sheet's assessment explained its own darkness locally: the Chrysler's canyon, the Flatiron's shade, 40 Wall Street's look-up. Six frames were refused as near-black under it, and the refusal was reading the convention, not the city. | **Done: the development is metered.** The physical Sun and sky are unchanged -- their balance is the one thing here that matched -- and the frame is rendered in linear light, its **median scene luminance is placed at middle grey** (`meter_exposure`, `METER_TARGET_LINEAR = 0.18`, the convention average metering follows), and the stops that took are published on every sheet with the linear median and percentiles the decision was made on (`lighting.development`). The stops **are** the measurement: a scene that needs +4.5 stops to read as a picture is a scene three and a half stops darker than the photographed one at the same physical light, and the assessment reads that number instead of a luminance ratio. Clamped at -3 and +6 stops so a frame from inside a wall cannot be developed into a picture; above +4 the record says *under-lit*. Night frames keep their fixed exposure, which was calibrated against a night photograph's own histogram. Measured on the Flatiron, the first sheet re-rendered this way: mean **0.567 against 0.5615 (1.01, from 0.425)**, p95 0.933 against 0.950, chroma 0.743 (from 0.594), at **+4.51 stops** where the physical rule had granted +1.03. Two things the reader must know: the absolute stops on a sunlit open frame still carry the calibration's own level, so it is the *differences* between sheets that are scene evidence; and the photographs' JPEG floor is registered here as the photographs' processing, not a renderer fault, and an assessment does not count a p05 deficit under 0.05 against the render. Every daylight sheet is re-rendered under it. **This supersedes two earlier entries**: I16, which registered the physically-lit-against-metered gap and, on fourteen scenes, called it a dispersion and not a bias -- on 164 it is a bias, 91 % of daylight frames below their photograph -- and J67's stated decision that the renderer *deliberately never stops down*, which is withdrawn: a fixed exposure beside a metered photograph is not a comparison, and the physical light is still on every sheet as the stops it took. Two more facts a sceptic added: the two frames metered first needed corrections **3.9 stops apart** (Bethesda +0.60, the Flatiron +4.51), which is the measurement that rules out any single-constant re-solve; and the Flatiron's shadow floor closed too (p05 0.022 to 0.133 against 0.078), so the 'JPEG floor' the diagnosis attributed to the photographs is at least partly the fixed exposure. |
| J84 | **Every dataset-placed kerb-side prop in the city faces north, because no loader ever wrote it a heading.** Measured over the 1,648 tile `props.parquet` files: `heading` is NaN on **3,380 of 3,380** `bus_shelter`, **9,864 of 9,864** `bike_rack`, **2,251 of 2,251** `linknyc`, **360 of 360** `newsstand`, **13,341 of 13,341** `bus_stop_sign` and **17 of 17** `bike_shelter` (and on all 109,724 hydrants, which are round and do not mind). The loaders in `furniture/datasets.py` (bus shelters, LinkNYC, newsstands, bike shelters, GTFS bus stop signs) never assign `cols["heading"]`, and the OSM bike rack gets one only from an OSM `direction` tag, which almost none carry. Both consumers turn NaN into yaw 0 -- `blender/verify/scene.py` and `unreal/.../build_levels.py` `heading_to_yaw(float(row.get("heading") or 0.0))` -- so every shelter, kiosk and newsstand stands square to north whatever its street runs. | **Found while closing J58's Citi Bike remainder**, whose plan assumed the docks could take "the kerb-parallel bearing the other kerb-aligned kinds already use": they use none. The only headings in `props.parquet` come from the OSM `direction` tag, the oriented-envelope long axis of planimetric polygons, and the rule-placed lamps and manholes. The picture is a bus shelter across the sidewalk on every east--west street, a LinkNYC screen facing the traffic on every north--south one, and 13,341 bus stop blades edge-on to the bus. It is not hard to fix and it was not fixed here because it would rewrite the rows of six more kinds in most of the 1,648 tiles, which is outside a stage confined to one kind. | **Closed on 2026-09-09 (Stage 56).** `furniture/kerb.py` is one post-pass after dedupe, beside the Citi Bike expansion, that fills the NaN headings of the six kinds from two things the sources hold -- the **axis**, `citibike.station_axes` on the nearest *roadway-class* CSCL centreline within 25 m (`rw_type` 6 path, 7 step street, 12 non-physical, 13 U-turn, 14 ferry excluded: with every class allowed 38 shelters, 4 Links, 1 newsstand, 119 racks and 70 bus stops took a different line, 128 of them 30 deg or more off; BR02066 at Dekalb Av & Lewis Av took the *Roosevelt I Houses ped path* and now takes Dekalb Av), and the **side**, the sign of the cross product of the segment's local tangent with the vector from the foot of the perpendicular to the prop (`kerb.side_of_centreline`), which is geometry read from two sources and says where the roadway is -- and one **rule per kind for the facing**, stated in `kerb.RULES`, `build_summary.json` `kerb.rules`, every row's `attrs.rules` / `attrs.facing` / `attrs.heading_rule`, DATA_CONTRACTS §8.2 and the catalogue: bus shelter, bike shelter and newsstand `away_from_roadway` (glazed back and service door to the kerb, open front and serving window to the footway), LinkNYC and bus stop blade `along_kerb` (slab and two-sided blade perpendicular to the kerb, screens and legend reading along the street -- the row above read the Link the other way; this is the reading the register now holds, and the tablet side is a coin taken as the axis direction), bike rack `toward_roadway` (hoop along the kerb). No MTA, DOT, LinkNYC or CityRack siting document is held in the repository, so each facing is named as a rule with its physical basis, and a later stage can flip one kind in `kerb.FACING` without touching the geometry. **Read back from the rebuilt tiles: 27,926 of the 29,213 rows carry a heading, 1,287 stay NaN with `axis_source = none` and no `facing`, drawn facing north as before.** Per kind: `bus_shelter` 3,376 of 3,380 (4 NaN, nearest roadway 37.2-59.1 m), `linknyc` 2,251 of 2,251, `newsstand` 341 of 360 (19 NaN in plazas, nearest roadway 26.5-64.7 m, one with only a path in reach), `bike_shelter` 17 of 17, `bike_rack` 8,723 of 9,864 (1,141 NaN: 819 more than 100 m from any roadway -- New Jersey, campus and park racks, median 1,541 m -- and 116 whose only line within 25 m is a path or step street), `bus_stop_sign` 13,218 of 13,341 (123 NaN, 63 beyond 100 m). Distance to the accepted segment: shelters p50 9.1 m p95 13.2 p99 15.8, Links p50 7.7 p95 11.8, newsstands p50 9.0 p95 17.6, racks p50 7.8 p95 13.8, bus stops p50 6.9 p95 12.0 p99 15.8. A wider acceptance (40-50 m) was measured before the value was kept: it gains 2 shelters, 16 newsstands, about 150 racks and 45 bus stops by snapping plaza objects to a road they do not stand on, so 25 m stays. **The side is geometry, and two things say so.** Recomputed from `segments.parquet` for 200 random rows per kind, `attrs.side` agrees on every one and the front is within 0.0 deg of the perpendicular (or the axis) of the accepted segment; over all 27,926 rows, 34 (30 racks, 4 shelters) have a front more than 5 deg off the offset vector -- 53 at any threshold above 0.01 deg, up to 84.8 deg, and 61 along-kerb rows whose offset is not perpendicular to the heading -- because the prop projects onto an endpoint or a bend of its segment -- the heading is the perpendicular to the axis there, and the half-plane is right on all 33. And the shelter dataset's own `Corner` quadrant, which never enters the computation, agrees with the derived side on **3,179 of 3,374 testable shelters (94.2 %)**; of the 195 disagreements 127 stand on an axis within 20 deg of a diagonal (Brooklyn's grid, where a compass quadrant does not separate the north side from the east side), 1 has the cross street as its segment and 67 are listed by `shelter_id` in the summary. Split: shelters left 1,752 / right 1,624, Links 1,142 / 1,109, newsstands 174 / 167, bike shelters 10 / 7, racks 4,389 / 4,334, bus stops 6,500 / 6,718, `on` 0; **rows within 0.5 m of the centreline, geocoded into the roadbed, where the toward/away choice is a coin: 1 shelter, 7 newsstands, 18 racks, 119 bus stops**, counted as `side_from_offset_under_0_5m` and not smoothed. **The nearest centreline is not always the street the object serves**, so for the two kinds whose source names it the nearest roadway segment within 25 m carrying that name is taken: 3,064 of 3,380 shelters match their `On_Street` (43 differ from the nearest, 25 by more than 30 deg; 312 fall back to the nearest, among them BR02154 at Shell Rd & Av X, whose Shell Rd is 29.9 m away and McDonald Ave 10.1 m), and 12,288 of 13,332 bus stops match the first street of their GTFS name (421 differ, **380 by more than 30 deg** -- blades that would stand edge-on to the bus under pure nearest; 930 fall back). The name match (AVENUE/AVE -> AV, ordinals dropped, E/W/N/S) is a rule and was not widened to chase the count. An OSM `direction` tag would win over the derived heading and be recorded as `heading_source = osm_direction`; measured, that is **0 of 9,864** racks. **Nothing else moved:** 1,883,278 rows before and after, `counts_by_kind` identical for all 34 kinds, `counts_by_dataset` and the z statistics identical, every other kind's every column byte-identical when keyed by `prop_id` -- except kind 7 itself: `prop_id` is the row's rank within its kind by (x, y), the 17,302 bikes moved 1.010 m, so **64,099 of the 112,021 Citi Bike rows carry a new `prop_id`** and 37 bikes crossed a tile boundary (`rows_per_tile` differs on 32 tiles), churn the engine's manifest sees although nothing but a heading and a setback changed; **1,053 of 1,648 tiles rewritten** -- the 1,052 that hold one of the six kinds plus `t_4_-2`, which holds only a Citi Bike station whose facing flipped (see J58) -- and the other 595 `props.parquet` files byte-identical to the pre-run copies. The pass costs 4.2 s of a 76.0 s stage (`timings_s.kerb`). **The consumers draw it:** `scene.add_props` instanced in bpy around shelter BR0049 (Atlantic Av & Court St) puts the shelter's rotated +Y exactly on the unit vector of its heading, pointing away from the foot of the perpendicular (dot 1.0000, 0.0000 along the tangent), and three bus stop blades' +Y lie along their segment's tangent (|dot| 1.0000); Unreal's `heading_to_yaw` is held to the same convention by a pure-python test of the four cardinal bearings. `test_kerb_kinds_carry_no_heading` is inverted into `test_kerb_kinds_carry_the_kerbs_heading` (the contract identity row by row, with the NaN counts above pinned as ceilings) and `test_kerb_side_is_geometry_read_back_from_the_segments`. **Still open here:** a prop on a median or a service-road island of a divided roadway (Park Av, Queens Blvd, Eastern Pkwy, Ocean Pkwy) has a roadway on both sides and `away_from_roadway` is away from the nearer one only -- not counted in this stage; the 1,287 NaN rows above; and the engine's `props.json` manifests are stale until the Unreal manifest stage is re-run, which `test_the_engines_prop_manifest_is_not_older_than_the_props_it_lists` holds red until then. |
| J85 | **Tenth Avenue dives eight metres into a rail yard that no longer exists, and the whole Hudson Yards public square sits six to ten metres under its own survey, because the terrain is a bare-earth flight of 2013 and nothing in the pipeline could put a deck on it.** Measured on the published tiles before this stage: the heightmap at NYC_TM (-4245.81, 5899.01), on Tenth Avenue between W 31st and W 33rd, read **-0.90 m**; under the DoITT roadbed polygon 12350003663 (the avenue across the yard) 1,149 samples read min -2.0, median **3.41**, max 9.64 m; under the OTI footprint of the Shops podium (BIN 1088961, 18,093 m2) 4,525 samples read median **2.37 m**; the avenue's own seven planimetric spot elevations along that stretch read **5.17 -> 9.20 m** (source ids 12300022329..12300023052) and the square's five post-2014 ('New') spots **10.05-12.34 m**, and the heightmap under them read -0.23 to 2.90 m. `roads/apply_terrain_z.py` then draped the at-grade avenue onto it: segment 1267 (10 AVE, CSCL level 13) vertices **5.15, 0.60, -0.65, 9.39 m**, lanes 40558-40561 likewise, node 21199 (10 AVE & W 31 ST) 5.15 m. Three sheets stood in it: `landmark_15_hudson_yards`' camera at z 0.27 m, 6 m below the street at the node (5.09 m) and 7.5 m below the deck now there (6.65 m), rendering half soffit; `landmark_hudson_yards_vessel` and `landmark_the_shed` on terrain of 3.14 and 3.18 m under a plaza the model declares at 7.82 m. The same flight holds **Eleventh Avenue's bridge over the yard** (DoITT transportation structure 12230000085, 5,236 m2) at 1.2-2.9 m where the avenue's 'Unchanged' spots read 7.67, 8.54 and 9.42 m -- bridge decks are classified out of a bare-earth product -- and, east of Tenth Avenue, the Manhattan West cut under spots that read 7.5-7.9 m (heightmap 0.95 and 3.59 m). | **Every existing repair was aimed at something else, and the tempting constant is a correct measurement of the wrong thing.** ADR-018's `repair_sub_datum` names this very epoch fault in its docstring and repairs land below **-2 m** only; a yard floor at +2.8 m is above it. `densify` rejects any survey point 3 m or more from the DEM as an outlier, so on the yard floor the real street points 5-10 m above it are exactly what it throws away (`terrain.json` t_-5_5 counted them: rejected_spot 13, rejected_bldg 15); dropping that test city-wide would re-admit every bridge deck and roof-fallen ground. The deck path (`hydro.py`, pier/jetty decks) admitted only planimetric HYDRO STRUCTURE polygons at a constant elevation. And the number that looked ready to burn -- the landmark catalogue's `origin_tm` z **7.8232 m** for `c_hudson_yards` -- is `c_common.ground_of`: the **mean of the OTI ground elevations of six footprints** (16, 28, 30, 31, 40 and 9 ft; the podium's own is NaN) across two epochs, from 10 Hudson Yards' real street corner at 4.88 m to the Vessel's 2013 yard floor at 2.74 m. It is not a survey of the deck, and a flat deck at it would step Tenth Avenue **2.5 m at W 31st and 1.6 m at W 33rd**, because the avenue grades. The register's recurring shape, again. A second constraint decided the mechanics: the 2 m source mosaic the tiles were cut from is **gone** (`src2m_removed.json`, 1.76 GB freed for the building-shell run) and re-ingesting the one 1 m product both tiles need is a ~525 MB download onto a disk with 1.6 GB free, so the tile pass cannot be re-run. | **Done, as a table the terrain stage reads, with two sourced rows (ADR-022, DATA_CONTRACTS section 3.1).** `terrain/platforms.py` holds a register of **platform decks**; `terrain/hydro.py` rasterises them beside the pier decks and `terrain/tiles.py` burns them through the same step. Extent from real polygons only: for Hudson Yards, the block bounded by the road network's own four intersection nodes (21145, 21201, 21146, 21143) minus the DoITT roadbeds of the three streets on the ground, plus roadbed 12350003663, the carriageway's east-sidewalk **frontage** (the part of DoITT sidewalk 12380000504 nearer to that roadbed than to any other -- the whole polygon rings the block to Ninth Avenue and a first attempt burned 155 real samples there by up to 11 m and reached the tile's east edge) and four curb strips: **62,447 m2**; for Eleventh Avenue, the DoITT bridge polygon itself. Elevation from the survey only: the inverse-distance-squared mean of the eight nearest survey points within 240 m -- `survey_fill`'s points, weights and reach, **without** its growing-radius rule, which on a road surveyed every 25-55 m made a staircase (measured on the first attempt: 5.62 m flat, 7.22 m flat, 8.82 m flat, 1.6 m risers) -- with every footprint ground inside an extent excluded (seven, from the Vessel's 2.74 m to a 16.76 m roof-fall) and no spot excluded (the plan's candidate, spot 12300022620 at 3.75 m, is on W 30th's centreline and agrees with the heightmap to 0.02 m). Burned only where it stands above the ground (`z = max(z, deck)`), so the block's un-excavated corner keeps its measurement. Applied to the published tiles (decode, burn, re-quantise; `apply-to-published`), which agrees with a rebuild to one 2.5 mm quantum inside the extents and can differ outside at **1** sub-datum sample on t_-5_5 (`rebuild_may_differ_px`). **Measured after:** t_-5_5 11,318 samples burned of 12,262 in extent (944 kept), t_-5_6 4,573 of 4,614; every changed sample inside an extent, the north/south seam row changed identically on both tiles, `verify` 5,724 seam pairs at 2.8e-14 m with 0 violations and 8/8 known elevations. The pit sample **-0.90 -> 6.65 m** (the linear grade between its two neighbouring spots is 6.58); the heightmap passes through the twelve control spots to |dz| <= 0.023 m (0.136 at one intersection spot, 12300022533, where the flight's ground already stood above the spot and is kept; the 0.111 m first quoted beside it belongs to spot 12300023052 outside the extent, which did not change); Tenth Avenue's centreline rises **5.40 -> 9.33 m**; roadbed 12350003663 median 3.41 -> **6.15 m**; podium footprint median 2.37 -> **8.79 m**; Vessel footprint 2.80 -> 11.46 m; Eleventh Avenue's bridge 2.78 -> 7.8-9.1 m. Re-lifted: segment 1267 **5.40, 7.45, 8.28, 9.39 m**, its four lanes likewise (the heightmap profile between them dips under 0.06 m at 10 m stations and bumps 0.32 m where kept flight ground reaches 9.63 m short of the node), node 21199 5.405 m (spot 12300022533: 5.31), node 21145 unchanged at 6.921, `min_ground_z_m` -5.124 unchanged city-wide; runtime binaries re-exported; pavement rebuilt (t_-5_5 roadbed mesh on that stretch -1.59..10.69 -> **4.99..10.69 m**, drape p99 62 -> 45 mm, pierced fraction 0.00164 -> 0.00082, 334 curb ramps unchanged; t_-5_6 p99 113 -> 120 mm, 179 ramps), structures and parkground rebuilt, the Unreal manifest re-hashed. **What it did not settle, stated:** the Stage 24 ground test now refuses the model's plaza from the other side -- heightmap median under it **8.88 m, the catalogue's 7.8232 is 1.06 m below it** (was +5.34 above), so the terrain will draw through the model's plaza on the next Vessel and Shed sheets until the landmark datum is re-derived from the survey (`Group(ground_z=...)`, a landmark decision, not a terrain nudge); W 33rd's roadbed reads 6.26 m against a 'New' spot of 9.64 m at (-4313, 6054), leaving a 3.4 m step at the platform's north kerb; Manhattan West and that regrading are named in the register as candidates without an extent; the seven Hudson Yards BINs' `buildings.parquet` ground_z (podium 5.83, Vessel 2.74) are untouched and their shells sit inside the landmark model; the roadbed stub 12350003662 off Eleventh Avenue is decked at street level with no spot on it. **Viewpoints:** `landmark_15_hudson_yards` moved 130.2 m onto node 21145, the crossing its note names, azimuth 280.0 -> 310.4 deg (the old azimuth pointed at the subject from the old coordinate to 0.1 deg: J75's shape), subject 245.7 m away against the note's 'about 250 m', with `lat_was`/`lon_was`/`azimuth_deg_was` and the source; the Vessel and Shed viewpoints stand **52.1 m and 8.3 m inside** OTI footprint 1088961 (the podium the model builds as a 34 m plinth) and are **measured, published, not moved** (J82's rule; their meta.json carries the audit and a real-polygon candidate, the DoITT plaza centroid), pinned by `test_reference_subject_positions.py`. Tests: `test_terrain.py` (burn rule, survey surface ignores the footprint grounds, in-pass == published to one quantum, the two tiles' contract and the pit's grade), the viewpoint-on-named-node and moved-viewpoint tests. **The landmark datum (the next decision, taken 2026-09-09):** measured on the published tiles after the deck, the platform under the model's 20,061 m2 of ground faces reads, at **5,009** lattice samples, **p10 5.71 / p50 8.88 / p90 11.35 m** (min 4.41, max 12.11) -- and the faces are not a plaza: **18,313 m2 is the Shops podium's plan**, which the Stage 24 rule fills from the ring of spandrel-top faces the curtain wall carries 0.7 m above the datum (IoU 0.988 with OTI footprint 1088961), 1,606 m2 is the Vessel's plaza disc and 142 m2 the Shed's rail tops; the model has no ground face over the public square at all, and the square's five 'New' spots (10.05-12.34 m; the heightmap under them 10.047-12.339) all lie outside the outlines. The deck under the faces slopes: a plane through the samples grades **4.0 % rising towards 330 deg** (residual rms 0.96 m), the west-east strips run 5.7 -> 11.6 -> 8.5 m and the south-north strips 5.3 -> 10.2 m; Tenth Avenue's east kerb (curb strips 12225000915/003524/008879/003377) reads 5.52-9.21 m, median 7.98, under avenue spots that grade 4.88 -> 8.82 m. The spread under the faces is **5.6 m p10-p90**, far over 2 m, so it is said plainly: **the model needs a sloped plaza and per-part grounds; the datum chosen is the median, not a fit.** `c_hudson_yards.GROUND_Z_M = 8.88` (`Group(ground_z=...)`, the survey cited in the constant's comment) replaces `ground_of`'s 7.8232: rebuilt in 11 s at 1,147 MB peak RSS, catalogue `origin_tm` z **7.8232 -> 8.88**, 103,450 tris and IoU 0.9593 unchanged. Residual, recorded: only **39 %** of the deck under the faces is within 1 m of the datum (31 % more than 1 m above it, 30 % more than 1 m below); the flat model stands **+4.0 m** above the survey at 10 Hudson Yards' street corner (footprint deck median 4.89; spot 12300022305 reads 4.877), **+3.6 m** under the Shed's rails (5.29), +1.7 m under the 15 HY / Shed footprint (7.16), -0.5 m at 35 HY (9.40), -0.7 m at 55 HY (9.55), -1.3 m at 50 HY (10.21) and **-2.6 m** under the Vessel (11.46) -- so along Tenth Avenue the plinth base floats up to 4 m above the kerb and in the square the terrain still rises 2.6 m through the Vessel's disc. Own ground: `above_heightmap_m` **0.00**, so the Stage 24 rule accepts the faces it refused twice; `ground_outlines.parquet` re-exported for the whole catalogue (18.6 s, 676 MB): **21** entries supply ground (was 20), 24 outlines, 99,201.7 m2 (was 79,140.3); `test_comparison.py`'s deck test is flipped to assert the cut, its area and the datum. Pavement rebuilt (82.8 s for both tiles, 1,002 MB): t_-5_5 `polygons_cut_for_landmark_ground` 0 -> **1** (DoITT roadbed 12350004041, 1,873 m2, whose representative point lies inside the podium outline, under the modelled 34 m plinth), t_-5_6 **0**; Tenth Avenue's sidewalks **12380000512 and 12380000533 are not cut** (their representative points lie outside every outline; 12380000533 overlaps the podium outline by 20.8 m2 of 431.6); t_-5_5 849,119 triangles (was 850,170), drape p99 44.9 mm and pierced 0.00082 unchanged; park ground rebuilt, its counts unchanged. **Viewpoints moved:** `landmark_hudson_yards_vessel` and `landmark_the_shed` onto the centroid of the DoITT public-plaza polygon named above (feat_code 6000, status New, source_id 0 -- the id every 'New' planimetric record carries, as for the square's spots -- 1,571.0 m2, feature 713 of `plan_public_plazas.geojson`, centroid NYC_TM (-4394.66, 5895.14) = (40.753074, -74.002040), 13.0 m outside footprint 1088961), 99.8 m and 102.7 m from where they stood; azimuths 288.3 -> **350.5** deg (the Vessel, 81.7 m) and 290.0 -> **291.3** deg (the Shed, 77.8 m), each the bearing to its subject, with `lat_was` / `lon_was` / `azimuth_deg_was`, the source and the audit block (`moved: true`, the old 52.1 / 8.3 m kept) in each meta.json; the heightmap at the new point reads 7.05 m (the deck rule between the avenue's and the square's spots, not a survey of that point); `test_reference_subject_positions.py`'s pin is inverted (inside the plaza polygon, outside the footprint, `lat_was` still 52.1 / 8.3 m inside; 10 passed); `audit_viewpoint_aim` reads 59.1 m HIGH LINE / 16.1 deg and 59.1 m W 30 ST / 7.8 deg. `landmark_15_hudson_yards` is unchanged. Two consequences of the move, measured and not hidden: the sheet chooser ranks the photographs by azimuth error against the item's azimuth, so on its next render the Vessel sheet will show 'The Vessel October 2022 006' (camera azimuth 296.0) in place of 'The Vessel April 2022 004' (290.1) -- `test_no_comparison_scene_silently_changed_which_photograph_it_shows` says so until the render pass reaches that sheet; and the three Vessel photographs' own camera GPS fixes stand **20.4-21.9 m inside** footprint 1088961 (71-98 m from the new viewpoint), so either the OTI polygon includes open square -- a correct outline of something other than the Shops building -- or three phones erred the same way; not decided here. **Still not settled:** a model that follows the 4 % grade (per-part grounds, a sloped plaza, and a ground face over the public square so that the rule has something real to accept there); the seven BINs' `buildings.parquet` ground_z; W 33rd's 3.4 m step; Manhattan West. |
| J86 | **The city's woodland is on disk as coverage and the build never read it: the Ramble, Van Cortlandt's forest and every wood in the five boroughs render as bare terrain because the only tree sources are point inventories.** Measured before this stage: the 2015 census has **0 rows inside Central Park** (a street inventory); the OSM `natural=tree` nodes put **1,472** trees in the park and **7** inside its **41.91 ha** of `natural=wood` polygons; `osm/landuse_leisure.parquet` holds, inside the five boroughs, **wood 2,144 / forest 365 / scrub 1,426 polygons, 5,557.1 ha in union**, and **94.5 % of them hold zero trees** of either inventory; and the repository holds and registers **no canopy raster, no LiDAR point cloud and no per-tree inventory of any NYC woodland** (`sources.py`). The polygon's extent is therefore the only measured claim about a wood, and the verification scene drew nothing under it (the scene half -- park ground and the far-tree LOD -- is reported with `blender/verify/scene.py`'s change). | **D10 read the point layer of the extract and never the polygon layer of the same extract**, and the register's recurring shape did the rest: the 49,175 OSM trees were a correct measurement of the trees a mapper walked past, and D10 quoted them against a published canopy (Central Park 1,566 against ~18,000, '8.7 %') as if the shortfall were a matter of more points. It is not: no point source of the woodland exists, so the honest move is a declared rule over the one real claim, not a wait for a dataset that is not coming. | **Closed on 2026-09-09 (Stage 55, pipeline half) as a rule, `rule:woodland_canopy`, whose every stem says it is inferred.** `furniture/canopy.py` fills each wood / forest / scrub polygon (clipped to the borough land and the scope box -- **3,952 polygons, 5,561.9 ha** counted this way, against the scoping figure's 3,935 and 5,557.1 ha in union) minus a 2 m edge inset (5,137.3 ha; 175 polygons too thin for it), the roads-stage pavement (**46.0 ha** over 644 polygons), open water (35.1 ha over 433) and the built park surfaces (0.31 ha over 10): **5,055.9 ha plantable**. Stems are a deterministic Poisson-disc sample seeded from (wood osm_id, 5 m cell, round) through SplitMix64, suppressed within **6.0 m** of any census or OSM tree (the measured 5.0 m cross-source radius plus one metre; `dedupe.CROSS_SOURCE_RULES` holds the same radius against both inventories as a second line and **dropped 0 of 1,752,719**), and placed until the projected crown area of the placed stems plus the 387 dataset trees standing inside the polygons (2.17 ha credited) reaches the plantable area -- crown widths read from the asset catalogue's `nominal_size_m` at the scale the consumer draws each stem, so the crown closed over is the crown shown. **Read back from the rebuilt tiles: 1,752,719 stems** -- wood 904,430 / forest 132,291 / scrub 715,998; Staten Island 849,879, Queens 412,420, Bronx 229,281, Brooklyn 222,228, Manhattan 38,911; **Central Park 9,779** (`parks_properties` signname; the same polygon holds 1,468 OSM trees after dedupe and no census tree -- D10's 1,566 was counted over the OSM park way, a different polygon), the Ramble bbox NYC_TM (-1984, 8384)-(-1519, 8829) **2,325**, **Prospect Park 17,408**; 17,945 candidates suppressed next to a dataset tree, 445,064 fell on pavement, water or a built surface, 28.5 M of 102.4 M candidates rejected by spacing over 16 rounds; 1,891 polygons closed, 582 jammed, **1,304 still open at the 16-round cap** -- roughly 1,269 of the 1,886 unclosed polygons are scrub, which no round count can close (see below). Species: 1,750,948 from the borough's census species pool (a street-population proxy, `species_from = census_pool`), 1,367 from tagged OSM neighbours inside the same polygon (2 polygons), **404 stems in 3 needleleaved polygons drawn with the broadleaf fallback and `species_substituted = true`, species left empty** -- no conifer asset exists and none is invented; heights are the OSM-tree draw (`height_source` 4), `variant` 3, `dbh_cm` 0. **The per-hectare figures are a consequence of the rule and are reported under that name, never as a measurement -- crown closure over the catalogue's crown widths for wood and forest (1,497 of 2,492 polygons close, area-weighted crown/plantable 0.93), and for scrub the jam density of the 3 m spacing constant (54 of 1,323 close, area-weighted 0.54: small assets carry 2.3–4.1 m crowns and closure at 3 m spacing needs 3.15 m even at hexagonal packing), a correction the sceptic made to the first wording, which had written crown closure on the scrub count too**: wood/forest **262.6 stems per plantable ha** (239.5 per polygon ha), scrub **646.2** (580.8; the small size band at 3 m spacing, since no shrub asset exists). The rule text stands verbatim in `build_summary.json` `canopy.rules`, `props_catalog.json` `rules` and every row's `attrs.rule`; the catalogue's tree kind names the rule as its third `dataset_id`; DATA_CONTRACTS section 8.3. **Nothing but the tree kind moved:** `tree` 699,691 -> **2,452,410**, `counts_by_kind` identical for the other 33 kinds, `counts_by_dataset` identical for every dataset; **1,680 tiles** (32 new ones holding only canopy rows), **1,201 rewritten** -- 602 that hold a stem and 599 that hold no stem but a tree, because a tree's `prop_id` is its rank within the kind by (x, y) and every tree re-ranks (J84's churn) -- and 479 byte-identical. Stage time **76.0 -> 511.7 s** (canopy 413.8 s), tiles 98.9 -> 164.8 MB, peak RSS **5.0 GB** against the 3 GB the plan asked for. Tests: the ten `test_canopy_*` (determinism and order independence, inset, exclusion, suppression, closure on one hectare, scrub band, needleleaved flag, crown table against the consumer's own asset choice), the dedupe rule, the catalogue naming, the summary's own declaration and every canopy row in the tiles carrying the rule. **What this is not, said plainly: the city-wide count is a consequence of crown closure (wood, forest) and of a spacing constant (scrub), not a measurement of any tree**, and a different catalogue or spacing gives a different count; 1,304 polygons stop short of closure at the round cap, most of them scrub that cannot close; scrub is drawn as the smallest tree assets because no shrub asset exists; the 5.0 GB peak and the 414 s are the cost of a 7.3 M-cell lattice and stand until the lattice is tiled; the engine's `props.json` manifests are stale until the Unreal manifest stage is re-run (`test_the_engines_prop_manifest_is_not_older_than_the_props_it_lists` holds red until then). **The eventual per-tree answer is a LiDAR crown segmentation** (the 2017 or 2021 NYC LiDAR, once ingested and registered in `sources.py`), which would replace every stem under a wood polygon with a measured one and retire this rule. **The first render of the stage showed three faults the numbers had not (the render sceptic, Bethesda Terrace):** 27 % of the canopy band was solid black because Cycles' `transparent_max_bounces` stood at 2 and a view ray through the Ramble crosses 48-88 stems of three alpha-cut planes each, so it was terminated black at its third crossing and the wood threw an opaque shadow -- the cap is now 256 (`render_sheets.py configure_cycles`); every tree stood at yaw 0 because no inventory measures which way a tree faces, so 5,494 identical silhouettes met the camera with the same plane -- a tree's yaw is now a declared rule, a hash of its own position, recorded per frame as `scene.props.canopy.yaw_rule` / `yaw_by_rule` (`scene.TREE_YAW_RULE`); and the Lake had turned to lawn because `build_parkground.py` subtracted the pavement from a park property polygon but not the hydrography, and Central Park's property contains the Lake whole -- open water is now subtracted the way pavement is (`open_water_subtracted_m2` in every `parkground_manifest.json`; 948,509 m² over the 133 tiles, 84,853 m² on t_-2_8), and the 133 tiles were rebuilt (205 MB). The Bethesda sheet was rendered once more under all three before the pass. |
| J87 | **The clearance walk and the deck rule are each correct and compose into a frame with no subject in it, and the sightline then certifies that frame as a success.** Found by opening the MoMA sheet of the v16 pass. The recorded viewpoint is boxed in — the view azimuth closes 4 m ahead against the 25 m the frame needs — so the walk moved the camera **44 m to the left** to the nearest open air (J79). That point lies over `lm_moma.15`, the museum's own level deck at 29.73 m NAVD88, so the deck rule lifted the camera out of the **7.95 m** it sat below it (J65). From a terrace **9.3 m** from MoMA's flank with a 90° lens tilted 15.6°, the delivered frame is two wall planes, the deck underfoot and Billionaires' Row's diagrid **1.4° off axis at 337.2 m** — while the subject itself sits **66.8° off axis**. Not one of the scene's 68 vehicles or 333 people is in the picture; they are on the street below and behind. **Three separate rules are implicated and each is doing what it was written to do.** (1) The azimuth is the bearing from the photograph's GPS to the subject, computed *before* the walk moved the camera 44 m, and never recomputed from the point actually used. (2) The walk ranks candidates on how much of the subject they see, and a point pressed against the subject's own wall scores perfectly. (3) The sightline reports `subject_visible_fraction` **0.923** with **12 of 13 rays on the subject**, and all 12 land on MoMA's own fabric **nearer than the recorded coordinate**, at **9.1 m** against a subject recorded **66.8 m** out: the self-fabric continuity rule (J78) exists so a building cannot hide itself, and here it accepts a blank wall at arm's length as a view of the museum. The frame is also published **below its own metered target** — it wanted **+7.56 stops** and was held at **+6.00**, the clamp's note saying that a scene this far from a photographable level is not developed into a picture of one (J83). **Nothing here is a modelling failure:** the probe casts 43 rays, all 43 land on built fabric, and measures **73.45 m** against the catalogue's **74.7 m**. The museum is modelled and the right height; this sheet is not evidence of it. Open, with three named repairs: recompute the heading after the walk, add a distance test to the self-fabric rule so a hit far nearer than the recorded coordinate does not count as seeing the subject, and refuse a candidate whose best sightline is fabric within a few metres of the lens. |
| J88 | **The clearance walk clears the view azimuth of built fabric and never weighs a street lamp, so on two sheets it chose a point with a cobra-head mast across the sightline and reported the same visible fraction of 0.077.** Found by opening the Madison Square Garden and New York Public Library sheets of the v16 pass, independently, and reading the same number off both. **Madison Square Garden**: the recorded viewpoint is boxed in at 17 m against the 80 m the frame needs, so the walk moved the camera 25 m onto a surveyed sidewalk polygon where the view azimuth is clear for **96 m** — and `prop_lamp_cobra_davit_5` stands **17.5 m** away at −9° yaw. Twelve of thirteen rays stop at **18.0 m** on it; one reaches the arena at 134.6 m. **New York Public Library**: boxed in at 35 m against 57 m, moved **28.3 m**, view azimuth clear for **69 m**, and `prop_lamp_cobra_davit_30` stands **13.3 m** away at +34° yaw. Twelve of thirteen rays stop at **14.7 m** on it; one reaches the portico at 50.6 m. Both verdicts read `subject_visible: true` on the strength of that single ray, and in both cases the subject's own height agrees with its catalogue to within a tenth of a metre — MSG 44.51 against 44.4 from an origin 0.9 m off the coordinate, NYPL 38.28 against 38.2 from 3.8 m. **So the modelling is right and the viewpoint is unusable, and the record says both.** The fraction (J78) is what makes this visible at all: under the old boolean rule each sheet would have read as a clean pass. Two rules are implicated. The walk's clearance probe tests *built* fabric along the azimuth, and a lamp mast is a prop, so a candidate with a mast 13 m from the lens scores as clear. And the walk ranks candidates on how much of the subject they see (J79) using the same probe, so the ranking cannot see the blocker either. Open, with one repair: the candidate's score must come from the same ray cast the sightline uses, props included, and a candidate whose best fraction is under about a third should be rejected rather than chosen. |
| J89 | **Two of the One World Trade Center item's three stored coordinates are wrong: its viewpoint is inside a building and its subject coordinate is on a different tower. The model itself is exact.** Found by opening the sheet of the v16 pass and then measuring the glb. **The viewpoint**: a ray cast straight up from the recorded eye point strikes the roof of `lm_b_wtc_site.243`, so the clearance walk moved the camera **166.3 m** onto the nearest surveyed roadbed — the largest displacement in the pass, and a recovery from a coordinate that is not merely awkward but unusable. **The subject coordinate**: the probe casts 43 rays, 41 land on built fabric, and it measures **292.65 m** on a 75 m square object where the catalogue says **541.3 m**. The model is not short. `blender_out/landmarks/b_one_world_trade_center.glb` carries the published values to the centimetre — `spire_beacon` **541.30 m**, `spire_mast` **542.80 m**, `parapet` **418.50 m**, `roof` and the top of `shaft` **417.00 m**, `podium_cap` **57.80 m**, `podium` **56.40 m** — against a `fidelity_statement` claiming 541.3 to the tip, 417.0 roof and 56.4 podium. What the probe measured is whatever stands at the recorded coordinate, which the record places **167.6 m** from the camera while the model's own origin is **327.4 m** from it: over a hundred metres from the tower the item names, out across the memorial plaza where the neighbouring World Trade Center towers stand. **J74 worked and that is how this was caught**: had the height still been taken from the nearest catalogue entry within 120 m, the sheet would have published 541.3 m over a frame of a different building and nothing would have looked wrong. Open; the repair is to the item's stored viewpoint and subject coordinate, not to the model or the rule. |
| J90 | **Forty-three of the ninety-nine viewpoints rendered in the v16 pass could not be used as they were stored: ten of them are inside a building and thirty-three are walled in.** Found by opening the MetLife, NYSE and One World Trade Center sheets one after another, noticing the same line in each record, and then counting it across every metered render rather than reporting three anecdotes. **Ten viewpoints put the eye inside built fabric** — a ray cast straight up from the eye point strikes a roof — and the walk had to carry the camera between **7.0 m and 166.3 m** to get it out: `landmark_one_world_trade_center` (166.3 m, inside `lm_b_wtc_site.243`), `landmark_35_hudson_yards` (75.6 m), `landmark_metlife_building` (62.2 m), `landmark_bronx_county_courthouse` (57.3 m), `drive_bronx_grand_concourse` (44.3 m), `landmark_domino_sugar_refinery` (39.2 m), `landmark_50_hudson_yards` (34.4 m), `landmark_7_world_trade_center` (26.2 m), `landmark_brooklyn_bridge_walkway` (8.0 m) and `landmark_nyse` (7.0 m, inside `lm_federal_hall.3`). Six of the ten are inside a *tile* roof membrane rather than a landmark, so this is not a landmark-model artefact. **Thirty-three more are boxed in**: the view azimuth closes before the distance the frame needs, from 2.0 m at `landmark_chelsea_market` to 76.0 m at `landmark_111_west_57th`. **In total 47 of 99 cameras had to be moved and 52 stood where they were recorded.** A separate 23 sheets could not use the photograph's own position at all, because its fix is past the 250 m at which it could be the same view or is absent — up to **1,534 m** away at `landmark_40_wall_street`. The consequence is visible on the sheets: at MetLife every one of 13 sightline rays stops on a curtain wall 20.1 m from the lens and the walk records that no point within 80 m had the clearance it needed, so `subject_visible` is published as **false** with a fraction of **0.0**. **The renderer is not at fault and neither is the walk** — both report what they found. The stored viewpoints are. Open; the repair is to the item catalogue: a viewpoint that fails the up-ray test or cannot see its own subject from within a few metres should be corrected where it was authored rather than recovered at render time. |


**One correction to I11b.** That entry says "40 of the 93 catalogue entries supply ground". 40 have an
upward horizontal face at their declared ground elevation; **20** survive the test that separates
ground from a deck standing on it, and those 20 are what the exported outlines contain. The
difference is Hudson Yards' plaza at +5.34 m, the Lincoln Tunnel portals at −9.68 m and eighteen
others. The rule was always stated correctly in the function; the count in the entry was the
pre-filter one.

**A second amendment to I11b (J85).** The +5.34 m was not the plaza standing on the ground: it was the
plaza standing over the 2013 flight's rail-yard floor, which the heightmap still held under the whole
block. With the platform decked from its survey (J85) the heightmap under the model's 20,061 m2 plaza
face reads a median of **8.88 m** and the catalogue's 7.8232 m now stands **1.06 m below** it, so the
same test refuses the plaza from the other side and the count stays at 20. The Vessel does not "hover
on a disc above the plaza" any more; on the next render the terrain will rise 1 m through the model's
plaza instead, because 7.8232 m is a mean of six footprint grounds across two epochs and not a survey
of the deck. The datum is the next decision; the test is doing its job.

**A third amendment to I11b (J85, the landmark datum).** The count is now **21**: `c_hudson_yards` supplies
ground because its datum is **8.88 m**, the median of the survey-burned deck under its ground faces
(`GROUND_Z_M`, J85), so `above_heightmap_m` is 0.00 and the rule accepts what it refused twice. Two things
the acceptance does not mean, measured. The "20,061 m2 plaza" is mostly not a plaza: **18,313 m2 of it is the
Shops podium's plan**, which the rule fills from the ring of spandrel-top faces the curtain wall carries 0.7 m
above the datum (IoU 0.988 with OTI footprint 1088961); 1,606 m2 is the Vessel's plaza disc and 142 m2 the
Shed's rail tops; there is no ground face over the public square, so the square's five 'New' spots all lie
outside the outlines and the terrain there is drawn as before. And the deck under the faces is not one level
-- p10 5.71 / p50 8.88 / p90 11.35 m, a 4 % grade from Tenth Avenue up to the square -- so the cut removes
terrain that stood between 4.4 and 12.1 m under a plinth whose base is flat at 8.88: along Tenth Avenue the
base is up to 4 m above the kerb, and at the Vessel the disc is 2.6 m under the survey. The rule is doing
what it says (a face within a metre of the heightmap's median under it is ground) and the median is the
number the decision chose; what the rule cannot see is that a median inside the band can sit over a spread
of 7.7 m. That is a limit of the rule, written down here rather than dressed as a defect: the one-metre band
was chosen for a landmark whose ground is one surface, and a graded platform is not one. The pavement cut
that follows is one polygon (roadbed 12350004041, under the podium); Tenth Avenue's sidewalks are untouched.


---

## What is *not* on this list

Two things, stated so their absence is not mistaken for an oversight:

* **Nothing was found where a procedural or approximated result is presented as real.** Every inference in
  this build carries a flag in the data (`*_source` columns, the fidelity bitfield) and a sentence in its
  stage report. The one time that failed it was in the *accounting* rather than the world: the fidelity
  report itself reported 0 % inferred facades and 0 % inferred roofs for a period, because its generator
  counted those bits in a table that does not set them. That is recorded in `docs/DEFINITION_OF_DONE.md`
  and is now guarded by a test.
* **No deviation here was discovered by an agent's self-report alone.** Each was either declared by the
  lane that made it or found by the orchestrator opening the artefact. Four defects were reported as
  successes by the stage that produced them and found only by inspection: 32.8 M kit placements pointing
  at assets that did not exist, a 1,003 km² stretch of New Jersey highland modelled as sea, a packed C++
  struct that made the road graph unreadable to the router, and a character whose garments all carried
  the wrong vertex weights.
